[pytest]
# Run from pipeline/ with the pipeline dir on sys.path, so `import config`, `import jobs.job_a`
# resolve the way `uv run python -m jobs.job_a` does in the workflows.
pythonpath = .
testpaths = tests
