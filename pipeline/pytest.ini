[pytest]
# Run from pipeline/ with both the pipeline dir and the tests dir on sys.path, so `import config`,
# `import jobs.job_a`, and `from toy_series import ...` (test infrastructure) all resolve the way
# `uv run python -m jobs.job_a` does in the workflows.
pythonpath = . tests
testpaths = tests
