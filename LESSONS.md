# LESSONS.md — mistakes and their guards

Read at session start after CLAUDE.md (plan §9.2). A lesson that repeats means the guard was
inadequate — escalate it to a test or a skill. Write each lesson in plain English so a future
session (or the user) understands it without context (CLAUDE.md, "Readability & documentation").
Format:

```
YYYY-MM-DD · symptom → root cause → fix → guard added
```

---

(no entries yet — no build sessions have run)
2026-07-10 · The font-budget check reported "no latin fonts found" on a build that clearly had fonts → I assumed the `unicode-range: U+0000-00FF` that Google Fonts serves survives into the built CSS, but Next's minifier rewrites it to the wildcard `U+??`; a second bug hid behind it, a `\b` word boundary that can never match after a `?` because both sides are non-word characters → the check now accepts all three spellings of the latin range (`U+??`, `U+0000-00FF`, `U+0-FF`) and the regex was tested against real values from the build before being trusted → guard added: the script exits non-zero when it finds ZERO latin faces, so "found nothing" can never masquerade as "budget met"; both its pass and fail paths were exercised deliberately.

2026-07-10 · `node -v` reported 20.20.0 in every shell even after `nvm install 24 && nvm alias default 24` → Claude Code itself runs on Node 20 and exports its own bin directory into the PATH of every command it spawns, which shadows nvm's default before nvm.sh ever runs → build and test commands prepend the Node 24 bin directory to PATH explicitly → guard added: `.nvmrc` (24) and `"engines": {"node": ">=24"}` in app/package.json, so CI and any human shell fail loudly rather than silently building on the wrong runtime.

2026-07-10 · Twelve of the seventeen colour tokens were missing from the built CSS while five were present → Tailwind v4 tree-shakes `@theme` variables that no utility class references, and only the five used directly in plain CSS survived; the chart hook (plan §3.7) reads `--color-ink`/`--color-up`/`--color-down` off computed styles at runtime, which Tailwind cannot see → `@theme static` forces every variable to be emitted → guard added: the phase-exit token grep now asserts all seventeen §3.3 hexes are present in the built stylesheet, so a silent token deletion fails the gate.

2026-07-10 · My own verification printed "✓ typecheck clean" over a real TypeScript error → I wrote `npm run typecheck 2>&1 | tail -8 && echo "✓ clean"`, and in a pipeline the shell reports the LAST command's status, so `tail` (always 0) masked `tsc` (exit 1) → checks now capture output into a variable and branch on the command's own exit code, never on a pipeline's → guard added: the phase-exit gate runs each step through a `run()` helper that fails loudly; a green tick in the terminal now means the command actually succeeded. A gate that cannot fail is not a gate.

2026-07-10 · `tsc --noEmit` failed on a stale `.next/types/validator.ts` referencing a page I had just deleted → tsconfig includes `.next/types/**/*.ts`, and those generated types outlive the file they describe until the next build → re-running `next build` regenerates them; a fresh CI checkout has no `.next` at all, so the plan's gate order (typecheck before build) is safe there → guard: none needed in CI, but locally, delete `.next` after removing a route rather than trusting an incremental type check.

2026-07-10 · The Playwright smoke test could not find the heading "00 — Pipeline" that was plainly on screen → SectionMasthead wrapped the em-dash separator in `aria-hidden="true"`, and the spaces around it were hidden with it, so the heading's accessible name collapsed to "00Pipeline" — a screen reader announced the index and title as one run-on word → the dash is no longer aria-hidden; every major screen reader is silent on an em dash anyway, while the spaces survive → guard added: the e2e suite locates that heading by its accessible name, so if anyone hides the separator again the test fails. Accessible names are the cheapest a11y check there is, and a test that reads the page the way assistive tech does catches this for free.

2026-07-10 · Two auth e2e tests failed on `getByRole("alert")` resolving to two elements → Next renders its own `role="alert"` route announcer (`#__next-route-announcer__`) into every page, so any bare alert-role locator is ambiguous → the login error carries `data-testid="login-error"` and the tests scope to it → guard: prefer a test id over a role when the framework injects elements with the same role.

2026-07-10 · Every jose signing test failed with "payload must be an instance of Uint8Array" → the tests ran under jsdom (the config default, correct for component tests), and jsdom's TextEncoder returns a Uint8Array from a different JS realm, so jose's `instanceof` check rejects it → lib/auth.test.ts declares `// @vitest-environment node`, which is also simply true: auth runs on the server, in the proxy and a server action, never in a browser → guard: server-only modules get the node environment; the error message points at the payload but the cause is the realm.

2026-07-10 · Auth e2e tests that had passed began failing with "AUTH_USER or AUTH_PASS_HASH is not set", after I added the app/.env symlink → an EMPTY `AUTH_PASS_HASH=` line in .env, combined with a bcrypt hash (full of `$`) passed via the test's process.env, got collapsed to "" — Next loads .env through dotenv-expand, which reads `$2b`/`$12` as variable references and eats them; an empty .env line is enough to trigger it over the process.env value → removed all empty placeholder lines from .env (an absent var is safe; an empty one is not) and, for the real local hash, escape every `$` as `\$` (hash-password.mjs now prints both a raw form for Vercel/GitHub and an escaped form for local .env) → guard added: verified escaped hashes survive loadEnvConfig and that a `$`-value with no .env line is untouched. Vercel/GitHub inject env directly and never run dotenv-expand, so only local .env needs the escaping.

2026-07-10 · The font-budget guard reported "no latin fonts" again, this time after switching the build from Turbopack to `--webpack` for Serwist → the two bundlers spell the latin unicode-range differently: Turbopack emits `U+??`, webpack emits `u+00??` → the regex now accepts every known spelling (`U+??`, `u+00??`, `U+0000-00FF`, `u+0-ff`) and was unit-checked against latin-ext/cyrillic negatives → guard holds: this is the second bundler-specific spelling to break it, so the pattern is now written to be spelling-agnostic rather than patched per-bundler.

2026-07-10 · Serwist's manifest injection is a webpack plugin, but Next 16 defaults to Turbopack, which errors the moment a `webpack` key is present in next.config → applied Serwist only when `process.env.TURBOPACK` is unset, so `next dev` keeps Turbopack (SW is disabled in dev anyway) and `next build --webpack` engages Serwist and emits public/sw.js → guard: the PWA e2e suite runs against the webpack build, so if the SW ever stops being emitted the installability tests fail. Revisit when Serwist ships Turbopack support (serwist/serwist#54).

2026-07-10 · The FMP provider probe returned HTTP 403 while all other 8 providers passed → not a bad key: FMP deprecated its entire /api/v3/ surface on 2025-08-31 and the error body said "Legacy Endpoint ... only available for legacy users prior August 31, 2025"; the key works fine on the new /stable/ API → probe now calls /stable/quote, and a comment flags that the P2 earnings-calendar adapter must use /stable/earnings-calendar (verified returning 200) → guard: when a single provider 403s but others pass, read the response BODY before blaming the key — providers move endpoints, and the body says so.

2026-07-10 · After adding my real AUTH to root .env, 12 e2e login tests started failing locally ("AUTH_PASS_HASH not set") while CI stayed green → app/.env is a symlink to root .env, so the e2e web server loaded my real AUTH_PASS_HASH line, which made dotenv-expand mangle the Playwright fixture hash (bcrypt is full of $) to empty; there is NO single hash form that survives both cases (RAW survives with no .env, ESCAPED survives with a .env — opposite in CI vs local) → tried a Playwright globalSetup that moves .env aside, but the web server starts and reads .env BEFORE globalSetup runs, so it lost the race → real fix: moved the developer login to app/.env.development.local (loaded by `next dev`, NEVER by the `next start` the e2e server runs), leaving root .env AUTH-free so the fixtures survive naturally in both local and CI; deleted the globalSetup hack → guard: the 28-test e2e suite passes locally and in CI with no per-run file juggling. Lesson within the lesson: Playwright's webServer can start before globalSetup — never use globalSetup to prepare state the webServer reads at boot.

2026-07-10 · A REVOKE UPDATE/DELETE on signal_log did not make it insert-only — an UPDATE still succeeded → the app connects as the table OWNER on Supabase, and a table owner bypasses GRANT/REVOKE entirely → enforced insert-only with a BEFORE UPDATE OR DELETE trigger that RAISES an exception, which applies to the owner too (verified UPDATE and DELETE both rejected) → guard: the guardrail is a trigger, not a grant; a future test should assert both are blocked. Also: I verified this against the real Supabase DB and it left an undeletable junk row I had to TRUNCATE away — app-side DB checks should use a throwaway/test database, not production (the plan already calls for a seeded test DB and dockerised Postgres for pipeline tests).

2026-07-11 · Tried to spin up a local dockerized Postgres for publish.py tests; `docker --version` works but `docker ps` fails "Cannot connect to the Docker daemon" → the Docker CLI is installed but Docker Desktop (the daemon) is not running, and I can't start a GUI app autonomously → publish integration tests target TEST_DATABASE_URL and SKIP when it is unset; a Postgres service container in ci.yml runs them on every push, which is the plan's harness anyway (§6.1). To run them locally, start Docker Desktop first → guard: the tests never silently pass — they skip loudly (pytest skip) when there is no test DB, and CI always has one.

## The UI redesign (R0–R6, 2026-07-12)

**A guard that cannot fail is worse than no guard.** The visual-regression suite's "fonts
blocked" shot — the one meant to lock the pre-swap fallback layout — was passing while testing
absolutely nothing. Signing in first had loaded the fonts into the browser's memory cache, so the
later navigation made no network request, the route interception never fired, and the
"fonts-blocked" screenshot was quietly an ordinary one. What gave it away was that the file was
byte-identical to its neighbour. It now asserts that it actually intercepted a font request.
Whenever a test is supposed to capture an UNUSUAL state — offline, fonts blocked, an empty table,
an error boundary — assert that the unusual state actually happened.

**Measure before you take the fallback ladder.** Lighthouse came back at performance 86, and the
plan had a ready-made remedy: strip the card translucency, then remove the orbs. It would have
been easy, plausible, and completely wrong. Total Blocking Time was 20ms — the glass, the wash
and the orbs cost nothing measurable — while the page was pulling ten font files totalling 429KB
against a budget that claimed 273KB. The weight was in bytes, not pixels. Cutting two font
weights took performance from 86 to 93 and left the design intact. The plan's fallback ladder was
a reasonable guess written before anyone had measured; the measurement is what actually knows.

**A budget that measures the wrong thing is worse than no budget, because it reassures you.** The
font-budget script counted basic-latin faces and reported 273KB, and it passed. The browser
fetched 429KB, because Google splits every family into latin AND latin-ext and a real browser
takes both. The check was not wrong — it was NARROW — and it had been quietly flattering us by
about sixty percent for two phases.

**Screenshots catch what tests cannot.** Three real bugs in the redesign were invisible to a
green test suite and obvious in a picture: the Desk rendering module 02 above module 01 (the grid's
dense backfill pulling the Brief into an empty cell); the header reading "Jul 10" while the module
beneath it read "Jul 11" from the same value (a bare calendar date formatted in Eastern time,
which shifts it back a day — a trap that lib/time.ts already documented, and that I walked into
anyway); and every quantile dot in the Range Ladder squashed into an overlapping ellipse by
`preserveAspectRatio="none"`. A dot you cannot count is not a dotplot. Look at the thing.

**Removing an accident can expose a bug that was hiding behind it.** Taking the `cookies()` call
out of the Desk layout (so the theme could live on `<html>`) made the route statically
prerenderable — which is what the plan always wanted, and which the cookie read had been silently
preventing. It also meant the layout's watchlist query started running at BUILD time, on a machine
with no DATABASE_URL, and the build died. The query had never been wrapped in a try/catch, unlike
every other read in the app, and it had got away with that for six phases purely because the route
happened to be dynamic. The bug was always there. The fix just stopped hiding it.

**The plan's own rules will collide, and the collision is information.** The anti-drift grep bans
the word `transition` anywhere inside a P2 file. The motion spec explicitly permits a chevron to
rotate on a `<details>` summary. SetupCards is a P2 file with a chevron. Rather than carve an
exception into the grep — where the next reader would find it and not know why — the chevron moved
into its own component file. A coarse rule that is easy to verify beats a subtle one that is easy
to get wrong.

## The app-feel build (F0–F7, 2026-07-12)

2026-07-12 · My new navigation-timing test reported Scans as the FASTEST room in the app (14ms) when it is in fact the slowest (1342ms) → Playwright matches an accessible name by SUBSTRING, not exactly, so `getByRole("heading", { name: "Scans" })` matched the Desk's own module masthead "07 — Sectors & scans" — which is already on screen before the tab is tapped. The timer started and stopped on the page it was supposed to be navigating AWAY from → the spec now asserts the destination heading is ABSENT before every timed tap, so a locator that starts matching the origin page fails the test instead of flattering it → guard added: the absence check runs on all 8 samples of all 4 destinations, not once. The general rule, which is the one worth keeping: when a test measures the ARRIVAL of something, it must first prove that thing is not already there. `exact: true` would have fixed this one locator and left the trap armed for the next.

2026-07-12 · The new TTFB budget passed /scans/unusual-volume at 46ms — a route that does not exist yet and was answering 404 → I built the probe list from the plan's future route map (correctly — a family left out of the probe set is a gap that becomes permanent), but the check only compared latency against the budget and never looked at the status code. An empty 404 is the fastest response a server can give, so the budget was rewarding the absence of the page it was meant to be measuring → the gate now treats any non-200 as a miss, and the evidence table prints the status beside every number → guard: both the pass path and the fail path were exercised deliberately before the instrument was trusted. This is the same species as the fonts-blocked VRT shot that tested nothing (R-phase): a check that cannot fail is worse than no check, because it also produces a reassuring green tick.

2026-07-12 · There is no way to run Postgres locally on this machine → brew ships no postgresql bottle for macOS 26.3 and its source build dies in configure; the prebuilt embedded binaries link against a libz that exists only inside the macOS dyld shared cache, so there is no file to point them at; Docker Desktop's daemon is not running and cannot be started headlessly (LESSONS 2026-07-11, same wall) → the seed is verified WITHOUT a database instead: 12 unit tests assert its invariants directly (frozen mover ranks, awkward rows below the movers' cut, fills that agree with lib/paper.ts), and CI's Postgres service container runs the real `migrate deploy && db:seed` on every tag → guard: the fixture tests are negative-controlled — nudging SMCI's seeded return by 0.006 turns them red. Do not spend another session trying to install Postgres here; the database oracle is CI, and that was already the project's settled answer.

2026-07-12 · A stale `.next/types` broke typecheck with errors about routes I had just deleted → the same lesson as 2026-07-10, met from the other side: I created two temporary probe routes for the prefetch experiment, deleted them, and `tsc` kept type-checking generated validators that still described them → `rm -rf .next` before the next typecheck → guard: none needed beyond the existing lesson, but note the trigger is ADDING-then-removing a route, not only removing one. Any temporary route must be followed by a clean build.

2026-07-12 · A visual-regression baseline came back as a picture of Next.js's 404 page, and the bug behind it would have deleted an entire room in production → `revalidatePath(path, "layout")` DROPS THE KNOWN-PARAMS SET of any route that declares `dynamicParams = false`. Every URL in that family then 404s — permanently, until the next deploy. `/scans/[preset]` declares exactly that (a closed set of five presets; the flag is what makes /scans/garbage a real 404 rather than a 200 carrying a not-found page). The theme action had called it since P6 and the watchlist action since F1 — so the reader would have found this by changing their theme ONCE, and every scan table in the app would have been gone → the app now has NO layout-scoped revalidations anywhere: the theme applies client-side and needs none, and the watchlist names the five rooms it changes → guard added: drift rule 17 greps for `revalidatePath(..., "layout")` and fails the build. Three lessons inside the one bug: (a) a framework flag with a whole-route-tree blast radius will eventually be aimed at something you did not think about; (b) the failure was invisible in isolation and only appeared in the FULL suite, because it needed another spec to fire the action first — a bug that only exists in combination is exactly what an end-to-end suite is for; (c) it was reproduced locally in two commands (request the page → 200; call a layout revalidation; request it again → 404), which took ten minutes and ended an hour of theorising. Reproduce, then fix.

2026-07-12 · The VRT diff images have never once been uploaded → the ci.yml artifact step pointed at `app/playwright-report/`, which CI never writes: the run uses the `github` reporter, not `html`. Every visual-regression failure for six phases uploaded an empty artifact, so the one thing a pixel failure owes you — the picture — was the one thing you could not get → the step now uploads `app/test-results/`, where Playwright actually writes the expected/actual/diff triptych → guard: the very next failure produced a readable 404 screenshot, which is what identified the bug above in one look. A diagnostic that has never been exercised is not a diagnostic.

2026-07-12 · A `notFound()` inside an ISR-cached dynamic route returns HTTP 200 with the 404 page in the body → Next caches the not-found render as a static entry and serves it with an OK status. `/ticker/GARBAGE` does this today (it became ISR at F1; before that it was force-dynamic and 404'd properly). `/scans/[preset]` avoids it only because `dynamicParams = false` produces a genuine 404 → the scans route keeps the flag (and drift rule 17 protects it); the ticker's wrong status is logged for the user as [FYI] — the reader still SEES "this page could not be found", so it is a correctness wart rather than a lie on screen → guard: e2e asserts the 404 STATUS on /scans/garbage, not merely the words on the page.

2026-07-12 · The e2e that proves the PM journal saves an entry has never once proved it → it waited for a `journal-saved` marker, and that marker renders whenever `state.ok` is true — which is the INITIAL value of `useActionState`. "Saved." was on the page from the moment it loaded, before anything had been written, so the assertion passed whether or not the write happened. It only started failing when the write became genuinely slow, which is the one circumstance in which a guard that cannot fail finally does → the test asserts the TEXTAREA CLEARED instead: the form resets itself on a successful write, so an empty field cannot be true until the server action has come back ok → guard: this is the third guard-that-could-not-fail found in this build (the fonts-blocked VRT shot, the 404-scores-as-fast TTFB probe, and now this). The pattern is always the same — the assertion is true in the state the page STARTS in, so it never observed the transition it was written to observe. When writing a test, ask what the page looks like BEFORE the action: if the assertion already holds there, it is not a test.

2026-07-12 · A server action that saves one journal row took over five seconds in CI, and looked like a hang → a server action's response carries a full re-render of the page it was invoked from. On the Desk that is the entire morning — ten-odd database reads — before the client sees its result. It is a slow round trip by design, not a stuck promise, and on a contended CI Postgres it comfortably outlives Playwright's 5s default → the test's timeout now says so explicitly, with the reason → worth knowing when the Desk grows: every write from a Desk form pays for a full Desk render before the button stops saying "Saving…".

2026-07-12 · Every write on the Desk left its button stuck on "Saving…" forever — while the row landed in the database perfectly — and it had been doing so since the Desk became a cached route on 2026-07-11 → calling `revalidatePath("/")` INSIDE a server action that was invoked FROM "/" deadlocks the action's own reply: the action invalidates the very page whose fresh render it is trying to stream back as its response. The write succeeds, the browser never hears about it, and the reader concludes their journal entry was lost and writes it again → both Desk writes (the journal, and the setup cards' weakener checkboxes) now revalidate inside `after()`, which is the framework's own primitive for work that must happen once the response is finished. The reader gets their reply immediately; the cache entry is invalidated a moment later, for whoever comes next. Nothing about the freshness contract changes — only the ordering → guard: the journal e2e now asserts the FORM CLEARED (which only happens on a successful round trip) instead of a marker that was visible from page load. Isolated in one experiment: delete the revalidate line, and the same action returns in 14 seconds of test wall-clock; put it back, and twenty seconds is not enough. The bug survived six phases because the only test that could have caught it was asserting a state the page starts in.

2026-07-12 · The `feel-final` tag's CI failed on an accessibility rule that printed "nested-interactive (1 nodes)" and nothing else, on a page whose own markup contains no buttons at all → the offending element was injected by a LIBRARY. Lightweight Charts' `attributionLogo` option appends a TradingView link inside the chart container, and that container is `role="img"` — an element whose children are, by that role's own definition, part of one picture. So the licence's link was on the page, present in the DOM, painted on screen, and reachable by nobody with a keyboard. Two lessons stacked: a third-party widget can violate your accessibility rules with markup you never wrote, and `role="img"` on a container is a promise that NOTHING inside it is interactive → the logo option is off and the attribution is a real link in a caption below the chart, which is what the licence asked for in the first place ("a link on the page of your website that is available to your users") → and the gate now prints the failing node's selector and HTML. A guard that names the rule but not the node tells you that something, somewhere on a page of a hundred elements, is wrong, and leaves you to find it. That is half a guard, and I paid the other half in search time.

2026-07-12 · The service-worker test began timing out at F7 — waiting 10 seconds for the worker to take control of the page, on code that had not touched the service worker in eight phases → nothing about the worker broke. Installing a service worker means downloading its entire precache, and this release quietly made that bigger (every room became a cached, prerendered route) while the Desk now prefetches five more of them on idle. On a cold CI runner the install was still in flight when the test reloaded — and a worker that has not finished installing cannot control anything. The test was not waiting too little; it was waiting for the wrong thing, at the wrong time → it waits for `serviceWorker.ready` (an ACTIVE worker — a real state the app reaches) and THEN reloads. The general shape: when a timing test starts failing after an unrelated change, ask what the change made SLOWER before you ask what it broke — and fix it by waiting for a state, never by waiting for longer. A longer timeout is a race you have agreed to lose more slowly.

2026-07-12 · Re-enabling axe's colour-contrast rule reported 58 failures across every colour the app owns — muted, ink, the accent, the up and down chips — which reads as "the palette is broken" and is one of the most alarming things a gate can say → the palette was fine. Every page in this app fades in (`.route-fade`, opacity 0 → 1) and axe composites a foreground colour through any ancestor's opacity, so it was measuring the page WHILE IT WAS STILL ARRIVING and faithfully reporting the colours of a half-transparent screen. `text-muted` at 55% opacity really is 2.23:1; it is also not a colour any human has ever read. Wait for the fade to land and 58 failures collapse to ONE → the gate has a `settle()` helper that waits for every `.route-fade`/`.content-fade` to reach opacity 1 before axe runs → the general rule, which is not about colour: NEVER MEASURE A COLOUR, A SIZE OR A POSITION WHILE THE PAGE IS STILL ANIMATING — you will measure the animation. And note what the false alarm nearly cost: I had already written the 58 into an evidence file and asked the user to approve a palette change on the strength of it.

2026-07-12 · I surveyed contrast across both themes, found dark clean, wrote "a colour that passes is not a colour to change" into the token sheet — and CI failed on the phone Desk in dark → the survey had been run with `--project=desktop`. Midnight's grey clears 5.00:1 on a STANDARD card and 4.44:1 on a RAISED one, and the phone Desk's shelf cards are raised — so the one surface that failed was the one surface my survey could not see. I measured half the app and reported on all of it → both muted values now clear the floor against the LIGHTEST card each theme ever puts them on, and the a11y gate runs both themes × both projects (42 checks) → the lesson is not "run both projects", it is that a clean result from an instrument is only as wide as the instrument's coverage, and the coverage is the first thing to state when reporting a null finding. "No failures found" and "no failures exist" are different sentences.

2026-07-12 · A watchlist row that a test had just added VANISHED when the next button in the same row was clicked — intermittently, on a page that had passed this test for weeks → /settings was an ISR-cached route whose own server actions revalidated "/settings". Every click there runs an action that re-renders that page as its reply, and when the reply can be served from a cache entry written before the click, the reader gets back the page as it was BEFORE they touched it. The extra page-loads from a new test suite warmed the cache more aggressively and turned a rare race into a reproducible one → /settings renders on request; its actions no longer revalidate their own path; the exception is declared in three places that must agree (the page, check-routes' ALLOWLIST, drift rule 13) → the rule, which is the second time this exact shape has cost us (the first was the Desk's hanging writes): A PAGE MAY BE CACHED, OR IT MAY BE WRITTEN TO AND READ BACK IN THE SAME CLICK — NOT BOTH. NEVER REVALIDATE THE PATH YOU WERE INVOKED FROM. Every other room in this app is a reader; the one room that is a writer is the one room that cannot be cached.

2026-07-12 · A tag CI that had never run caught a real bug on its first firing → `nc-0` went red at the "Migrate and seed" step: a fixture helper `t("22")` produced the string `"2026-07-09T22:00.000Z"` — no seconds field — and `new Date()` returned an Invalid Date rather than throwing. Everything local was green: 395 unit tests, typecheck, lint, drift, build, budgets. The fixture tests passed because they asserted the INTERESTING fields (the significance scores, the ordering, the blur placeholders) and never looked at a timestamp → two lessons, and the second is the bigger one. (1) `new Date("nonsense")` does not throw; it returns an object that looks like a Date and fails only when something finally tries to use it — so a bad date travels a long way from its cause. (2) **A fixture's boring fields are still fields.** The test suite was thorough exactly where I had been thinking hard and blank exactly where I had not, which is the normal shape of self-written tests. Guard added: a test that walks every Date in every fixture and asserts it is real — verified by re-introducing the bug and watching it go red.

2026-07-12 · A source-status key that cannot fail is not a status → production recorded `fred: ok` on a night when all three index levels were NULL, because the check was `any(value is not None for value in macro.as_columns().values())` and VIX alone satisfied it. The Desk therefore showed four ETF prices under a footer claiming FRED index levels, and nothing anywhere said the levels were missing → the honest diagnosis was NOT that `fred: ok` was wrong. FRED *was* reachable; it answered for two of its four cells. **One key cannot describe two different failures.** The repair is a second key (`fred-indexes`), not making the first one lie in the other direction. Generalisation: when a status is computed with `any()` over a heterogeneous set, ask what it is really claiming — an `any()` over things that fail independently is a status that reports the healthiest member and hides the rest.

2026-07-12 · An upsert that writes what it fetched will eventually destroy what it had → `sp500 = EXCLUDED.sp500` in the nightly's ON CONFLICT clause meant a flaky FRED night overwrote a perfectly good stored index level with NULL. The run succeeded. The data was gone. The Desk silently fell back to an ETF, and every gate stayed green → **a successful run destroying data is the worst shape a bug can have, because everything looks like it worked.** The fix is not COALESCE(EXCLUDED.x, table.x): the table is keyed by run_date, so each night is a NEW row with nothing to coalesce with — the repair has to reach back to a previous row, which is why it is a function (macro_levels.resolve_index_levels) and not a clause. Rule of thumb: before writing a nullable column from a fetch that can fail, ask what the null MEANS. "The source said nothing" and "the value is nothing" are different facts, and only one of them should ever be persisted.

2026-07-12 · A static provenance line is a lie with a delay fuse → the Macro Pulse's footer was the fixed string "Index levels · FRED · prior close", written for the happy path and rendered regardless of what the rows above it showed. On the one night it mattered it sat beneath four ETF prices and declared them FRED index levels → **a sentence describing data it cannot see will eventually describe data that is not there.** Provenance must be COMPOSED from what actually rendered (ruling C6), and the test that proves it is a property test, not an example: build two different states and assert the line differs. An example test would have passed happily against a hard-coded string.

2026-07-12 · A visual-regression gate that scales its tolerance with page size gets weaker exactly where it needs to be stronger → `maxDiffPixelRatio: 0.01` on the desktop Desk's full-page shot (1366×2763 = 3.77M pixels) is a budget of **37,742 differing pixels**. N1 rewrote the whole macro row — reordered it, relabelled "Russell 2000 · IWM (ETF proxy)" to "Small caps", deleted a chip, replaced the provenance sentence — and the desktop baseline passed unchanged. The phone shot, on a page with half the pixels and therefore half the budget, caught it. Same DOM, same build, same commit: one viewport told the truth and the other did not → **a ratio-based pixel tolerance is a guard whose strength depends on how long the page happens to be, which is the wrong variable entirely.** Replaced with an absolute `maxDiffPixels: 600`. Second, compounding trap: `--update-snapshots` defaults to `changed`, which only rewrites baselines whose comparison FAILED — so a real change that fits inside the tolerance is neither reported nor re-baselined, and the stale image survives as the new "truth". Use `--update-snapshots=all`. How it was caught: not by a test — by opening the regenerated PNG and looking at it, after noticing that only 6 of 46 baselines had changed when the change was to a shared component. **When a re-baseline surprises you by changing less than you expected, that is a finding, not a relief.**

2026-07-12 · On a cached route, "reset the data" is TWO steps, and the second is the one you forget → the Academy VRT shot was photographing a checkmark that `academy.spec.ts` had left behind (opening a lesson marks it read). Deleting the `lesson_progress` rows before the shot did NOT fix it: `/academy` is an ISR route, and the action that marked the lesson read had already revalidated it, so the CACHE held a rendering with the checkmark. Emptying the table changes what the next render would produce; it does nothing to a render that is already cached and being served → clear the state, THEN bust the cache. This generalises to every cached room in this app, and to any test or script that "resets" state behind an ISR page. Related, and the deeper structural point: **the VRT baselines were being generated by a job that runs only `vrt.spec.ts`, but verified in a job that runs the whole suite** — so they were born in a world where no other spec had mutated the database, and checked in a world where several had. A baseline must photograph a state the suite establishes on purpose, not residue.

2026-07-13 · CI can be green for months while production runs a schema the code does not match → N0 authored a migration (five tables, one column), committed it, and every CI run since went green. Production never got it. The reason is structural and worth internalising: **CI migrates a FRESH POSTGRES CONTAINER on every run, so a green pipeline proves only that a migration APPLIES — never that it was applied to the database the deployed app actually reads.** The Vercel build ran `next build` and no migration step, so nothing ever ran it there. And nothing looked broken, because the app's loaders all catch a database error and degrade to their honest empty state (which is correct, and which the DB-free CI build depends on) — so the missing column did not crash anything. It just made the Macro Pulse fall back, silently, every night, which is the exact disease the current plan was commissioned to end. **The honest-degrade path is a good thing that made a real defect invisible; that combination deserves a name.** Fixes: the deploy now runs `prisma migrate deploy` (fails closed — a migration that will not apply is a deploy that does not happen), and `npm run check:migrations` compares disk against the live database before a push. Generalisation: any gate that runs against a DISPOSABLE copy of a system cannot make claims about the PERSISTENT one, and the gap between them is invisible precisely because everything in it is green.

2026-07-13 · Thirty-four passing tests, and the page could not render at all → `pipeline_run.runDate` is a Prisma `@db.Date` — a trading DAY — and the loader serialised it with `.toISOString()`, which is right for an instant and wrong for a date: it produced `"2026-07-10T00:00:00.000Z"` where the freshness machine wanted `"2026-07-10"`. Every date it built from that was an Invalid Date, and the first formatter to touch one threw a RangeError. Nineteen tests on the machine, fifteen on the component, all green — because **every one of them constructed its own fixture, and every fixture was correct.** The defect was in neither unit. It was in the JOINT, and both sides of a joint can be individually perfect while the joint is broken. A test that builds its own inputs is structurally incapable of seeing the seam where the real inputs come from. Fix: a test on the SEAM (the loader's real output, fed to the real machine, with no hand-tidying in between), and the machine now refuses a timestamp at its own front door with a message naming what it wanted — so the next regression fails where the mistake IS, not three components downstream in a formatter that did nothing wrong.

2026-07-13 · Two more guards that could not fail, and they had the same shape as every previous one → (1) the iOS 16px sweep had been PASSING ON /paper WITHOUT LOOKING AT THE FORM: the ticket lives behind a Suspense boundary, `page.goto()` resolves before it hydrates, so the sweep queried the DOM, found zero fields, iterated over nothing, and reported success — on the one page whose form is the entire reason the rule exists. Any unrelated change that shifts the timing flips it, which is why it looked like a flake. It is not a flake; it is a guard that never ran. (2) The touch sweep's running-text list omitted `figcaption`, so the chart's "Chart by TradingView" attribution — a link inside a sentence, exactly what WCAG 2.5.8's inline exception covers — was measured as a bare 82×14 control; it had been passing only because the chart is code-split and the caption often did not exist yet when the sweep ran. **The pattern, now seen six times: a guard passes because the thing it measures is ABSENT, not because it is correct. Every sweep must assert that it swept something.** Both now count what they examined and fail on zero.

2026-07-13 · The iOS zoom rule was ALSO crying wolf, and over-triggering is its own kind of broken → the same sweep measured every `input`, including the segmented control's radios (13.5px, the browser default, unchanged since F2). But iOS zooms on focusing a field that summons a KEYBOARD, and a radio summons none. Failing the build for it would have driven someone to "fix" a control that was never broken, in a way that made the design worse. **A guard that rejects correct code teaches people to work around the guard** — which is the only outcome worse than not having it. Same lesson landed twice this phase: drift rule 18 fired on `has-[:disabled]:text-faint`, which is not merely allowed but exactly what the token exists for. When a rule fires on code you believe is right, the first question is whether the RULE is right.

2026-07-13 · A baseline that depends on the wall clock rots on its own → the pipeline strip grades freshness in the browser (deliberately: a cached render must never photograph a dead pipeline as a healthy one), so the Desk now renders something that depends on WHEN IT IS. The seed publishes one run for a fixed trading day; real time keeps moving. The first baseline run came back with the strip in its AGING state — correct, and unrepeatable: a day later it would have been the DEAD banner, and the baseline would have failed with nobody having touched a line of code. Then someone re-baselines it to go green, and a week later it fails again. **A picture that rots with the calendar is not an oracle; it is a chore with a countdown on it.** Fix: pin the browser's clock (Playwright's `page.clock`) to the seeded session's evening, and ASSERT the expected state before shooting — so if the seed's date moves and the pin does not, it fails loudly instead of quietly photographing the wrong rung of the escalation ladder. Found the same way the last two VRT bugs were found: by opening the PNG and looking at it.

2026-07-13 · Re-baselining is how a budget dies, and every individual step is defensible → the bundle guard fired (/paper +10.5KB over its baseline). The growth was real, understood and shared (a client component imported by two entry points gets hoisted into a chunk every route carries). The tempting move — bump the baseline, note the reason, move on — is correct ONCE and fatal as a habit: six phases of "it only grew a little, and here is why" puts the app at 260KB with a green gate the whole way, because **drift is measured against the last thing that was accepted.** So the rebaseline landed together with the HARD CEILING the plan always specified (200KB) and nothing had ever enforced. Drift catches the unexplained; a ceiling catches the accumulation of the explained. Any budget expressed only as "no worse than last time" has this hole in it.

2026-07-13 · When a gate goes red, the first question is not "what broke" — it is "is the gate asserting the thing it means to assert" → the `nc-2` tag failed with 11 failures across 6 tests. Not one was the product, and not one was a pixel (all 55 VRT baselines passed). Three causes, three ways a test can be confidently wrong: (1) **`getByRole("alert")` was measuring the FRAMEWORK.** Next.js renders `<div role="alert" id="__next-route-announcer__">` on every page — an empty live region for announcing client-side navigations — so a bare `getByRole("alert")` matches everywhere, always, and `toHaveCount(0)` can never pass. Four tests were asserting "no alarm on a healthy night" while the healthy night rendered no alarm at all. (2) **A test that reads time-dependent output did not say WHEN it was reading.** The strip grades against the browser's clock; the seed is a fixed trading day; real time moves. The test asserted the fresh line while CI, days later, correctly showed the aging one. The product was right. (3) **"Zero network requests" was the wrong claim, and enforcing it would have cost real performance** — the router prefetches the tab-bar rooms on idle, which is the mechanism that took a tab tap from 1342ms to 280ms. A test demanding literal silence would have driven someone to delete the app's navigation speed to satisfy it. The claim the control actually rests on is narrower: *no data fetch for this page*. **Generalisation: an assertion inherits the ambient behaviour of the platform it runs on. Before believing a red gate, check what else is on the page answering to the same name.**

2026-07-13 · Three of this repo's FRED fixtures were never recorded — they were invented, and they had been "passing" for three phases → the `new-provider-adapter` skill's first rule is "Record REAL fixtures first", and R0 (commit `4a1c736`) did not: it hand-wrote `sp500.json`, `nasdaqcom.json` and `djia.json` in the shape of a real FRED response and filled in plausible numbers. The tell was `"count": 8000` against the real series' 2610, and a value of 6812.34 where FRED actually said 7575.39. Nothing broke — the parser works on the real data too — so **for three phases every index-level test proved that the parser agreed with my imagination.** What the fiction had already destroyed was a real property of the data: FRED's index series post a day AHEAD of the VIX (the S&P's newest observation is the 10th, the VIX's is the 9th), and the invented fixtures gave them the same date. That is a fact about the world that no test could have learned from them, and one the Mood gauge's as-of reasoning depends on. → The generalisation is the sharpest version yet of this build's recurring disease: **a fabricated fixture is not a weak test, it is an INVERTED one** — it does not fail to check the provider, it actively certifies that the code matches a fiction, and it hands you a green tick for doing so. A guard that measures nothing is merely useless; a guard that measures the wrong thing is worse than none. Rule adopted: **a fixture that was not recorded must say so in its own FILENAME.** Gold's key does not exist, so its success fixture is `xau_usd_UNVERIFIED.json` and the only real gold recording in the repo is `error_403.json` (the genuine unkeyed rejection — which does pin the route and the auth header, and is worth having). Nobody can read that filename and mistake it for evidence.

2026-07-13 · The unit a freshness rule counts in is a product decision, and getting it wrong makes the lamp useless in the direction nobody notices → the macro board's stale-cell rule is "older than three cadences". Implemented in calendar days, gold's cell would go amber **every Monday**: Friday's price is three days old and ZERO SESSIONS old, because the gold market was shut and there is no newer price to have. A lamp that cries wolf every Monday is a lamp nobody looks at by Tuesday, and then it is not there on the morning it is finally telling the truth. But the OPPOSITE unit is equally wrong one cell to the right: Nepal Rastra Bank publishes every calendar day, weekends included, so for the rupee a weekend genuinely IS three missed publications and counting sessions would hide a real outage. **Same ladder, different clocks, because the sources keep different clocks.** This is N2's freshness lesson ("count in sessions, never days") discovered to be half of a bigger rule: the honest unit is not "sessions" and not "days" — it is *whatever unit the source actually publishes in*, and a staleness rule that does not ask that question is guessing. Both consumers now share one session counter (`market-hours.sessionsBetween`); two copies of "what counts as old" is one copy too many.

2026-07-13 · The seed already contained the exact contradiction the ruling was written to prevent, and nothing was checking → the Mood gauge's C8 contract says each component must show which way it pulls the score. N0's seed stored that arrow as a hand-written field beside each percentile — and one of them had already drifted: momentum sat at the **48th percentile**, below its own median, carrying the arrow **"greedy"**. It looked plausible (the index really was above its 125-day mean), it broke nothing, and it would have shipped to screen. → **A derived value that is stored as a field is a value that is free to disagree with what it was derived from, and eventually will.** This is ruling C6 (provenance must be composed from what actually rendered) applied one level down, inside a data structure, and it has now cost this build twice: once as a static provenance sentence describing rows it could not see, once as an arrow describing a percentile it did not read. The arrow is now DERIVED at both ends — the pipeline computes it from the percentile, the app recomputes it from the percentile — so no stored value, however it got into the database, can put a label on screen that contradicts the number printed beside it. When you catch yourself storing a field whose value is a pure function of a field next to it, you are storing a future contradiction.

2026-07-13 · The nc-3 tag went red on three tests, and jsdom was structurally incapable of catching any of them → the macro board renders TWICE — once in the phone shelf (`md:hidden`), once in the ≥md row — and exactly one of the two is `display:none` at any given width. Every component test passed, correctly, using `getAllByText`; **jsdom applies no CSS, so "hidden" is not a state it can have an opinion about.** In a real browser at desktop width, `page.getByText(...).first()` resolves to the shelf's copy: present in the DOM, correct in every particular, and invisible. Three tests failed with "13 × locator resolved to `<span>wk of Jul 9</span>` — unexpected value: hidden". → Two things worth keeping. (1) **In a real browser the honest question is never "is this in the DOM", it is "can the reader see it"** — `.filter({ visible: true })`, which is exactly what every other macro assertion in that same file was already doing. I wrote new tests into a file whose established pattern I had not read, which is a cheaper mistake to make than to fix. (2) The unit-test layer and the browser layer fail at *different* things, and a responsive layout that renders the same content twice is precisely the seam between them: jsdom sees both copies and cannot tell them apart; the browser sees both and hides one. Neither layer alone can check that a duplicated element is visible in the width it was written for. That is not a gap to be closed — it is a division of labour to be remembered when writing the e2e.

2026-07-13 · A secret in GitHub is not a secret in the job, and this build has now shipped that bug TWICE → GOLDAPI_KEY was provisioned and the gold cell would still have printed "not yet reported" every night, because nightly-a's env block never passed the key to the job. That is exactly the ANTHROPIC_API_KEY bug from N0, which silently skipped every LLM stage in production for four phases. Both were found by hand. → The pattern that makes this class so dangerous: **the pipeline is written to degrade quietly when a key is missing — correctly, because one dead provider must not take the night down — so the job prints a calm line about skipping, carries on, and every gate stays green.** The failure is invisible precisely BECAUSE the error handling is good. A thing found by hand twice will be missed the third time, so the check is now derived from the code rather than from anyone's memory: tests/test_workflow_env.py walks the import graph from each job module, collects what it actually reads off the settings object, and fails if the workflow that runs it does not pass a required secret. Negative-controlled: deleting the GOLDAPI_KEY line turns it red.

2026-07-13 · A fabricated fixture does not merely fail to test the provider — it destroys a real property of the data, and you cannot know which one → the gold key finally landed, the recorder ran, and the invented `xau_usd_UNVERIFIED.json` could be compared against the truth at last. The field shapes matched (it was written from the documentation, so of course they did). What it had got WRONG was subtler and more useful: it stamped the quote at exactly midnight UTC — a clean session date, which is what every other cell on the macro board carries. GoldAPI does not work that way. It stamps the LIVE QUOTE INSTANT (13:21:02 UTC, the second the recorder called it). So gold's "as of" date is the day we ASKED, not the day a session closed. → The generalisation, which is sharper than N3's: an invented fixture is not just an inverted test, it is a **smoothed** one. A human writing a fixture writes what is tidy — midnight boundaries, round numbers, consistent dates. Real data is not tidy, and the untidiness IS the information. N3 found the same thing from the other side (the invented FRED fixtures gave the index series and the VIX the same date, destroying the fact that FRED posts index levels a day AHEAD of the VIX). Both times, the fiction's tidiness deleted the exact property the code needed to know about.

2026-07-13 · A threshold copied from a plan is a guess; the sixth real bug of this build was found by printing the output and reading it → The plan set the clustering bar at Jaccard 0.55. Run against 134 real articles it merged **nothing** — zero clusters out of 134 — because "VitalHub acquires Buddy Healthcare" and "VitalHub Announces Acquisition of Buddy Healthcare" score 0.50 ("acquires" and "acquisition" are different tokens). A bar that high is not strict, it is **inert**, and the Front Page would have printed every story once per outlet while its "3 sources" line never once appeared, with every test green. Measured instead: 0.45 finds every true merge and no false one; 0.40 collapses Qiagen, Conmed and Tiny Ltd into ONE card because all three headlines say "jumps after report of takeover interest". The right answer sits 0.05 from fabrication. → And the classifier, on its first draft, buried "Iran's IRGC navy says Strait of Hormuz closed until further notice" at 0.165, dead last — while "An emboldened India holds out for better terms in US trade talks" LED the page, on the strength of the words "trade talks". No test caught it. Printing the front page and reading it caught it in one look. **That is now six real bugs found by looking at the output rather than at the tests, and by no other means.**

2026-07-13 · The providers do not do the work you assumed they did — read the recording before you design around it → Finnhub's market news carries NO tickers. Not "sometimes", not "for small caps": zero of 160 items, while its company news carries them on 20 of 20. Same provider, same adapter, a different endpoint. The plan's clustering rule required overlapping ticker sets and its significance formula measured magnitude over the linked tickers; both were built on a field that is always empty on the feed that supplies most of the page. → The reason it was missed is worth naming: the repo already HAD a Finnhub fixture, so the endpoint felt known. It was a different endpoint. **A fixture for a neighbouring endpoint is not evidence about this one**, and the family resemblance is exactly what makes the assumption feel safe enough to skip checking.

2026-07-13 · A mode declared without a handler is not a broken button — it is a button that runs the whole nightly → job_a's main() dispatched `macro` explicitly and let everything else fall through to the full run. So adding `compute` to MODE_STAGES without a handler would have produced a control-room button that, pressed at noon, re-ingested the entire market mid-session and wrote half a day of unformed bars over the last good close. The fall-through was not a bug anyone had written; it was the SHAPE of the dispatch. → main() now refuses any mode it has no handler for, and `compute` is left undeclared until N6 builds it alongside its button, so the promise and the code land in the same commit.

2026-07-13 · The test suite never runs the module as a script, so it cannot see the one thing that breaks it → `run_news_mode` was appended to job_a.py after the `if __name__ == "__main__": main()` block. Python executes that block the moment it reaches it, so main() ran while the name was still undefined. **Every unit test passed** — a test IMPORTS the module, which defines every function and never executes the entrypoint. Production died with a NameError in eleven seconds. → Guarded structurally rather than by discipline: a test now reads job_a's own syntax tree and fails if anything is defined below the entrypoint. The lesson generalises past Python — **the suite exercises the module in a mode the user never uses, and the mode the user uses is the one that breaks.**

2026-07-13 · A test universe of 44 names cannot tell you anything about a universe of 12,933 → The ticker resolver passed every test — including deliberately adversarial ones loaded with trap names (Target, Gap, Visa, Key, Match, C3.ai) — and produced zero false positives across the whole recorded night. Then it ran against PRODUCTION'S instrument table, and immediately linked **UAE** (a country) to an iShares ETF on a story about export controls, **LNG** (a commodity) to Cheniere Energy on a Baker Hughes deal, **NDAQ** to five separate stories about the Nasdaq *index*, **MSTR** from the word "strategy", **PPLI** from "people", **BPOP** from "popular", **TISI** from "team", and **FOSL** from "fossil". → Two rules were wrong, and the shape of the error is the same in both. The bare-uppercase rule assumed a stoplist of acronyms could fence it; in a universe of thirteen thousand tickers **almost every three-to-five letter uppercase string is somebody's ticker**, and no list survives that. The single-word-name rule assumed a curated list of ~90 "names that are also words" could fence it; a great many of thirteen thousand companies are named after ordinary words **on purpose**. → The fix was to INVERT the test: stop listing the names that are words, and list the WORDS. A closed English word list refuses "strategy" and "people" and "fossil" while keeping "Nvidia" and "Pfizer" and "Conmed", and it does not need to know anything about the universe it is protecting. **When a stoplist has to grow with the data it filters, it is the wrong shape of guard.**

2026-07-13 · A provider's symbol is not your symbol, and the collision prints a true number under a false headline → Marketaux tagged "VitalHub Announces Acquisition of Buddy Healthcare" with **VHI** — correctly, for VitalHub on the Toronto exchange. In OUR instrument table VHI is **Valhi, Inc.**, a New York chemicals holding company with no connection to the story at all. Unchecked, the card would have printed Valhi's real one-day price move under a headline about a Canadian health-software acquisition. **Every number on it would have been true, and the card would have been a lie.** → A symbol identifies a listing on an exchange, not a company in the world, and providers tag against their own exchanges. So a provider tag is now checked rather than trusted: the symbol must exist in our universe AND the company the provider says it is must be the company we say it is. Where the provider does not say which company it means, the tag is refused — an unverifiable claim is not a weak claim, it is an unusable one.

2026-07-13 · The Desk told readers "markets open" out of a cache, and only the pixel oracle could see it → `/` is an ISR-cached route (budget B1), and the page computed `marketOpen={marketState(new Date()) === "open"}` in a SERVER component. A value derived from `new Date()` in a server component is evaluated when the page is GENERATED and then served to everyone until the cache turns over — so a Desk built at 3:55pm went on telling every reader "markets open" long past the close. → **This is F4's cooling-off bug, exactly:** a server-interpolated "now" records the moment the page was BUILT, not the moment the reader arrived, and on a cached page those are different moments. The rule is now twice-learned: **on a cached page, anything that depends on "now" must be computed in the browser.** The strip's freshness grading already was; the market state was not. → It was invisible to every test, because the string was well-formed and merely stale. It surfaced only because the VRT baselines had quietly become **photographs of whatever the market happened to be doing when CI last ran** — green at 3am, red at 10am. A baseline whose content depends on the clock is not an oracle, it is a coin flip that has not landed on tails yet.

2026-07-13 · The narrator was asked to write about eight stories the extractor had never read, and the failure was invisible → Stage B narrates the top 20 clusters by significance; the Stage-A extraction cap ranked the top 60 by "pre-LLM salience" (corroboration, then ticker-move magnitude). Those are different orderings. Measured on the recorded night: **8 of the 20 narrated clusters were outside Stage A's 60** — and they were the eight biggest stories of the night, the whole Gulf/Hormuz/oil cluster. Corroboration is 1 for 131 of 134 clusters and magnitude is 0 for the ~130 that name no company, so a **single-source macro story — which is precisely what a market-wide event IS — sorted to the bottom of the extraction queue while sitting at the top of the page.** → The salience rule was written to break a circle that does not exist in this tree: the plan assumed `event_type` came FROM Stage A, but N4 built a DETERMINISTIC classifier, so significance is fully known before any model runs. **A rule inherited from a plan can outlive the constraint it was written for, and it will not announce that it has.** Both caps rank by significance now, so the narrated set is a subset of the extracted set BY CONSTRUCTION rather than by luck. → The guard is a test that asserts the subset relation at any cap sizes. It matters because **an unread story produces an honest null, and a null prints nothing** — the room would have rendered perfectly, with its most important stories silently missing their context lines, and no screen and no test would have said a word.

2026-07-13 · The verification gate invented the number "2" out of "2.1x", flagged it, and would have silently deleted every honest sentence about volume → The scanner refuses a number glued to a letter, so "Q3" is not the number 3 — correct. But faced with a **decimal** glued to a letter it **backtracked to a shorter match** instead of refusing the token: `2.1x` matched as `2`. The gate then scored that phantom against its sources, found nothing, and flagged the sentence. → "2.1x its usual volume" is exactly how anyone writes relative volume, and 2.1 is a figure the pipeline computes and hands the narrator. So the gate was set to throw away the very sentences it was being fed the data to support. **A regex that backtracks does not merely fail to match — it can match something SHORTER and wrong, and a wrong match is not a near-miss, it is a fabricated entity presented as a finding.** → Fixed for both surfaces (the evening briefing's gate had it too): a multiplier is now a first-class number (so it is CHECKED, and a made-up 9.9x is still refused), and the quantifiers are possessive so a decimal welded to letters yields no entity at all. **The deeper lesson is about the failure mode:** a dropped note becomes null, and a null prints nothing by design (P9) — so a too-strict gate produces a page that looks EXACTLY like a page the narrator honestly had nothing to add to. Nothing on screen can tell those apart. That is why every outcome of the gate is now counted and the night prints the counts.

2026-07-13 · The corroboration count could not be opened, and it is the second-heaviest term in the ranking → `news_cluster.sources` is a bare `Int`. The card whispers "5 sources" and **nothing anywhere named them** — no outlet on the card, and the story page's source list (the FIRST line of its spec) had no data at all. So the number doing 25% of the work of ranking the front page was a number the reader simply had to believe. → **A count is a claim about things, and a claim about things you did not keep is a claim nobody can check.** The plan said the articles lived in `news_item`; they cannot — Job B reads every `news_item` row in its window and synchronously extracts every id that was not in the nightly batch, so putting the market feed there would fire ~200 extra model calls a night and feed the whole feed into the evening briefing. (Verified by reading the code, not assumed.) → New column `news_cluster.articles`, snapshotted at publish. And the seed now **refuses to load** when a cluster's `sources` disagrees with the number of articles behind it — without that check the card prints "5 sources" over a list of two and **every test still passes, because no test compared those two numbers.**

2026-07-13 · A `revalidate` that caches nothing, on a page that looks perfectly correct → `/news/[cluster]` carried `export const revalidate = 600` and re-rendered on **every single request**. A dynamic route needs `generateStaticParams` — even returning an empty array — or the framework silently renders it on demand and the `revalidate` line does nothing whatsoever. → **This repo already knew.** `/ticker/[symbol]` carries a comment explaining this exact trap, written when F1 hit it. The story page fell into it anyway, because the failing code LOOKS right: the `revalidate` export is sitting there, plainly, doing nothing. → Caught by the B1 routes budget — not by eye, not by any test, and not by the page misbehaving, because it did not misbehave. **A performance budget is the only instrument that can see a page which is correct and expensive.** Keep it in the standing gate.

2026-07-13 · The seeded news night modelled JSON shapes the pipeline has never emitted, and the tests agreed with it → The fixture was hand-authored in N4, before the narrator existed, and it GUESSED at two shapes: `verification: {status: "dropped", flags: [<string>]}` where `narrate.py` writes `{narrated, dropped, reason, checked, citations, flags: [{location, entity, kind, reason}]}`, and `key_numbers: [{id, label, value}]` where it writes `{value_str, what}`. The app reads the real ones, so it silently found nothing: the story page told the reader the narrator had simply had nothing to add **when the gate had actually DELETED a line**, and every key number in the seeded room was discarded on read — so the mono emphasis on a verified headline figure, the treatment whose entire meaning is "this was checked against its source", **had never once rendered.** → **And the tests agreed with the fixture.** `fixtures.test.ts` asserted `verification.status === "dropped"`, and its hand-written `Cluster` TYPE declared that shape — a type invented alongside the fixture it describes, so it certified the fiction instead of catching it. The fixture agreed with the test, the test agreed with the fixture, and neither agreed with the producer. **This is N3's fabricated-fixture lesson in different clothes: there the lie was in the VALUES, here it is in the SHAPE — and the app reads shapes.** → The guard is `lib/news-fixture.test.ts`: it runs the whole seed through the app's OWN boundary reader and **counts what goes in against what comes out**, because a silently-dropped key number is invisible to any test that merely asks "did some survive?". Both halves negative-controlled. Every shape is now copied from a real production row.

2026-07-13 · A bundle baseline that was never a measurement → B4 failed: `/news` at 195.1KB against a 184.2KB baseline, "grown 11KB". The changes since were 51 lines of comments and CSS classes, which cannot cost 11KB gzipped — so instead of re-baselining on a shrug, I rebuilt the BASELINE COMMIT and measured it. **It was 194.9KB there too.** The route never grew. 184.2 had been read out of a `.next` that predated the final build of that commit, so it described a page that had already changed. → **A baseline is a measurement, and a measurement taken from a stale artifact is a guess wearing a number's clothes.** That is exactly the species of error the bundles guard exists to catch — and it caught mine. The rule: when a guard fires on a change that cannot plausibly have caused it, suspect the guard's OWN number before you suspect the code, and go and re-measure it.

2026-07-13 · "A failed article is skipped" — said the docstring; the code only skipped a failed PARSE → `sync_extract` caught malformed responses and let an API exception (a timeout, a 529, a dropped connection) unwind straight out of the loop. In production ONE slow call took down the entire newsdesk narration and the night reported "0 of 0 extracted": sixty articles were queued to be read and not one of them was, because the first was slow. → **A source degrades ALONE is the rule this whole pipeline is built on**, and the loop that most needed it did not honour it. Worse, the docstring SAID it did — so every reader of that function believed the boundary was guarded. **A comment that describes a robustness the code does not have is worse than no comment: it stops anyone from looking.** → Fixed for both surfaces (the evening briefing's remainder pass had the identical hole). And the 30-second timeout I had just added was itself too tight — **a bound that trips on a healthy night is not a safety rail, it is an outage.** The counts are the only reason any of this was visible: a narrator with nothing to say and a dead narrator produce exactly the same page, and `0/0 extracted` is what told them apart.

2026-07-13 · The API returns nothing, and both the plan AND the vendor's own docs said it returned everything → Plan 8.2 said to dispatch a workflow with `return_run_details: true` because "the response carries `workflow_run_id`". GitHub's own REST documentation says the same: a 200, with `workflow_run_id`, `run_url`, `html_url`. **Both are wrong.** The live API answers **204 No Content with an empty body**, and `return_run_details` is not a parameter that exists. → Had I trusted either, every dispatched run would have carried a null run id, could never have been polled, and would have sat on "requested…" forever — a run that fired and a run that never fired looking identical from the couch, which is the exact failure the phase was warned about. → **N4's rule was "record the provider's real response before writing the parser". This extends it: the VENDOR'S OWN DOCUMENTATION IS NOT A RECORDING.** Docs describe an intended API; a `curl` describes the one you have. The whole bridge was designed around a two-minute probe, and the design that fell out (recover the run id by matching a request id printed into `run-name:`) is completely different from the one the plan imagined.

2026-07-13 · A mock that is KINDER than reality does not fail to test the code — it certifies that the code works in a world that does not exist → `R2Store.sync_down` never created its parent directories. Real boto3 writes to a temp file *beside* the destination and raises `FileNotFoundError`; it will not `mkdir` for you. It survived six phases because `FakeS3.download_file` opened with `target.parent.mkdir(parents=True, exist_ok=True)` — **the fake did the one thing the real client refuses to do.** It broke on its first true caller (N6's `compute` mode, on a fresh runner with an empty checkout), in production, immediately. → Nothing had ever really used `sync_down`: the full nightly *writes* the lake to disk (which mkdirs on the way) and syncs **up**. A function with no real caller has no real test, whatever the coverage says. → **This is N3's fabricated-fixture lesson in a third set of clothes.** There the lie was in the VALUES; in N5 it was in the SHAPE; here it is in the FAKE'S BEHAVIOUR. A fake's job is to be cheap, not to be nice — and every convenience you build into one is a bug you have agreed in advance not to find. The fake refuses to mkdir now, and both `sync_down` tests go red without the fix.

2026-07-13 · JSON has no Date type, and an `as` cast is a promise, not a check → The control-room panel's first render comes from a server component and carries real `Date` objects, so it mounted perfectly. Then it polled: `JSON.parse` handed back **strings**, `formatEtClock` threw `RangeError: Invalid time value`, the panel died on re-render, and React kept the old DOM on screen. **A real run — dispatched, accepted, executed, completed, written to the ledger — left the screen showing a Run button and "2 of 2 left today", exactly as if the press had done nothing.** → TypeScript could not see it because I told it not to: `(await response.json()) as {rows: ActionRow[]}` is an ASSERTION, and it was false. `.json()` returns `any`; the cast said "trust me"; the compiler did. **Every `as` on a parsed payload is a place where the type system has been switched off and nobody wrote it down.** → **This is N5's `_json_safe` lesson mirrored.** There: *does the payload actually SERIALIZE?* (a dict that looked perfect killed a production run four and a half minutes in). Here: *does it actually DESERIALIZE into what the type claims?* Same boundary, opposite direction, same class of lie. → The guard asserts the CONSEQUENCE — it round-trips through real JSON and asserts the panel still RENDERS — with a negative control that throws. Asserting that the keys survived would have proved nothing: the keys always survived.

2026-07-13 · Eight bugs, found by running the thing and looking at it → N6 added three more to the tally, and not one of them was visible from inside a green test suite: the panel crashing on its first poll (572 tests green, the button looked inert, the run was real); `compute` mode dying in production on its first execution; and production quietly claiming its data ran "through" a **Saturday**. → The pattern that keeps holding: **the test suite proves the code does what I thought it does. Only the running system proves what it actually does.** The three instruments that have never missed are (1) open the PNG and look at it, (2) fire one real request at the real API before you design around it, and (3) run the thing end to end once, in a browser, against production, with your own eyes on the screen. → And the corollary that keeps costing: **a green suite is evidence about the tests, not about the system.** Every bug above had a test that could have caught it and did not, because the test was built from the same misunderstanding as the code.

2026-07-13 · A guard that does not prove it MEASURED something is not a guard, and the status code is not the witness → The touch-target, sideways-scroll and accessibility sweeps walk a list of routes and measure whatever is on the screen. A story page is addressed by a cluster id, and an id that is not in the database calls `notFound()` — so an unseeded run measured **the 404 page**, found nothing wrong with it, and reported the story page clean. Verified before touching anything: the axe sweep PASSED `/news/nc-fed-hold` against a database that has never held that cluster. → **Then my own fix failed the same way.** I asserted `status === 200`, and it still passed. Recorded: `/news/nc-fed-hold` → **200**, body "404 — This page could not be found"; `/ticker/NOTAREALTICKER` → **200**, same body; `/scans/not-a-preset` → a real 404. A `notFound()` raised INSIDE a statically-generated route is served as **200 with the 404 page in the body**; only a path the router cannot match at all gets a true 404. **So a status check certifies exactly the page it was written to refuse.** → The app already knew half of this: an F-phase QUESTIONS note logged "unknown tickers return HTTP 200 instead of 404" and judged it fairly — *"a wrong status code, not a wrong screen"*. True of what the READER sees. Nobody asked what it does to a GATE. **A cosmetic defect in the product was a load-bearing defect in the instruments**, and it sat there for a month because the two were never thought about together. → The witness is the BODY, because the body is what a reader can actually see. And the sweep now counts what it measured: zero controls in a room that always has a tab bar means the page did not render. **A sweep that swept nothing is a failure, not a pass** — this build has now written that sentence three times (the /paper form that hydrated late, the figcaption that had not loaded, and this), and each time it was a different guard.

2026-07-13 · Two sources of truth for one document is not redundancy — it is a slow-motion lie → `DEVELOPMENT-PLAN.md` is generated from `docs/src/dp-*.html` and says so in its own first line. I edited the generated file anyway, and the next person to run the build script would have silently erased the entire docs sync. Fine — that one is caught by reading. **The one that is not caught by reading:** the PDF is built from `docs/src/development-plan.html`, a SECOND hand-maintained copy of the same document. Nobody ever decided that. It just happened, and then the two copies disagreed for a month. → Measured: `"Amended 2026-07-12"` appeared 3× in the markdown source and **0×** in the PDF source. `APP-FEEL-PLAN`: 3× and **0×**. `/scans/[preset]` — a room that has existed for a month — was simply **absent from the PDF's route map**, which still promised the `force-dynamic` rendering the app had abandoned. **The markdown was right, the PDF was wrong, and nothing anywhere said so, because both files looked authored, current and confident.** → The copy that rots is the one that is READ LEAST — which is exactly the PDF, the artifact the user actually opens. A generated file cannot lie about its provenance; a hand-kept duplicate lies by default, and gets more convincing with age. → One source now: `build-plan-pdf.py` generates the print HTML AND the PDF from the same parts the markdown comes from, and ASSERTS the parts still concatenate into exactly one document rather than trusting it — because a part that quietly stopped closing its tags would print a plausible PDF missing its second half.

2026-07-13 · A picture whose subject depends on the wall clock does not BREAK — it EXPIRES, and the failure looks exactly like a regression → The /paper VRT baselines failed at the nc-final tag with nobody having touched a line of code. The seed dated its closed trades absolutely (2026-07-02, 07-06, 07-07); the app counts "round trips this week" as `closedAt >= now - 7 days`, a window that rolls against the wall clock — which is the CORRECT product behaviour. So: nc-6's CI ran at 19:22Z (cutoff 07-06 19:22, the 07-06T19:50 trade was IN, count 2, baseline photographed); nc-final's ran at 20:39Z (cutoff 07-06 20:39, the same trade was OUT, count 1). **The baseline expired at 2026-07-13T19:50Z — twenty-eight minutes after the run that last certified it.** It took the cost mirror with it: "−31.2%/yr" became "−15.6%/yr", because half the trades had aged out of the projected drag. → **The trap is that re-shooting the baseline WORKS — for one day.** Tomorrow the 07-07 trade ages out too, the count reads 0, and the whole mirror goes to zero. A green re-baseline would have hidden a fixture that was still decaying. **There is no fixed date that stays "this week" forever**, so a fixture claiming "two round trips this week" has to say so RELATIVE to the week. → This is N6's rotting-baseline lesson (the panel photographed at 3pm saying "Markets are open") in a second room, and the general rule is now: **anything a test pins — a pixel, a count, a sentence — must be pinned to something that does not move on its own. If the fixture is absolute and the rule is relative, the test has a fuse on it.**

2026-07-13 · A test can pass because the clock was kind, and encode a rule the product explicitly rejected → The control-room e2e asserted that the `full` ROW must always carry one of five reasons. It passed at nc-6 and failed at nc-final, and the diff between those two runs was **the hour of the day**: nc-6's CI ran 3:22pm ET with the market OPEN, so the row truthfully said "Markets are open"; nc-final's ran 4:39pm ET, after the close, when `full` is genuinely RUNNABLE and has no reason to give. → **And the assertion contradicted N6's own decision while it did.** The missing-token sentence is printed ONCE, above the rows — "the STATE is per-row, the REASON is per-panel" — so a row that is runnable-but-for-the-token correctly says nothing, and the test was demanding that it repeat the panel. A test that passes only in a window of the day is not testing the rule it names; it is testing the window. → The repair is to assert the invariant that is true at EVERY hour: the reader is never left with a row that neither works nor explains itself — the row states a reason, OR offers a button, OR the panel explains the absence. **When a test is time-dependent, the fix is almost never to pin the clock; it is to find the sentence that was true all along.**

2026-07-13 · The warning that has been sitting in the file since phase one, and could never fire → `ci.yml`'s header has said, from the first phase of this build: *"The tag list here and the `if:` conditions on the tag-gated jobs below must be kept in step. A tag family that appears in one but not the other is a gate that silently never runs."* It was right, it was prominent, and it was **a comment** — so for five tag families and four months it was enforced by nobody except whoever happened to read it. → G0 turned it into `pipeline/tests/test_ci_tag_families.py`, and **it caught a real bug on its first run — mine.** Mid-edit I had added `gate-*` and `pd-*` to the trigger but not yet to the oracle's `if:`. That is not a typo; it is the precise half-wired state the disease lives in: `git tag gate-0` starts a run, the run goes **green**, and the only job that can see pixels, seeded data or accessibility never executes. The phase "passes its gate" without one. → **The lesson is not "wire your tags".** It is that *a warning written to a human is a guard that fires only when someone is already looking* — and at an exit, tired, at the end of a long phase, nobody is. The repo has now written this sentence in three forms (a guard that cannot fail; a sweep that swept nothing; a comment that cannot fail a build) and they are all the same sentence: **if it cannot go red, it is documentation, not a gate.** The test that replaced it is negative-controlled, like every guard this build now ships.

2026-07-13 · A guard that gets confused should fail, not guess → The first version of the tag-family test looked for any job condition containing the string `refs/tags/`. Then the same commit added that string to the `app` and `pipeline` jobs — where it means the **opposite** ("sit tag runs out"). The test found three tag-touching jobs where it expected one, and **it failed loudly instead of picking one.** → That failure was worth more than a pass. It forced the guard to say what it actually cares about: not the string `refs/tags/`, but a **named family** (`refs/tags/nc-`). The version that guesses would have gone green while silently reading the wrong line — and a guard reading the wrong line is the same animal as a guard that cannot fail, just better dressed. → The house rule this crystallises: **when a guard's assumption about the world breaks, the correct behaviour is red, not a best effort.** `assert len(gate) == 1, "if a second tag-gated job was added, teach this test which families it owes"` — the message tells the next person what to do, and the build stops until they do it.

2026-07-13 · 43% of the CI bill was spent proving a thing that had been proven one second earlier → The tag run re-ran `app` and `pipeline` on a commit whose main run had passed both jobs — measured on nc-final: tag run 29286543491 and main run 29286513129, the same SHA, one second apart, both running the same 2.9 minutes of work. Nobody designed that; it is what you get when the tag trigger and the branch trigger sit in the same workflow and nothing says otherwise. → **It hid because it was green.** A duplicated check reports success, costs money, delays nothing visibly, and looks exactly like diligence. The only way to see it was to ask what each run *learned* — and the answer for those two jobs was "nothing, twice". → Generalised: **a check earns its place by what it could discover, not by what it verifies.** Re-verifying an unchanged SHA can discover nothing by construction. The oracle stays on the tag (the untouchables require the full suite on the exact shipped SHA, and that rule is about what the suite *could* find — a genuinely different thing to run there). The other two jobs left.

2026-07-13 · A new guard's first act was to break an old one, and only running it showed that → G0 gave ci.yml a concurrency group, `ci-${{ github.ref }}` with `cancel-in-progress`. Correct, and proven: a superseded run died in 40 seconds instead of burning 785. Then G1 added the rehearsal — and a rehearsal dispatched on main carries `github.ref == refs/heads/main`, **the same ref, and therefore the same group, as main's own push run.** The first rehearsal ever dispatched (29291878002) cancelled the push run of the very commit it was rehearsing (29291818293). → I did not deduce this. I dispatched the thing and watched a run I wanted die. The house rule that keeps paying — *the vendor's documentation is not a recording* — generalises one step further here: **your own last phase's design is not a recording either.** G0's group was right about the world it was written in, and G1 changed that world. → **The dangerous direction is the reverse one**, and it would never have announced itself: push a docs commit during a 9-minute rehearsal and the *rehearsal* dies, and the exit walks to its tag believing it rehearsed. Nothing red, nothing missing, no message — the gate that silently never runs, arriving through a door that did not exist a day earlier. Two features, each individually correct, whose composition is a silent hole. **The only reason this is a footnote and not the next phase's 90-minute bug is that the guard was exercised the same hour it was built.**

2026-07-13 · `--update-snapshots=all` hands you every shot, not the shots that moved — and the difference is the whole rule → Proving the auto-mint on a scratch branch, I made one controlled change: `gap-12` → `gap-14` on the `/styleguide` wrapper, and nothing else in the tree. Two shots failed on the desktop leg (styleguide light and dark). **The candidate artifact came back with six byte-different files.** `desk-light`, `desk-dark`, `ticker-dark` and `track-record-light` were re-photographed too — and those rooms *cannot* have changed, because nothing outside `/styleguide` imports that page. → They passed their comparison, correctly, inside the 600-pixel tolerance, and left no `*-diff.png` at all. They came back different because **`=all` re-shoots every baseline from what renders right now**, and rasterisation jitters a few dozen pixels between runs. `=all` stays — it is the only reason a real change hiding inside the tolerance ever gets caught, and it is an untouchable for exactly that reason. → But the consequence had never been written down: **`cp candidate/* baselines/` commits four files you cannot explain**, and "an unexplained diff is a build failure" is the one rule this suite has. The mint's convenience quietly undermines the mint's purpose. That is very likely a large share of the churn this build has been paying — 283 snapshot files rewritten, whole-world re-bakes of 44 and 58 files, phase after phase. → The rule now: **the triptych is the list of what moved; the candidate is only where you fetch those files from.** Copy them one at a time. Tedious on purpose — each line is a thing you looked at and can explain.

2026-07-13 · The mint would have destroyed the evidence it exists to explain → The auto-mint re-runs `vrt.spec.ts` to shoot fresh baselines. **Playwright empties its output directory at the start of every run** — so that re-run deletes `test-results/`, which is where the expected/actual/diff triptych lives. The step written the obvious way (test → detect → mint → upload everything) would have handed back beautiful new baselines and **thrown away the only picture that could tell you whether to trust them.** → Caught by asking, before writing the step, "what does this command do to the directory the previous step wrote?" — not by a test, and not by CI, which would have gone green: the mint works, the artifact uploads, and the triptych is simply *absent*, which looks like "no diff images this time" rather than like a bug. → **Ordering is a correctness property, not a style choice.** Photograph the crime scene before you rebuild on it. The failure-artifact upload now runs before the mint, and the workflow says why in a comment that names the mechanism, because the next person to reorder these steps for tidiness will not otherwise know.

2026-07-13 · Main can move under you, and it did → Mid-endgame a push to main was rejected: the remote held `92f7d72 chore: heartbeat`, authored by `github-actions[bot]`. An **empty** commit — no files changed — pushed by `nightly-a.yml` after each full nightly run, because GitHub disables a private repo's scheduled workflows after 60 days without repo activity. Legitimate, documented in the workflow, and completely invisible until it collided with a session that happened to be tagging at 7:42pm. → The tree is byte-identical, so nothing was harmed. **But the invariant it threatens is an untouchable:** "the full suite runs on the exact tagged SHA." Rehearse `X`, heartbeat lands, `git tag gate-1` on `HEAD` — and the tag now sits on a commit the oracle never saw. Today that commit is empty. It only has to be non-empty once. → **Tag the SHA you rehearsed, by SHA.** `git tag gate-1 <sha>`, never `git tag gate-1`. Written into CLAUDE.md's command block, because this is precisely the kind of thing a tired session at the end of a phase will not remember on its own.

2026-07-13 · The one locator call that does not wait, and the red that blamed the page for it → G2's rehearsal went red on `news.spec.ts:97` — "every story on the page is reachable by some chip" — **expected 13, received 0**, on a tree whose app code was byte-identical to the one `gate-1` had proved green an hour earlier. Nothing had been touched. → The machinery told me the shape of it before I read a line: the auto-mint **declined to fire** ("no `*-actual.png`: this red is an assertion, not a pixel"), so it was logic, not pixels. And the failure artifact had the answer sitting in it — the page snapshot in `error-context.md` **stops at the press line**. The banner, the heading and "Assembled … 14 catalysts" had rendered. The filter row had not. The stories had not. → **`locator.all()` is the one locator call in Playwright that does not auto-wait.** It hands back whatever is in the DOM at that instant. The filter row is rendered by `NewsFeed`, a `"use client"` component, inside a **streamed Suspense boundary** — and `page.goto("/news")` resolves before that chunk arrives. So `.all()` returned an empty list, the `for` loop ran **zero times**, `reachable` kept its initial `0`, and the assertion failed. **The test reported a broken filter row when what had actually happened is that it measured a page that had not arrived yet.** Every other test in that file is safe *by luck*: they locate a chip by name and *click* it, and clicking auto-waits. One call in the whole suite skips the wait, and it is the only one that enumerates. → The temptation here is the whole lesson. It looked exactly like a flake, it sat on an untouched tree, and `gh run rerun --failed` would have turned it green in eight minutes. **It failed on its retry too, and the mechanism is knowable in ten minutes of reading.** `rerun --failed` is for a *suspected* flake; re-running a race until it passes is how a real defect becomes folklore. → The fix waits for the row, then **states how many chips it is about to sweep**, so a zero can never again be mistaken for a finding. **This is the third costume of the same house rule** — a sweep that swept nothing is a failure, not a pass (the `/paper` form that hydrated late; the figcaption that had not loaded; the sweeps that measured a 404 page). It keeps coming back in a new disguise because *every asynchronous surface invents a fresh way to be absent*, and a test that counts is a test that can count zero and call it an answer.

2026-07-13 · A path filter is a new door into "the gate that silently never runs", and the vendor's word is not enough to close it → G2 made documentation free (`paths-ignore` for `**/*.md`, `docs/**`, `.claude/**`) — 58 of 249 commits were prose paying full CI to prove they had not broken the compiler. Cheap, obvious, and it carries **one edge that could have destroyed the phase**: this build deliberately tags phase exits on docs-only commits. If GitHub applied path filters to **tag** pushes, `git tag gate-2 <a docs commit>` would start nothing at all, the phase would "pass its gate" with the browser oracle never running, and **nothing would say so**. → GitHub's documentation says path filters are ignored for tag pushes. That is the right answer, and it was still not good enough to build on: **this repo has caught GitHub's own documentation being wrong twice** (the dispatch API returns an undocumented empty `204` with no run id; the ref-keyed concurrency group that cancelled the very run it was rehearsing). So it was proven live, in both directions, with the same query shape each time so that a zero could never be an artifact of looking in the wrong place: `.yml` commit → ran; `.ts` commit → ran; **empty** commit → no run; **docs-only** commit → no run; a `workflow_dispatch` on that docs-only SHA → **ran anyway** (so the rehearsal survives on exactly the commits a tag lands on — had *that* not held, G2 would have quietly disarmed G1); and the `gate-2` tag on the docs-only commit → **the full three-leg oracle, green, 7 m 59 s**. → The general rule, now earned three times against the same vendor: **when a change's safety rests on a behaviour you did not observe, the phase is not the change — the phase is the observation.** The safety check that mattered most took ten minutes and never appears in the diff: *does anything in the gate read the paths I am about to silence?* (Nothing did. If a future guard ever starts reading a document, that list is where it breaks, and it breaks silently.)

2026-07-13 · The guard that reported a room without ever judging it — a silence, not a failure → G3 built one routes manifest and made `check-bundles.mjs` reconcile its baseline keys against it. On the very first run the reconciliation fired: **`/scans/[preset]` — the match table, the app's ONE `DataTable`, the room a bundle guard should most want to watch — shipped in F3 and had never had a baseline.** → Look at how it hid. `BASELINE_KB["/scans/[preset]"]` came back `undefined`; the verdict expression is `base === undefined ? "" : …`, so the route printed its size, printed **an empty verdict column**, and was never compared to anything. Every run, for phases. The table looked complete. Twelve rows had a ✓ and one row had nothing, and nothing is not a shape the eye reads as an alarm. → **This is a new species of the rot-by-omission class, and it is worse than the old one.** When `/news` was missing from the sweep lists, the sweep at least never claimed to have looked at it. Here the guard *ran*, *printed the room*, and reported a number — while judging nothing. A missing entry is invisible; a missing VERDICT looks like a pass. → The general rule: **a check that can be undefined for an input must say so out loud, or it will read as consent.** The fix is not "remember to add the baseline" — it is that the guard now reconciles its keys against the one list of rooms, in both directions, *before it measures anything*, and refuses to run if they disagree. A lookup that silently returns `undefined` is a hole with a table drawn around it.

2026-07-13 · The gate that would have cried wolf, and the rule I did not write → G3's plan asked for drift rule 21: "no absolute date literals in `prisma/seed.mjs` or `e2e/*.spec.ts` outside the sanctioned constants". Written literally, that rule fires **thirty times on a correct tree** — `seed.mjs` alone holds ~25 dates, and every one of them is load-bearing. → **The seeded world IS a fixed morning, and it has to be.** A seed whose dates drifted with the calendar would repaint every VRT baseline every night, and the pixel oracle would be photographing whatever today happened to hold instead of a known state. So a rule that banned dates in the seed would have been a gate firing on ~25 lines that are *not* fuses — and *a gate that fires on correct code trains its reader to skim past it*, which this codebase has now learned three separate times (axe's minor/moderate buckets; the 1% pixel ratio; the faint-token rule that rejected the correct `disabled:` state). Shipping it would have been learning it a fourth. → **The fuse is not "a date exists". The fuse is a SECOND, UNNAMED date.** `/paper` blew up because its fixture's dates and the app's rolling window were two clocks that walked apart. `vrt.spec.ts` and `desk.spec.ts` each carried their own hand-written copy of the seeded evening — two clocks again, one edit away from disagreeing. So the rule became: **there is exactly one date per world, it has a name, and everything else derives from it.** Thirty literals → two anchors. It fires on the thirty-first. → And the readability trap inside the fix, because it nearly cost more than it saved: `monthStart(1)` exists because CPI's as-of date, written as a day offset, is `sessionPlus(-38)` — *honest, and unreadable*. **A fixture nobody can check against a calendar is how the wrong one survives.** Every derived call site carries its answer in a trailing comment, and rule 21 exempts comments precisely so it can. An anchoring rule that makes the fixtures unreadable has traded one silent failure for another.

2026-07-13 · When the plan and the tree disagree, the plan's INTENT is the thing to recover — not its wording, and not its silence → Three of G3's five items could not be executed as written, and each disagreement was a fact about the tree the plan's author had not seen. The plan named `wide` as a manifest field: **nothing in the codebase scopes the wide viewport by route** (it is scoped by spec file, in `playwright.config.ts`'s `testMatch`), so the field would have had no consumer. The plan said to put the "3am and 3pm" checklist line in the `new-lesson` skill: **`new-lesson` authors Academy lesson MDX** and has nothing to do with test assertions. The plan said rule 21 would exempt "the sanctioned constants" and named only e2e-side ones: **`seed.mjs` had 25 dates and no anchor at all.** → GATE-EFFICIENCY-PLAN Part 1 anticipates exactly this — *"the tree wins on the detail and this plan wins on the intent"* — and the value of that clause is that it tells you **which way to fail**. Following the wording would have shipped a dead field, a checklist line nobody would ever read, and a gate that cried wolf thirty times. → **The discipline: when a plan's instruction does not fit the tree, do not silently do the nearest easy thing, and do not down tools. Name what the instruction was FOR, satisfy that, and write the deviation down where the person who wrote the plan will see it** (three entries in DECISIONS, three in QUESTIONS). A deviation that is argued is a correction. A deviation that is quiet is drift.

2026-07-13 · A document that restates a machine's rule will eventually contradict it — so make the document POINT at the register, never copy it → Seven separate texts declared that amber has "exactly two consumers": UI-REDESIGN's P11 row, its §3.10 grep list, its Appendix A CSS comment, APP-FEEL's P11 row, the `new-surface` skill, DEVELOPMENT-PLAN's own token table, and three shipped source comments (`Tag.tsx`, `BriefArticle.tsx`, `paper/page.tsx`) · the truth, in `check-drift.mjs`'s `ALERT_ALLOWED`, was FOUR — grown by two properly-logged structural amendments (N2's PipelineStrip AGING state, N3's MacroBoard STALE cell), each argued in place, each correct · so the machine was right and every human-readable description of it was wrong, for two tagged phases, and NOBODY NOTICED, because all seven looked authored, current and confident · AN EXECUTOR OBEYING THE TEXT WOULD HAVE STRIPPED SANCTIONED AMBER AT A PHASE EXIT — the documents were not merely stale, they were actively dangerous · **and the register's OWN DOCSTRING said "two" as well**, which is the part that turns this from a stale number into a lesson: had I pointed the six documents at a register whose header was wrong, I would have relocated the lie rather than killed it — the register gets repaired FIRST, always · guard: the count is deleted from every document; they all cite `ALERT_ALLOWED`; and the rule is restated as what it always meant — amber is RESERVED, the list is SHORT, every entry is ARGUED IN PLACE — because P11 was never a count, and a rule expressed as a number is a rule with a fuse on it · the same file said "Eleven greps" while running 21 rules, so it now PRINTS ITS OWN COUNT at the end of every run, which is the only number that cannot rot

2026-07-13 · An empty fallback and a guard that cannot fail are the same bug — both answer "nothing to report", and that is indistinguishable from working → `check-font-budget.mjs` carries an emergency drop-order and states its own purpose: "the drop order is fixed in advance so the decision is not made at 2am under pressure" · it named Inter 500 first, then Playfair 600 · BOTH HAD ALREADY BEEN DROPPED AT R6 — which is precisely WHY there is 317KB of headroom today · so the ladder that exists to prevent a panicked 2am decision was A BLANK PAGE, and the only way to discover that was to reach for it at 2am · this is G3's finding in a different costume: G3 found a route whose bundle baseline was `undefined`, so the verdict column printed an empty string and the room was REPORTED WITHOUT EVER BEING JUDGED · a silence in a gate is indistinguishable from a pass; an escape hatch with no rungs is indistinguishable from a plan · guard: when you write a fallback, a drop-order, a code-split hatch or a "if this fails, do X" — ASK WHAT WOULD ACTUALLY HAPPEN IF SOMEBODY REACHED FOR IT, and if the answer is "nothing, it's already spent", say so IN THE COMMENT with the line numbers · and never write the current total into the prose (`npm run check:fonts` prints it live) — the number in the comment is the thing that rots

2026-07-13 · The tree wins on the detail, and the exit greps are the phase — not ceremony at the end of it → G4's Appendix A was a 22-row checklist, written the same day, by a session that had just read every one of those files · it still MISSED FOUR LIVE INSTANCES of the defect it was hunting, and one of them was the most authoritative statement of the rule in the entire repo: DEVELOPMENT-PLAN's `--alert` token row, reading "Exactly two consumers exist" · the other three were shipped source comments · the plan's own exit criterion (`grep -rn "two consumers"` returns only register-pointers) is what found them, ten minutes before the tag · guard: when a phase's exit criteria contain a grep, RUN IT AND READ WHAT IT RETURNS — not what you expected it to return · and expect it to return more than the plan enumerated, because a plan is a snapshot and the tree is the truth · corollary, learned the same hour: the grep will also return FALSE POSITIVES that are different rules wearing the same words (the brand gradient's "exactly two consumers" is TRUE; the scan presets' "one definition, two consumers" is TRUE and about preset keys) — so the honest exit report says WHICH instances survive and WHY each is correct, rather than claiming a grep came back clean when it did not

---

## PD0 (2026-07-13) — session truth

### A fixture that can certify what production forbids is not a fixture. It is a trap with a green tick on it.

**What happened.** PD0 made `publish` refuse a non-session `run_date`, and made the nightly derive
its edition from the bars. Then the nightly's OWN TEST FIXTURES turned out to be guilty of the exact
bug: `_history()` walked *calendar* days (so a fake night could end on a Saturday) and was anchored at
the START, while the night's `run_date` sat beside it as a hard-coded `date(2026, 6, 10)` that did not
move when a history's length changed. So a fixture night described one session and CLAIMED another —
the production bug of 2026-07-11, reproduced inside the test suite, green, for months.

**Why it matters more than it sounds.** The seeded world is what every e2e journey and all 76 pixel
baselines are photographed against. A guard that production obeys and the fixtures do not is a guard
with a hole in it: the tests would be green, the app would be right, and they would be describing
different worlds.

**The guard.** Fixtures walk trading sessions and anchor on the session they END on (a night IS a
session, with however much history behind it). `prisma/fixtures/sessions.test.ts` holds the seeded
world to the same calendar as production, and is negative-controlled so a sweep that finds nothing
fails itself.

### Drift rule 21 made the seed's dates AGREE. It could not make them LAWFUL.

G3 anchored every seeded date to one clock (`fixtures/clock.mjs`), which is what stopped them drifting
apart. But the offsets are **calendar days, not sessions**: from Thursday 2026-07-09, `sessionPlus(-15)`
lands on a Wednesday and is fine, and `sessionPlus(-12)` would land on a **Saturday** and is not.
Nothing about the anchor tells you which. Today every offset happens to be lawful; the next one
somebody writes is a coin flip, and the failure would have surfaced as a VRT diff in a room nobody
was editing. **One-source-of-truth is not the same property as correct-by-construction.** Both need a
guard.

### The plan's own census was wrong, and the shape of the error is worth remembering.

Plan 3.3 said the app had "exactly one" local weekday formatter to delete. It had one **display**
formatter — and a **second** thing using `{weekday}` that was not a duplicate at all: `market-hours.ts`
asks New York what day it is in order to DECIDE whether the market is open, and compares the answer
against `"Sat"/"Sun"`. No reader ever sees that string.

A rule written to the census ("`weekday:` outside lib/time.ts — zero matches") would have failed the
build on correct code, and the obvious fix — make the market-state check import a display formatter to
do arithmetic with — is worse than the thing the rule prevents. **Drift rule 22 names two doors, each
with its reason, and a third fails the build.** A census counts what it was looking for; ask what else
uses the mechanism.

### A comment inside a bundled JSON is a comment shipped to every browser.

Moving the holiday table to `lib/market-calendar.json` (one list, three readers) cost `/news` 1.8 KB
against a **200 KB hard ceiling with ~4.9 KB of headroom left**. 0.6 KB of that was an explanatory
`_comment` key I had put inside the JSON to explain itself. JSON has no comments; a `_comment` key is
DATA, and data in a client bundle ships. The prose moved to `market-hours.ts`, where the compiler
strips it. **Measure, do not assume:** the remaining 1.2 KB is real and is now booked, and PD5/PD9
must plan against **3.7 KB**, not 4.9.

### The instruments that can see production are local-only BY NATURE, and that is the whole point.

`check:migrations` asks "does the live database run the schema in this repo?" `check:live` (new) asks
"is the live app telling the truth?" **CI can answer neither, structurally** — it builds a fresh
database and a fresh deployment on every run, so every question it can ask, it asks of itself.

That gap is not theoretical. It is how production ran for days without N0's migration, and it is how
this app spent two days claiming a Saturday close with green CI on every commit. **On its very first
run against production, `check:live` found a live defect nobody had reported** (the retired-provider
calendar rows — see below). Every other guard in this repo asks whether the code is correct. Something
has to ask whether the answer is true.

### "It will self-heal on the next run" is a hypothesis, not a fix.

The plan predicted the stale `Coinbase Cryptocurrencies` calendar rows would be rewritten by the next
nightly (the write path was allowlisted a month ago). They were not, and the reason is exact: the
calendar refresh **replaces the forward window**, and a row that has fallen **behind** the window is
not in the window, so the replace never touches it. The rows rot in place.

**Fixing the write path does not clean the table.** Nothing that ran in CI could ever have told you —
only something that reads production. PD1 repairs it.

### A fixture whose docstring is a promise it does not keep is worse than no fixture.

**What happened.** The pipeline's `db` fixture says, in as many words, "every table, truncated
between tests." Its list named **9 of the schema's 23**. Fourteen tables — `briefing`, the analytics
set, the news set, the macro board, the paper ledger — were never cleared, so rows accumulated across
a whole pytest session and leaked from one test into the next. A new PD0 test asked
`SELECT run_date FROM briefing` and read back a row written twelve tests earlier.

**Why nobody had ever seen it, which is the real lesson.** Every db-backed test **SKIPS on a machine
with no Postgres.** The local suite is green, always. So the defect was structurally invisible to the
person most likely to introduce it, and could only ever have been caught in CI — which is exactly
where it was caught, on the first push of a phase that added the first new db-backed tests in months.

**Two guards, because either alone leaves the trap.** The fixture truncates all 23 tables now. And
the test asks for **the row it wrote**, not for "the only row in the table" — *a test that depends on
a table being otherwise empty depends on every other test in the suite, and on their order.*

**The general shape:** when a test harness can only run in one environment, its bugs live in that
environment and nowhere else. Distrust any local green that comes with a skip count.

---

## PD1 — Fixing a write path does not clean a table (2026-07-14)

**The plan predicted these rows would self-heal. It was wrong, and it was wrong for a reason worth
keeping.**

Four rows from a provider retired a month ago — "Coinbase Cryptocurrencies", dated on a Saturday and
a Sunday — sat on the live Desk. The allowlist that stopped them being written landed weeks earlier
and was correct. The plan reasoned: the calendar refresh replaces the forward window on every run,
so the next run will overwrite them.

The refresh deleted `WHERE date >= run_date`. **A row that has fallen BEHIND the window is not in the
window.** Nothing ever touched it again. The fix to the write path could not reach the rows the write
path had already written, because the delete only ever looked forward.

**The general shape: a write-path fix changes what happens NEXT. It says nothing about what is already
in the table.** Whenever you fix an ingest, ask the second question out loud — *what did the broken
version already leave behind, and what will ever go back for it?* Very often the answer is "nothing",
and it will be "nothing" forever, silently.

### And it was not cosmetic — it was evicting the real rows

The Desk takes the 15 earliest calendar rows in date order. The four dead rows sorted **first** and
spent 4 of those 15 slots. The table held events through **Jul 23**; the reader was told the calendar
ran **"through Jul 16"**. The rot had quietly **truncated the product's horizon by six days**, and
every test was green, because every test was asking whether the app rendered its rows correctly — and
it did.

**Stale data in a bounded list does not just add noise. It displaces signal.** A `take: N` with no
lower bound is an eviction waiting for something to rot.

---

## PD1 — If a surface is derived from the EDITION, measure it against the EDITION (2026-07-14)

**The hardest bug of the phase was in the instrument, not the product — and it appeared three times
in one hour.**

This app serves a dated **edition**, like a newspaper. Monday's paper is still Monday's paper at 1am
on Tuesday. But three different pieces of code compared an edition-derived surface against the **wall
clock**:

| where | it demanded | production said | who was right |
|---|---|---|---|
| `loadCalendar` (the product) | *no floor at all* | — | neither: the bug |
| `checkNextEdition` (the gate) | "Wed", the day after *today* | "Tue" — the next edition, landing tonight | **production** |
| `checkCalendar` (the gate) | every row ≥ *today* | Monday's edition carries Monday's own FOMC decision | **production** |

Between midnight ET and the ~6:40pm publish, the clock and the edition disagree. **Both checks would
have failed a completely healthy Desk every single night** — starting the night `check:live` joined
the standing gate. I only saw it because I ran the instrument against live production at 00:25.

**A gate that cries wolf nightly is not a gate.** It is a thing people learn to ignore, and then it is
not there on the night it is right. Both checks now take **no clock at all**: they read the edition
off the masthead and measure against that. A *stale* edition is a different assertion's job, and it
does that job loudly — so nothing was lost by removing the clock.

**The rule, now written into CLAUDE.md and live-truth.mjs: IF A SURFACE IS DERIVED FROM THE EDITION,
IT IS MEASURED AGAINST THE EDITION.** Two clocks in one system will always disagree eventually; the
only question is whether they disagree where someone is looking.

**Corollary, and it is the meta-lesson of the whole PD0→PD1 pair:** a new guard's first red is as
likely to be the *guard* being wrong as the product. **Read the failure before you believe it.** PD0's
lesson was "distrust a local green with a skip count". PD1's is the twin: **distrust a new guard's
first red.** Both of PD1's were the checker.

---

## PD1 — Re-sample an advisory number before you explain it (2026-07-14)

The first post-deploy Lighthouse run reported performance **77**, down from the historical **87**, and
LCP 3.86s → 4.98s. It would have been easy to shrug (it is advisory) or to panic and go hunting
through a change that could not plausibly have caused it (the Desk is a cached room; the extra query
is paid at revalidation, not per request).

I re-sampled twice: **87** and **86**. Synthetic-4G runs vary by ten points. There was nothing there.

**A shrug and a panic are equally wrong, and both are cheaper than a second measurement.** Take the
second measurement — it cost two minutes and it is the difference between "I think" and "I know".

---

## PD2 — A library that "works" can be ignoring you (2026-07-14)

The OG card needs two lines of type. sharp renders text, and it takes a `fontfile` option, so the
obvious thing is to point it at the app's own fonts. It ran. It produced an image. The image had
words in it. Everything looked fine.

Then I measured, because the output is committed and I wanted to know it was reproducible:

```
"myStockMarket", 40px, no font named at all   ->  1135 x 159
"myStockMarket", 40px, JetBrains Mono Medium  ->  1135 x 159     <- MONOSPACE
"myStockMarket", 40px, Inter Regular          ->  1135 x 159     <- PROPORTIONAL
```

**Three different fonts cannot set one string to one width.** Pango was ignoring the `fontfile`
entirely and substituting whatever this Mac happened to have installed. The card would have shipped
in a system font — and in a *different* system font on the next machine — and every test would have
passed, because no test in the world asserts "this text is in the typeface I asked for".

**A silent fallback is worse than an error.** An error is a fact; a fallback is a lie that renders.
The defence is not to trust the API — it is to construct a measurement the API cannot fake. Two
fonts with different metrics must produce different widths. If they do not, the parameter is dead,
whatever the docs say.

The fix removed the class of bug rather than the instance: the strings are now converted to vector
outlines from the vendored TTFs, so **by the time anything is rasterised there is no text left, only
shapes.** Nothing to resolve, nothing to fall back to, identical on a machine with no fonts at all.

## PD2 — A tolerance is a place where bugs go to live (2026-07-14)

Every room's VRT shot moved by exactly 746 pixels — the new 28px mark, in the identical place. The
Desk moved by 1,291. That extra 545 was at the *bottom* of the page, and blown up it said:

```
old baseline:  none saved tonight
new actual:    + 1 more · 1 saved tonight
```

`briefing.spec` writes a journal entry, runs before `vrt.spec`, and never cleans up. So the Desk the
full oracle photographs and the Desk the standalone baseline job photographs **have disagreed by 387
pixels for as long as the baseline has existed.** Nobody knew, because `maxDiffPixels: 600` was
absorbing it.

Two lessons, and the second is the bigger one.

**A pixel baseline that depends on which tests ran first is not a baseline.** `desk.spec.ts` had
already worked this out and written it down — *"asserting 'none saved tonight' would be asserting
the test ORDER, not the product"* — and then the oracle went on asserting exactly that, in pixels,
where nobody would read it. A rule enforced in one instrument and not in its neighbour is a rule
with a hole in it.

**And: only 14 baselines went red, but 59 had actually changed.** Forty-five shots moved by 746 px,
sat under the tolerance, and would have gone on passing *while showing a top bar this app no longer
has*. The tolerance was not protecting the gate from noise; it was hiding real drift from it — and
it had been hiding a 387-pixel bug for months in the same breath.

**A baseline that is tolerated is still a baseline that is wrong.** Re-photograph everything that
moved, not everything that failed. What the gate FORGIVES is exactly where the next bug will sit,
because it is the one place nobody is looking.

## PD3 — A baseline proves the page did not CHANGE. It never proved the page was RIGHT. (2026-07-14)

**The pixel oracle had been photographing a broken page for months, filing it as the baseline, and
passing it on every single run.**

`/ticker` on a phone renders the Range Ladder — the app's central probability visual, the thing that
says what a range actually *means*. Its sentences were coming out one word per line:

> In / the / past, / 8 / in / 10 / 5- / day / paths / from / here / stayed / inside / this / range.

That is what a reader on a phone has been seeing in production. `ticker-light-phone-linux.png` and
`ticker-dark-phone-linux.png` are *photographs of it*, committed, green, run after run.

**Why the oracle could not catch it.** A VRT baseline is a comparison against the *previous* picture.
It catches CHANGE. It cannot catch a page that was already wrong when the first picture was taken —
in that case the baseline is the bug, and the oracle's job becomes *defending* it. Nothing ever fails.
Nothing ever will.

PD2's law was "a baseline that is TOLERATED is still a baseline that is WRONG" (the 387px journal
count, hiding under `maxDiffPixels`). **This is the harder version of it: a baseline that is EXACT can
still be wrong.** The tolerance was innocent here. The picture itself was the lie.

**How it actually surfaced, which is the part worth internalising.** Not from a test. PD3 moved the
styleguide's gutter by four pixels — an incidental side-effect of extracting `PageContainer`. That
made the styleguide's copy of the same component fall into a *different* wrong state, which changed
its **height**: the candidate baseline came back **272px taller**.

And a page that gets **wider** is not supposed to get **taller**. That sentence is the whole catch. It
was a smell in a number, in a diff I only had because PD2's law made me diff every candidate rather
than read the failure list. I pulled the thread because the arithmetic was absurd, not because
anything was red.

**What to actually do with this.** You cannot re-derive every baseline from first principles. But:

1. **A resize is a louder signal than a diff.** A shot that changes *height* changed its LAYOUT, not
   its pixels. Never wave one through on "the container moved" — check that the height moved in the
   direction the change implies. Wider → shorter. Taller on wider is a bug, every time.
2. **When you touch a shared container, the diff is your one free audit of everything inside it.** Every
   component in that container just got re-photographed at a width nobody chose. Read the heights.
3. **A brand-new surface's first baseline deserves eyes.** It is the only moment anyone will ever look
   at it with fresh judgement. After that it is just "unchanged" forever.

## PD3 — `flex-1` is a flex-basis of ZERO, and a zero-basis item cannot wrap. It can only be crushed. (2026-07-14)

The mechanism behind the ladder bug, worth knowing on its own because it is a trap in ordinary CSS.

```
<li class="flex flex-wrap ...">
  <span class="w-10 shrink-0">5d</span>
  <span>-4.30% to +4.80%</span>
  <span class="min-w-0 flex-1">In the past, 8 in 10 5-day paths…</span>   <-- flex: 1 1 0%
  <span class="shrink-0">n=495 · 500d window</span>
</li>
```

`flex-1` is `flex: 1 1 0%`. The **flex-basis is zero**, so the item's hypothetical main size is zero,
so **it never contributes to the line-breaking decision**. The row therefore believes all four items
"fit" — because the sentence can shrink to nothing to make room for them — and instead of wrapping, it
**crushes the sentence to about one word wide**.

The behaviour balances on a knife edge:
- **narrow enough** → the `shrink-0` caption wraps to its own line, the sentence gets full width. Fine.
  This is what every fixed-width baseline in the suite happened to catch.
- **eight pixels wider** → the row stops wrapping, and the sentence becomes a vertical column of words.

**The fix is a real minimum, not a smaller font.** `min-w-[18ch]` — because a flex item's `min-width`
**does** participate in line breaking (the hypothetical main size is the flex base size *clamped by
min/max*). Give the thing a floor and the line wraps when the floor does not fit, which is what it
always meant to do.

**The general rule: in a `flex-wrap` row, any item that must stay legible needs a `min-w-*`.** `min-w-0
flex-1` says "I will shrink to nothing rather than let this line wrap", and for a column of prose that
is never what you meant.

**And: sweep a width RANGE, not the widths you happen to shoot.** This bug lived in a band that all
four VRT viewports missed on the styleguide and that one of them (phone) hit on the ticker — where it
was silently baselined. A 14-width sweep from 320 to 1536, run in a loop against the real page, found
and then closed the whole window in about a minute.

---

## PD4 — the phone composition (2026-07-14)

### A page-level overflow guard is STRUCTURALLY BLIND to a cell-level overflow

This is the most transferable thing PD4 found, and it nearly shipped a bug *while I was writing the
guard against it*.

The sideways-scroll sweep asks the **document** a question: `scrollWidth === clientWidth`. A cell
that spills into the cell **next door** lands its spill *inside* the page, never past its edge. So
the document reports zero overflow — honestly — while a figure sits under the border of its
neighbour.

PD4's first tape row did exactly this: index levels overflowing their cards by 8px, delta chips
shattered into three lines, **and every guard green** — the unit tests, the class contract, and the
brand-new 360px sweep I had just written. Only the screenshot showed it.

PD3's law was *"layout is asserted in bounding boxes, never the DOM."* The sharper form:

> **The box you measure must be the box the bug is in.** A guard aimed one level too high is not a
> weak guard — it is a guard that will never fire, and it will make you confident while it does not.

And measure to the **content** edge, not the border edge: a figure sitting in its own card's padding
is already touching the wall, one character from being outside it.

### A guard pointed at the comfortable end of a range is not measuring the range

The Desk scrolled sideways **16px at 360px, in production**, and the sweep built to catch precisely
that was green — because it ran at each project's viewport, and the phone project is a Pixel 7 at
**412px**, the one phone width where the defect does not occur.

The sweep was never wrong. It was **pointed at the easy end**. Same family as PD3's baseline lesson
(*an exact baseline can still be wrong*) and the N1 ratio-tolerance lesson (*a ratio makes a gate
weaker the longer the page gets*): the instrument was fine and the aim was not.

**So: a sweep must state the RANGE it covers, and cover both ends of it.** PD4's sweep now runs at
360 as well as 412, and it **counts the rooms it visited** and refuses to pass if it swept none — a
sweep that measured nothing must never be allowed to report success.

### "Wrapping is honest" is a claim about a SENTENCE, not about a phrase

The rule that came out of PD3 — *numbers never truncate, never ellipsize, never clip; wrapping is
just typography* — is true, and I over-applied it and produced the same bug it was written to
prevent.

Told to wrap rather than clip, a delta chip wrapped into `▲` / `+0.29%` / `· 1D`: **three lines, one
token each.** That is PD3's Range Ladder, one component over.

> A phrase broken one word per line has not been **wrapped** — it has been **shattered**, and a
> shattered figure is no more readable than a truncated one.

**The unit of wrapping is the ATOM: the smallest group that still means something on its own.** A
delta chip has two — the signed delta (`▲ +0.29%`, one fact in three redundant channels) and its
window (`· vs prior week`, the delta's unit). Wrap *between* atoms; `whitespace-nowrap` *within*
them. And this was not merely my own bug to fix: **the mortgage cell has been rendering "· vs /
prior / week" in production**, because its window span had `white-space: normal` and nothing forbade
it.

### When the plan's arithmetic and the ruler disagree, the ruler wins — and then you AMEND

Part 7.1 specified a 3-up grid of cards and estimated "≈112px interior". Measured: **74px at 360**.
The gap is not tunable — a mono numeral has no wrap opportunity inside itself, so it cannot be made
to fit, only to overflow.

The temptation is to shrink the type until it "fits". I got it to within **1px** of breaking. **A
design one pixel from failure is not a design; it is a coin flip.** The right move was to change the
SHAPE (a full-width list), keep the plan's *argument* intact, and log the amendment loudly — in
DECISIONS.md, in the evidence file, and **in the plan itself**, so the plan and the code never
disagree in silence.

### Two local test failures that are harness, not code — recognise them fast

- **`thin-night`'s Law-2 test measuring a stale render.** Playwright reuses a hand-started server
  (`reuseExistingServer` locally). If that server lacks `CRON_SECRET`, thin-night's ISR cache-bust
  silently no-ops and the test photographs the **full** night — 318px where it wants ≤120. It looks
  exactly like a layout regression. **Let Playwright start its own server**; its `webServer.env` has
  the secret.
- **`ticker-range` failing only in parallel.** The documented one-database race. Serial, green.

---

## PD5 — the voice (2026-07-14)

### A DUPLICATED COMPONENT IS NOT A BUG. IT IS A BUG'S HABITAT.

PD4 discovered the delta chip's wrap contract the hard way — a chip that shattered into `▲` /
`+0.29%` / `· 1D`, three lines, one token each — and fixed it. **In StatFigure.** There were FOUR
copies of that chip in the tree (StatFigure, Movers, Watchlist, NewsCard). The other three were never
touched, so all three still carried the exact shape of the bug PD4 had just spent a phase killing: no
`flex-wrap`, no atoms, a chip that could only overflow its cell.

Nothing failed. Nothing would have. The fix lands in one copy and the other three go on being wrong,
quietly, in production, while the test suite stays green and the lesson file says the bug is fixed.

**When you fix a bug, grep for the component's SIBLINGS before you close it.** If the same contract is
written down in more than one place, the fix is not the fix — the extraction is.

### THE RULE THAT IS BEING KEPT BY LUCK IS THE RULE THAT IS ABOUT TO BREAK

Movers and Watchlist carried `transition-colors` on their row hover for six phases. P2 says a money
visual never moves, and the ancestor walk enforces it by walking up from every `[data-p2]` node.

The two rows were fine — because **their delta chips were unmarked**. The app's strictest guard had
never once looked at the two busiest money surfaces on the Desk. Marking the chips (Q-G4-1) failed the
build on both rows immediately.

**A guard only guards what it is pointed at.** Ask of every rule you rely on: *what is in scope, and
what is quietly outside it?* The answer for P2 was "the Desk's two most important rows".

### A MATCHER OVER NARRATED PROSE MUST MATCH THE WORDS THE NARRATOR WRITES

`TermProse` landed on the Desk's brief. Every unit test passed. The rendered brief carried **zero**
glossary doorways.

The glossary holds the display title **"RVOL"**. The pipeline's narrator writes **"relative volume"**,
because that is what a person would say. The matcher, knowing only the title, walked straight past it.

The unit tests all passed because they were written against the glossary's own vocabulary — they
tested the matcher against the words *we* use, never the words *the product* produces. **Only the
screenshot showed it.** (PD4 said this in different words and it is now a house law: when your tests go
green, that is not the same as your surface being right. Look at the pictures.)

### `reuseExistingServer` WILL SERVE YOU A LIE FOR AN HOUR

Playwright's `reuseExistingServer` is `true` locally. A server started by an earlier run **stays bound
to port 3210** and keeps serving whichever build it started with — while `npm run build` rewrites
`.next` underneath it.

PD5 spent an hour chasing a "PD5 regression" in `settings.spec.ts` on the strength of an A/B against
`pd-4` that showed pd-4 passing and PD5 failing. **Both measurements were taken against a stale
server.** With the port actually cleared, `pd-4` fails the same test at the same rate: it is a
local-only flake (ISR revalidation timing under `next start`), and never was a regression.

**`lsof -ti:3210 | xargs kill -9` before ANY local A/B against another commit.** Otherwise the answer
you get is about the server, not about the code. (This is the same law as PD4's "is this the APP or is
this the CAMERA?", wearing a different hat: *is this the code, or is this the harness?*)

### A SELECTOR THAT RESOLVES IS NOT A SELECTOR THAT SEES

`a[href^="/ticker/"]` found the chip. The click then waited **thirty seconds** for it to become
visible, and died pointing at a chip that was on screen in front of me.

**DataTable renders BOTH layouts into the DOM** — the desktop table and the phone's card list — and
hides one with CSS. `.first()` picks whichever copy comes first in the document, which on a desktop is
the **hidden** one. It **passed on the phone**, where the visible copy happens to be first — a green
tick on one leg and a thirty-second death on the other, from one selector.

**`:visible` is load-bearing on any DataTable cell.** And a spec that passes on one project and dies on
another is telling you something about the DOM, not about the viewport.

## PD6 — the voice, the remaining rooms (2026-07-14)

2026-07-14 · PD5 hunted the delta chip's siblings and found FOUR; there were SIX, and the two it missed were rendering on front-facing rooms every night → the sweep was a HAND GREP for the component's name, and one of the survivors was a private `function DeltaChip` inside `DataTable.tsx` that **shadowed the kit component's own name** — a grep found it and read it as the kit — while the other was an inline `<span>` with a template-literal className and no component name to grep for at all → both replaced by the kit; the window became a TYPE (below) → **guard added: drift rule 28** — the direction WASH (`bg-up-wash`/`bg-down-wash`) has exactly two doors, DeltaChip and OutcomeChip. It is pointed at the one thing all six copies had in common and could not have avoided: a direction-coloured BACKGROUND is what makes a chip a chip, so a seventh must paint one to exist and fails the build the moment it does. **A hand grep is not a guard. If a rule matters, point a rule at it.**

2026-07-14 · The app's one table showed a delta with NO WINDOW on every phone, and had since the table was written → the window lived in the COLUMN HEADER, which is honest on a desktop (a `<th>` labels every cell beneath it) and **completely absent on a phone**: DataTable renders a card list, and a priority-1 cell is drawn with no header beside it (only priority-2 cells print theirs). Every signed-percent column in the app is priority 1 or 2. So `dist_52w_high` — "12.4% below the 52-week HIGH" — rendered as a bare red `▼ −12.4%`, which any reader takes for a bad day → `lib/table.ts`'s `Column<Row>` is a discriminated union now: `kind: "signedPercent"` **does not compile** without a `window` → guard added: the compiler. It cannot be forgotten and it cannot be pointed at the wrong file, and it named the three remaining call sites the instant it was written. **A rule that lives in a comment is a rule any caller may ignore. Ruling C2 was a required prop on DeltaChip and the table simply walked around it by keeping a private chip that took no window.**

2026-07-14 · A 21px touch target had been LIVE on the news story page since PD5, and the phone sweep — which exists precisely to catch that — passed it every single night → the sweep visits exactly ONE news story, `nc-fed-hold`, and that cluster has **zero** catalyst links in the seeded world, so `AffectedTable` rendered no rows, so there were no doors to measure, so the room reported clean. **The rule was being kept by the shape of a fixture.** PD6 put doors on three more rooms and the sweep failed all three instantly — which is how anyone found out → the 44px hit area moved into `TickerChip` itself, so the fix lands everywhere at once, and **the contract is pinned in the component's own unit test** rather than in a browser sweep that needs a fixture's cooperation to see it → guard added: `TickerChip.test.tsx`. **A guard only guards what it is pointed at — and "pointed at" includes the data it is pointed at.** (Q-PD6-2: the sweep should visit a story that HAS an affected table. PD8.)

2026-07-14 · The touch fix was half right, and **only a SCREENSHOT said so** → `min-h-11` gave the door a true 44px hit area, and that 44px then ADDED to the `<td>`'s 12px of vertical padding, so every row in every table in the app went from 45px to **69px** — half again as tall, to make room for a target that is invisible. **Every guard passed**: the unit tests, the touch sweep (44px is all it asks for, and it got 44px), the bundle budgets, the drift rules, axe. It was caught by putting the candidate baseline next to the committed one and looking at the two of them → `-my-3` pulls the anchor's 44px border box back INTO the padding the cell had already reserved instead of stacking on top of it; `boundingBox()`, which is what the sweep and the browser's own hit-testing read, still returns a true 44px. Measured after: row 46.3px, door 44px → guard added: `TickerChip.test.tsx` pins `-my-3` with the story in the comment. **A target that passes its guard and wrecks the layout is not a fix. The guard asked "is it 44px?" and never asked "what did it cost?"**

2026-07-14 · A chip is WIDER THAN THE WORD IT REPLACES, and the settings watchlist's column had been sized for the word → `w-24` (96px) held "AAPL" as bare text (~34px) beside a FOCUS tag comfortably. As a bordered chip, "AAPL" is ~48px, and the pair came to ~101px — so the tag wrapped onto its own line and pushed the company name down. Nothing failed; no guard in this repo has an opinion about it; it simply looked wrong, because it WAS wrong → the column is `w-32` → **PD4's law, in a new room: making a chip FIT is the LAYOUT's job, not the chip's.** When you replace text with a chip, go and look at every container that was measured for the text.

2026-07-14 · The pixel oracle is **BLIND to a large-area, low-contrast change**, and this is not the tolerance being generous — it is the instrument not seeing → `playwright.config.ts` sets `maxDiffPixels: 600` and leaves `threshold` at its default (0.2, a YIQ colour distance). A hover wash is a big region of a *slightly* different colour: every one of those pixels falls UNDER the per-pixel threshold, so none of them counts as "differing" and the 600-pixel budget is **never even consulted**. PD5 recorded `scans-preset` as changing by ~56,000 pixels while passing and assumed the tolerance had absorbed it; it had not. The committed `scans-preset` baselines dated from PD2, *before* PD4 parked the mouse at (0,0), and were photographs of a row sitting in a `hover:bg-accent-soft` wash — **confirmed by eye in PD6, with the crop side by side** → re-shot here, which closes Q-PD5-2 → **Q-PD6-1 for PD10: arming `threshold` is a change to the instrument all 83 baselines are measured by, and belongs in a phase that can re-photograph every one of them and look.**

2026-07-14 · PD6's endgame ran FOUR serial red rehearsals of the full browser oracle where one or two would have done, and the waste was ~50 minutes of round-trip WAITING → each time I diffed the candidate baselines against the committed ones and actually LOOKED, I found a real bug — the 44px door inflating every table row from 45px to 69px; then a chip wrapping in a column that had been sized for the WORD it replaced; then THAT fix pushing /settings into a 16px sideways scroll at 360px — and each time I fixed the one thing and immediately dispatched again. Every one of those was a genuine defect that no test caught, so the looking was right and is not the lesson. **The SERIALIZING was.** → **STANDING RULE (user directive, 2026-07-14, permanent): BATCH VISUAL FIXES INTO A SINGLE RE-SHOOT.** After a red rehearsal, download the failure artifacts AND the candidates for EVERY red leg, open every image, list every diff, fix them ALL, then dispatch ONCE. Never one dispatch per fix → guard: written into CLAUDE.md's VRT block, where the next session will read it before it touches a baseline. **The repo is public now and the minutes are free, and the rule still holds — what it saves was never the money.** A fix-then-verify loop with a nine-minute verify step is not a loop you may run three times in a row.

2026-07-14 · GitHub Actions stopped starting jobs MID-ENDGAME, two commits from a tag, and the failure looked like nothing I had ever seen: jobs "failed" in 2–3 seconds with **zero steps executed** and empty logs → not a flake and not the code. The repo was PRIVATE, and on a private repo every Actions minute is metered against a 2,000-minute monthly allowance. The real message was not in the logs at all — it was in the run's **check-run annotation**: *"The job was not started because recent account payments have failed or your spending limit needs to be increased."* → Bishan made the repo **public** the same hour (Actions is unmetered on public repos), after a clean scan of the full history. → **THE DIAGNOSTIC LESSON, which is the reusable one: a job with ZERO STEPS did not run. Do not read its logs — it has none. Read `gh api repos/<r>/check-runs/<job-id>/annotations`,** which is where GitHub puts the reason a runner was never allocated. I spent several minutes hunting a code cause for a billing event.

**The measured cost, because "CI is expensive" is not a fact until it has a number** (July 1–14, ~400 runs): ~300 **branch-push** runs, ~1,135 wall-minutes — **roughly two-thirds of the entire spend**, and nobody had ever looked at that number. 65 oracle **dispatches**, 456 wall-minutes. A GREEN rehearsal bills ~25 min (desktop 7.9 + phone 8.5 + wide 3.4 + mbp16 3.4, per-job round-up); a PIXEL-RED one bills ~30–35, because the auto-mint re-photographs all 83 shots. Tag runs DUPLICATED their rehearsals. Nightlies are ~14 min/night. **Estimated total ~2,900–3,200 billable minutes against a 2,000-minute allowance.** (An estimate from wall-times × parallel jobs — the billing API needs a token scope we do not have.) **Today alone ran three phase endgames — pd-4, pd-5 and pd-6 — for nine oracle dispatches and ~10 push runs.** The wall was not one greedy phase; it was two weeks of a gate that nobody had ever costed. Going public retires the meter entirely, so this is now history rather than a constraint — but the shape of it is worth keeping: **the expensive thing was the boring thing (every push, every branch), not the dramatic thing (the oracle).**

## PD7 (2026-07-14) — news depth: the pipeline

### The whole suite is green while production publishes garbage — and only reading the output finds it.

**What happened, twice, in one phase.** PD7 owes a live `news`-mode dispatch. Run 1 published ZERO
notes to production; the schema had outgrown `_MAX_TOKENS` (v2 is bigger, AND Sonnet 5 thinks by
default and thinking shares the `max_tokens` cap — a default that changed with the MODEL, not our
code). Run 2 published seven "context" sections that were each a sha1 HASH restated as a sentence —
"carried by 1 outlet tonight (cls:798fa63d…:corroboration)". Every guard passed both: the schema
validated, the verification gate cleared every figure, the E4 lexicon passed, and the night's own log
reported "14 notes written, 7 context sections published". Nothing was red. It was unshippable.

**Why the tests could not catch it.** The fake clients in the suite have no token cap to overflow and
no reader to offend — so a truncation and a hash-in-prose are both invisible to them. The schema
answers "is this well-formed?"; the gate answers "is this number true?"; NEITHER answers "would a
human want to read this?", and that was the only question that mattered.

**The guard.** There is no unit-test guard for "the output is readable" — the guard is the RITUAL:
run the real dispatch, query the rows it wrote, and READ THE PROSE. PD7's brief said so in advance
("Print them. Read them. A schema that validates is not a schema that says something true.") and it
was exactly right. The two bugs are each now pinned by a constant test and a deterministic guard
(the token cap; the id-in-prose lexicon rule), but the pins were written AFTER looking found the bug,
not before. **A pipeline phase's "pictures" are its real dispatch. Do not tag one you have not read.**

### The rendered VALUE of a stat is the gate's source of truth, so the WINDOW is a number too.

**What happened.** The verification gate builds its allowed set by parsing each stat's rendered
VALUE. So the numbers a value contains are exactly the numbers the narrator is licensed to write —
and a "52-week range" stat whose value did not contain "52" would make the gate flag an honest
sentence for the very window it was asked to describe ("52-week" scans as the number 52). The fix is
not a special case in the gate; it is that each depth stat states its window in the words the
narrator will use ("71.4% of the way up its 52-week range").

**The guard.** A test runs the real gate over the fixture's own context prose and asserts zero flags
— which also caught the fixture claiming the gate had cleared a "2" (an ordinal, which verify.py has
never read as a number). A stat that describes a window must say the window out loud.

### An absence must report the TRUE reason, per section — "silent" and "out of budget" are different sentences.

**What happened.** The v2 `sections` map exists to tell PD8's story page WHY a section is absent (the
gate held it / the narrator had nothing / it was out of budget) — because those absences print
identically and only the record can distinguish them. The first live run exposed a sloppy one: the
fully-silent early return marked EVERY section `silent`, including `context` on clusters that were
never in the top-8 budget, so 13 sections reported `silent` on a page where only 8 are ever deep. A
cluster nobody asked for a context is not a cluster whose narrator was stumped.

**The guard.** Two tests pin it: an out-of-budget cluster's silent note reports `out_of_budget`, a
deep cluster's silent note reports `silent`. Found by reading the arithmetic in the production
record, not by a red test — 13 > 8 was the tell.

### A writer with three new columns and no round-trip test is betting on luck.

**What happened.** `publish_news` — the one writer for the whole front page — had NO database test of
its column write. The only test touching it asserted it REFUSES a bad date: a guard on the door, not
a check of what goes through it. Its INSERT column list, ON CONFLICT clause and per-payload JSON
adaptation were covered by nothing. Fine while the shape was stable; PD7 added three columns to it.

**The guard.** `test_publish_news_depth.py` writes a row and reads it back — the v2 shape, the v1
shape (no new fields, the night the narrator never ran), and a re-run that must UPDATE in place. It
failed loudly the first time because the local test DB predated the migration, which is exactly the
proof that it tests something real.

2026-07-14 · PD7 published a sha1 hash into a sentence a human was meant to read, and every guard in the repo passed it — so PD8's surfaces, which RENDER that prose, could have shipped it to screen → the guards all measured STRUCTURE (a schema that validates, a tolerance gate that clears, a VRT that is pixel-stable, an axe pass) and none measured MEANING (is the sentence English?) → PD8 added the missing guard at the layer that renders: `news.spec.ts` reads the story's context prose in the browser and FAILS if it ever contains a registry id (`tkr:`/`cls:`/`cal:`) or a bare hash → guard added, and it is negative-controllable (seed a stat_id into the context and it reds). The generalisation, now proven at two layers: **a green VRT and a passing axe tell you the pixels are stable; they do not tell you the sentence is true. When a surface renders NARRATED prose, one assertion must read the words, not just measure the box around them.** The corollary that made PD8 safe: I opened the seeded story and ticker in a browser and READ them before tagging — the context read as English, the footer named the real models, the thin history said "22 sessions" not "52-week", and the non-served ticker showed a record with no chart. The picture was right because someone looked at it.

2026-07-14 · A single VRT shot changed on two pages PD8 never opened — `login-desktop` and `track-record-dark-desktop` → the reflexive read is "a shared component leaked"; the correct read is the CAMERA → the tell is the SHAPE of the change: `login-desktop` moved 176px and `track-record-dark-desktop` moved 0px above tolerance, and BOTH were a single theme × single project. A real shared-component change moves EVERY shot of the surface (light and dark, every viewport); a change confined to one theme-and-project is rasterisation jitter or gradient dither, not the app → left both committed as-is (PD7 saw the same two shots move and made the same call) → the rule, now mechanical: **before believing a diff on a page you did not touch, ask whether it hit ALL shots of that surface or just one. All shots ⇒ the app (find the leak). One shot ⇒ the camera (leave it — re-baselining a picture you cannot vouch for trades one unexplained image for another).** The pixel count is the second witness: decode both, count differing pixels above a threshold, and a camera diff is small or zero while a content change is thousands.

2026-07-14 · A one-word copy change — the rail's exit label "Open full view →" → "Full view: AAPL →" (10.2) — broke a seeded e2e that typecheck and lint could not see, and I only caught it at the SECOND rehearsal → I treated the label as trivial (ran typecheck + lint, not the seeded browser specs), and the label now put the SYMBOL into the link, so `drill.spec`'s `rail.getByText("AAPL")` — a substring locator that had resolved to one element (the heading) — suddenly matched TWO (the heading AND the exit link), a strict-mode violation. It passed the first rehearsal only because that rehearsal predated the label; the second one, on the SHA that carried it, failed on desktop and phone → the assertion targets the heading specifically (`getByRole("heading", { name: "AAPL" })`), and it was verified against the seeded database on both projects before pushing → guard, and it is a rule about scope not about rails: **a copy change is a code change, and a copy change that drops a SYMBOL or a WORD into a control's accessible name can make a loose `getByText` ambiguous three specs away. Run the seeded e2e that exercises that surface — typecheck and lint are blind to a locator that now matches twice.** The same class as PD5's `:visible` DataTable trap: a locator that resolves to more than it should is a failure the compiler cannot see.

2026-07-15 · I reasoned my way to the WRONG intercepting-route matcher and the build corrected it in five seconds → the plan said `(.)news/[cluster]`, but I over-thought it: the framework's own modal example uses `@modal/(..)photos`, so by analogy I wrote `(..)news`, reasoning that `@modal` and `news` are transparent-container siblings one level apart. `next build` refused it outright — "Cannot use (..) marker at the root level, use (.) instead" — because a route GROUP (`(desk)`) is transparent, so `@modal` sits at the ROOT level and `news`/`ticker` are its SAME-level siblings, which is exactly what `(.)` matches → renamed the two intercept dirs to `(.)`; the build passed and the interception fired on soft nav (verified by the overlay e2e opening the sheet) → the lesson is the census's standing warning made concrete, and it is cheaper than it looks: **do not derive a framework routing detail from an analogy — write it, run `next build`, and let the compiler tell you. The matcher for an intercepting route is the single most error-prone character in the whole feature, and the build knows the answer instantly while memory guesses.** The plan had it right; my cleverness did not.

2026-07-15 · An intercepting-route modal restored focus for the ✕/Esc/scrim but NOT for the hardware back button, and the fix had to move → Radix Dialog restores focus to the launcher on its OWN close (open→false). But the detail sheet never closes that way: every dismissal is a `router.back()` that UNMOUNTS the route, and Next moves focus to the document on a navigation. So Radix's restore never runs, and my first fix — re-focusing the launcher inside the `dismiss()` handler — covered the four dismissals that route through `dismiss()` but missed `page.goBack()`, which pops history without ever calling it → the restore moved to an UNMOUNT-cleanup effect (`useEffect(() => () => launcher.focus())`), which runs however the overlay unmounts, and the launcher is captured in a `useState` INITIALIZER so it is read before Radix's autofocus steals `document.activeElement` → guard: `e2e/overlay.spec.ts` parameterises all FIVE dismissals over the same focus-return assertion, so a future change that regresses any one path reds. The general shape: **when a component's job is to run on close, tie it to UNMOUNT, not to the handler for one of the ways it closes — the other ways will not call your handler.**

2026-07-15 · The overscroll-dismissal e2e failed, and the handler was fine — the TEST had scrolled the sheet away from the top first → the parameterised dismissal test scrolls the sheet's inner container to 200px (to prove that inner scroll does not move the feed behind it), THEN ran each dismissal — but overscroll-past-top only means "dismiss" when the pull BEGINS at scrollTop 0, so with the container at 200 the handler correctly refused, and the URL stayed on the story → the overscroll helper resets the container to the top first (`el.scrollTo(0,0)`), which is exactly what a reader does — scroll back up, then keep pulling → the synthetic `TouchEvent` dispatch was working the whole time. The lesson: **before blaming the feature for a red gesture test, check that the test put the feature in the state the gesture APPLIES to.** A gesture with a precondition (at the top) tested from the wrong state is a false red, and the pure decision behind it (`lib/overlay-dismiss.pullDismisses`) was green in a unit test throughout, which is what said the handler was not the problem.

2026-07-15 · A code comment asserted "a ≥44px touch zone" over a grabber that measured 24px, and it had been in the tree since PD9 → the comment stated a NUMBER nobody had measured — the grabber's container is `h-6 pt-2`, which is a 24px box, not 44px — so the comment was a claim wearing the authority of a measurement, and a future reader trusting it would have thought the target-size rule was satisfied there when it was not (it is satisfied a different way: the grabber is `aria-hidden`, so it is not a target at all, and the real pull surface is the 410×746 scroll container) → PD10 opened the sheet on a live phone build and MEASURED — ✕ 44×44, grabber 410×24, pull-surface 410×746 — then rewrote the comment to state the measured truth and left the pill decorative on purpose → guard: `e2e/overlay.spec.ts` now asserts the ✕ ≥44px, proves the grabber `aria-hidden`, and checks the pull surface ≥44px, so the sheet's touch story is measured every run instead of asserted in a comment. The general shape: **a comment that states a measurement is a claim that must be measured — write the number you observed, never the number you expected, or the comment becomes a green light wired to nothing.** The same disease as a test that cannot fail: an unverified "≥44px" reassured exactly like a passing gate that measured the wrong thing.

## Clarity & Cadence (CC1, 2026-07-15)

2026-07-15 · The Daily Brief held roughly every other night in production, and 578 green pipeline tests never saw it → claude-sonnet-5 THINKS before it writes, and that reasoning is spent against `max_tokens`; at 4096 the thinking alone consumed the whole budget, so the synthesis call stopped with `stop_reason=max_tokens`, a lone `thinking` block, and ZERO text — and a bare `except` in `_parse_draft` collapsed that empty response into a bland "synthesis failed validation" with no trace of the cause. The suite could not possibly catch it: `test_briefing_synthesize.py` drives a SCRIPTED FAKE client that returns text on demand, so a real-model behaviour (the thinking-token budget) is invisible to every unit test by construction → two fixes and a guard: (1) raise `_MAX_TOKENS` to 21000 — but NOT higher, because a non-streaming call refuses any `max_tokens` that could run past 10 minutes (`3600*max_tokens/128000 > 600` ⇒ >21333; sonnet-5 is not in the SDK's per-model non-streaming table, so 32000 crashed Job B with "Streaming is required"); (2) make the failure SAY WHY — `_parse_draft` returns the reason and `synthesize` prints it with `stop_reason` and the content-block types, which is the single line that named the whole bug. → guard: a unit test pins `_MAX_TOKENS` in the band (>4096 starvation floor, ≤21333 streaming ceiling). **Three lessons stacked, and the third is the biggest.** (a) A model parameter tuned for one model's behaviour rots when the model's behaviour changes — extended thinking made a budget that was always "fine" suddenly too small. (b) A bare `except` that turns many distinct failures into one message is a blindfold; the reason must survive to the log or the record, because a held night is a night someone will want to audit. (c) **A deterministic test suite that fakes the model is BLIND to everything the real model does** — the scripted client can only replay what you already imagined, so it certifies the parsing and the retry logic and says nothing about token budgets, refusals, thinking, or truncation. This is the `pipeline-phase-verification` rule proven a second time: the suite going green is not the check; the REAL dispatch and the published prose (or the failure log) is the only check. Found exactly that way — by dispatching nightly-b for real, reading the held row, and dumping the message shape.

2026-07-15 · The plan (Appendix C) predicted a "ticker sheet" VRT delta for CC1 that never appeared, and chasing it would have been a wide seed swing at the end of a scoped phase → the ticker-record slug leak (`patternLabel` echoing a scan-preset key like "gap-3plus") is REAL in production but the SEEDED world does not reproduce it: the seed's three `signal_log` rows are all RESOLVED (so they render no active-signal label at all) and they carry DETECTOR keys (`golden-cross`), which mapped correctly the whole time — while production's leak is on ACTIVE, unresolved, SCAN-fired signals whose `pattern_key` is a preset key → I fixed `patternLabel` to resolve both keyspaces and proved it with a unit test that observes the exact defect at the deciding function, and handed the RENDERED proof to CC4 (which already re-shoots the forecasts/track-record surfaces an active seed signal would touch) → guard: the unit test pins `patternLabel("gap-3plus") == "Gap of 3% or more"` and every preset key. The lesson: **a plan's predicted VRT delta is a hypothesis about the SEEDED world, and the seed can be too honest to carry the bug** — when the fixture never had the defect, adding it just to satisfy a prediction is a seed swing, and the disciplined move is to prove the fix where it is decided (the function) and defer the picture to a phase whose re-shoot already covers that surface.

2026-07-15 · A first draft of comment_stats.py (LC1) undercounted check-drift.mjs by ~40% (282 where the audit found 478), and after I added regex handling it over-corrected and undercounted every .tsx file (app/components fell from 4,313 to 3,758) → a naive string-state comment scanner is poisoned in two opposite ways: a quote INSIDE a JS regex literal (`/["']/`) flips it into false "string" state and swallows every comment line after it — and a drift checker is nothing but regex literals — while treating `<` and `>` as regex-open positions reads every JSX closing tag `</div>` as the start of a regex and swallows comments the same way → the C-family scanner now tracks regex literals explicitly (a `/` opens a regex only in expression position) and excludes `<`/`>` from the regex-open set, since no valid code starts a regex after them → guard: the three anchor cells (app/components 4,318, app/e2e 2,193, app/prisma 1,240) reproduce Part 2's hand-audit to within two lines, and the Python files match it exactly; any future edit to the scanner is checked against those anchors. Lesson within the lesson: comment-density tooling that "looks right" on plain files is silently wrong on the regex-heavy and JSX-heavy files — which are exactly the files worth measuring.

2026-07-15 · LC2's plan said `signIn` was "byte-identical in 15 spec files" and the session-cookie mint "exists three times" identically — and both counts were wrong in a way that, taken on faith, would have shipped a silent behavior change → measured, `signIn` was byte-identical in only 12 (auth omits the closing `toHaveURL`, nav differs by a cosmetic type annotation, offline carries an extra `serviceWorker.ready` wait that is load-bearing for its offline-cache tests); and the "three identical" cookie mints were two TTLs — check-live signs a ONE-HOUR cookie where check-nav and lighthouse sign thirty days. A blind find-and-replace of "all 15" would have dropped offline's SW wait; of "all 3" would have handed check-live a 30-day cookie → the extraction took the TRUE common shape (the 12-file signIn, whose `toHaveURL("/")` also serves as the redirect-wait, so it could not be dropped), preserved the three variants, and gave `mintSessionCookie` a `ttlSeconds` parameter → guard: preservation was proven, not asserted — check:routes and check:bundles print byte-identical stdout before/after against the same build, and the extracted mint yields a byte-identical token to the old inline one (both TTLs, clock pinned). The lesson, a cousin of the fabricated-fixture and predicted-VRT-delta ones: **"identical N copies" in a plan is a hypothesis to verify by reading all N, not a fact to extract on** — a consolidation is only "pure" once you have confirmed the things you are merging are actually the same, and the ones that differ are exactly where a pure extraction turns into a silent regression.

2026-07-15 · A comment-only prover built on a token SCANNER rejected a comment-only edit — because the scanner cannot read real JavaScript → LC3's whole risk is "did a 'comment' edit quietly change code?", so the prover compares the non-comment token stream old-vs-new and rejects any difference. The first version used the raw TypeScript SCANNER (`createScanner`, skipTrivia). It failed on the FIRST real file, morning.ts, reporting a token divergence on a genuinely comment-only edit: the scanner had swallowed a deleted comment into a mis-scanned `NoSubstitutionTemplateLiteral` that ran across dozens of lines. The cause is fundamental — a raw scanner cannot continue a template `${}` substitution or tell a regex from a divide without the parser's brace-and-position context, so `/["']/` in check-drift and `` `${a}: x` `` in morning both flip it into a state that eats every comment after them. → The fix is the PARSER (`ts.createSourceFile`) walked to its leaf tokens, whose `getText()` excludes comment trivia — with the JSDoc node range skipped, because the compiler parses `/** */` into the AST (unlike `//` and `/* */`, which stay trivia), so a compressed doc comment would otherwise read as a changed token. → **The lesson is the one this build keeps relearning in new clothes: a checker is only as honest as the thing it measures, and a lexer that "looks right" on plain files is silently wrong on exactly the regex-heavy and template-heavy files worth checking.** The good outcome is that the prover caught its OWN inadequacy on the first file rather than passing a real code change — because it was pointed at a genuine invariant (byte-identical non-comment tokens) and self-tested on 30 adversarial cases including the `a/**/b`→`ab` token-merge, so a false GREEN was never possible, only a false red that then got fixed. guard: the prover's self-test battery is kept in the scratchpad's shape and its assertions (template-with-substitution, regex-with-quotes, JSDoc-compressed, deleted-pragma, deleted-clock-date) are the negative controls any future edit to it must still pass.

2026-07-15 · The plan promised "half"; the honest result was 30%, and the gap is the sacred list doing its job → LEAN-CODEBASE's estimate for LC3 was ≈4–6k lines removed. The real number is 1766 (30% of the 25 files), and the difference is not a shortfall — it is that the hottest files are disproportionately made of content Part 3 forbids touching: check-drift.mjs is largely argued skip-list entries (only −55 of 331), news.mjs carries per-cluster significance ARITHMETIC a reader verifies by hand, brand-assets keeps its BRAND_FIELD provenance, and a fixture's test-name strings are code, not comments. → **A phase's headline number is a prediction, and a prediction that collides with a constitutional constraint should yield to the constraint, out loud, rather than being met by stripping the thing the constraint protects.** The report says 30% and says why; the alternative — pushing to 50% by compressing the argued register — would have hit the number and lost the exact whys the next session relies on, which is the failure this whole standard exists to prevent. guard: the evidence file states the honest number, the per-file table shows where the sacred content sat, and QUESTIONS-FOR-BISHANT names it so the 30% is not read as a miss.

## CC2 — time, told properly (2026-07-15)

2026-07-15 · Adding a weekday to formatUtcDate would have silently doubled the weekday on the pipeline strip and three other surfaces ("Fri Fri, Jul 10") → those sites concatenated a separate `formatUtcWeekday(d)` prefix with `formatUtcDate(d)`, a pattern invisible until the date's own shape changed → dropped the prefix at all four sites (PipelineStrip sessionLabel, StoryPageBody watch rows, TickerBlocks, TickerPageBody); two were caught by the PipelineStrip unit tests that pinned "Fri Jul 10", a grep for the `formatUtcWeekday`+`formatUtcDate` adjacency caught the rest → guard: when a shared formatter gains a field, grep for every site that composes it with a sibling formatter that already supplies that field. A rendered string is assembled from parts, and enriching one part can duplicate another.

2026-07-15 · The committed vrt-diff.mjs threw ERR_MODULE_NOT_FOUND at the CC2 re-shoot because `pixelmatch` is no longer in app/node_modules → LC1 relied on it resolving transitively through Playwright, and a dependency tree can drop a transitive package silently (only pngjs survived); the LC1 "verified: import succeeds" was true then and false now → worked around it with a throwaway pngjs-only pixel counter, and the DIFF IMAGES (triptychs) were the real proof regardless → guard: a dev tool that leans on a transitive dependency is one `npm install` away from breaking with no test to catch it (nothing runs vrt-diff in CI). Declare what you import, or lean only on what you can see. Escalated to Q-LC1-1 with the proof.

2026-07-15 · A shared chrome element changed EVERY room's picture, but the failure list named only two → CC3 added a theme toggle to the top bar of every room. Rehearsal #1 redded ONLY styleguide and desk (4-8 shots per leg); everything else PASSED. But the toggle is ~547px on desktop and ~232px on the phone — under the 600px `maxDiffPixels` tolerance — so ~30 room baselines CHANGED (gained the toggle) without FAILING, and committing only the failures would have locked a toggle-less top bar into the oracle forever, on pages nobody re-shot. This is the PD5 law ("a baseline that is TOLERATED is still a baseline that is WRONG") at chrome scale: when a SHARED element moves, the diff is small per-page and lands under tolerance on most pages, so the failure list is the tip of the change, not the whole of it → diffed EVERY candidate against its committed twin (pngjs counter — vrt-diff.mjs still broken), found all ~87 that moved, cropped each category's top bar (pngjs, top 64px) and confirmed the toggle/pill/gear, re-shot all 87, left login (129px AA, no top-bar change) → guard: the rule already exists (CLAUDE.md VRT block, "diff every candidate, not just the failures"); this is its loudest instance yet, and the uniformity of the per-category diffs (every desk room 547px, every academy 900px, every phone room 232px — zero outliers) is what proved it was one shared cause and not many small bugs. When a change touches shared chrome, EXPECT the failure list to undercount, and diff everything.

2026-07-15 · A reader-facing copy change would have redded the one guard that asks whether PRODUCTION is right → CC3 slimmed the pipeline strip's fresh line from "… · next: Fri 6:37 PM ET" to "… · next edition Fri ~6:37 PM ET". `checkNextEdition` in scripts/live-truth.mjs — the check:live assertion that reads the strip's future promise and measures it against the edition date — greps the literal "next: {day}". So the strip copy change would have failed check:live the moment CC3 deployed, and check:live is the ONLY gate that tests the deployed product rather than the code, so nothing local would have caught it: unit tests, e2e, drift and the oracle are all green on a strip that the production-truth guard can no longer parse → found it by READING scripts/live-truth.mjs before running check:live at the gate (looking specifically for anything that greps strip or masthead text), not by watching check:live fail; updated the regex, the desk-healthy.txt fixture phrase, and the test's replace strings → guard: the general rule is that lib/copy.ts strings the STRIP or MASTHEAD renders are also PARSED by scripts/live-truth.mjs (the masthead's long date, the strip's "next edition" promise, the calendar dates, the press time). Any phase that touches strip/masthead copy must grep live-truth.mjs for the old phrase in the SAME commit. The oracle cannot catch this — check:live is local-only by nature, and its unit test uses a frozen fixture, so the fixture has to be updated by hand too.


2026-07-15 · The plan's grammar asked for `text-faint` on a matching as-of stamp, and the same repo forbids it → 4.3's meta row says a module's as-of renders `text-faint` when it equals the edition's stamp (recede) and `ink-2` when it differs (information). But `text-faint` is a 2.2:1 grey, and TWO guards born from the F7 axe lesson forbid it on information: drift rule 18 greps for any bare `text-faint`, and the axe sweep fails a 2.2:1 timestamp. A timestamp is provenance = information. → Rendered the MATCH case in `text-muted` (recedes, clears AA) and the DIFFER case in `text-ink-2` (comes forward). The hierarchy the plan wanted — a differing stamp outranks a matching one — is preserved exactly; only the absolute faintness is traded, to keep the timestamp legible. → guard: the two guards already existed and would have caught it at the gate; the SectionMasthead unit test now pins both renderings AND asserts the stamp is never faint. The general shape, seen before in this build: when the plan's LOOK collides with an a11y guard, the guard wins on the a11y axis and the look is realized as close as the floor allows — "faint" became "muted", which is the quietest legible grey.

2026-07-15 · A "safety net" that fires unevenly is a defect, not a net → StatFigure's `row` layout (the phone tape) carried `flex-wrap` on the OUTER row, documented (PD4) as a safety net: "a long label beside a long figure wraps to a second line, it never spills." It DID fire — but only on the widest of the three tape echoes (Nasdaq), dropping that figure below its label while the shorter two stayed inline. One grammar, two renderings — the exact D10 complaint. The net was real; its unevenness was the bug. → Removed the outer wrap (the figure never drops below the label) and left the safety net where it belongs: the value block below keeps its own `flex-wrap`, so if a value and its chip truly cannot share a line, the CHIP drops under the value — consistent on every row, and still honest (the chip wraps, nothing spills). A PD4 test asserted `flex-wrap` on the row root; it now asserts NO wrap on the root and `flex-wrap` on the value block. → guard: the updated test pins the corrected behavior. The lesson: a wrap that triggers on content WIDTH will fire on your widest item and not your others, so "it wraps rather than spills" is only a uniform grammar if every item wraps — otherwise it is a grammar that changes shape with the data.

2026-07-15 · The "already right" reference and the literal token disagreed, and the reference won → 4.3's grammar table lists the room dek as "text-prose" while ALSO calling Scans/Track "already right — adopt everywhere". Those two rooms use `font-prose text-base` (15px), not the `text-prose` TOKEN (17.5px — the app's reading-prose size). Taking "text-prose" literally would have made the two reference rooms BIGGER, contradicting "already right". → Adopted the reference rooms' actual shape everywhere (Settings' `font-ui text-sm text-muted` dek → `font-prose text-base text-ink-2`); read "text-prose" in the table as the prose FACE, which font-prose supplies. → guard: none needed, but the general point: when a spec names a token AND names an exemplar, and they conflict, the exemplar is the ground truth — a token name in prose is a shorthand, an existing rendered surface is a measurement.

2026-07-15 · A tag run failed TWICE on a "local-only" flake that was neither local-only nor harmless (CC4) → the settings-watchlist e2e adds a row and cleans up through a FLAKY UI remove. When that remove flaked in CI, the row PERSISTED in the leg's shared database — and because the desktop/phone legs run settings.spec BEFORE vrt.spec against that ONE database (workers:1), the settings + desk VRT shots (both render the watchlist) then photographed a fourth row and failed on a RESIZE (1366×1693 → 1366×1762). So one flaky mutating test redded FIVE tests, on a SHA whose rehearsal had passed clean — the rehearsal passed only because that run happened not to flake. The wide/mbp16 legs were immune, and that was the tell: their testMatch is vrt/hardening/grid only, so they never ran the mutating spec. Two things were wrong and both are fixed: (1) the cleanup was not GUARANTEED — it now deletes the test's row directly in the database in an afterEach (the thin-night.ts PrismaClient pattern), scoped to this project's exact reason, so the VRT is clean whether or not the UI remove landed AND the CI retry starts clean (the original retries failed because they inherited the dirty DB); (2) the reflection assertions used the 5s default while each click fires a server action that re-renders the WHOLE of /settings — they wait 20s now with the reason written down (the Desk journal test's precedent), and CC4 had made the room heavier. → guard: verified 2/2 consecutive local runs green with 0 leftover rows, then a four-leg rehearsal on the fixed SHA. The general shape, and it is a species this build keeps meeting: a test that MUTATES shared state read by a later test is a poisoning hazard, and "it cleans up after itself" is a promise only as strong as the cleanup's weakest path — here a flaky UI action. A shared-DB gate needs its mutations undone in a way that cannot itself flake. And a "flake" that has failed its retry too is not a flake to rerun past (G2's lesson, met again): the retry failed for a REASON — the dirty database — and reading that reason is what found the fix.

2026-07-15 · The R4 e2e guard "the lead's headline is the tallest element of the card" FAILED on a correct text-first lead (CC5) → on a wide desktop the lead headline is one line (~39px), and the byline footer is ~44px because its outlet anchor carries a 44px touch target (`min-h-11`, given back above `md` but the div still measured ~44px in CI). So "the headline is the tallest element" lost to the byline — not because anything was wrong, but because the byline's height is an ACCESSIBILITY floor, not decoration. "The headline is the tallest element of the card" and "no decorative frame taller than the headline" are DIFFERENT claims: R4 killed the reserved geometry of the L4 grey slab, and the slab is what must not tower over the headline — a byline sized by a touch target is not that. → fix: the guard compares the headline to the card's CONTENT chrome (the catalyst/sector tag row and the ticker chips, both inside the story link), and EXCLUDES the byline footer (a separate sibling, 44px touch target) and why-it-matters (multi-line editorial prose). Verified against the seeded SMCI lead on desktop. The general shape: when a guard restates a design rule as a measurement, measure the thing the RULE is about, not every element that happens to share the box — an accessibility-sized element failing a "tallest" check is the check being wrong, not the layout (the same species as the iOS-zoom rule firing on a radio, PD3).

2026-07-15 · The CC6 movers floor would have emptied the Desk's Movers module in production the moment it deployed → the floor reads `instrument.dv_bucket`, a new column the migration adds as NULL and only the full nightly backfills; production had 12,992 instruments, ALL null, so `isLiquidFloorEligible` rejected every mover and the module went empty (its raw top movers, AHD/ASMH, were exactly the junk D6 named — but the floor with no data excludes everything, not just junk) → loadMovers now detects "no mover carries a bucket yet" and falls back to the raw ranked top 8 (the pre-CC6 behavior) until the first nightly backfills, self-healing — the same honest-degrade shape as N0's migration → guard: caught by QUERYING PRODUCTION before tagging, not by any test (the seeded world has buckets, so every local test and the whole VRT oracle showed the floor working perfectly). The general rule, the third time this build has met it: a migration takes effect on the NEXT run, and CI/seed prove only that the code is right given the data — never that the data the deployed app reads has been written yet. Any new REQUIRED data source needs a not-ready fallback, and the only way to know its production state is to look at production.

2026-07-15 · The CLARITY plan specified "significance v2" as a fresh 4-term product, unaware that a thoughtful 5-term model already existed and already produced the plan's own acceptance order → `newsdesk/rank.py` was built in the N-phase (2026-07-13), two days before the plan (2026-07-15); the plan's 4.5 names the factors from scratch and never mentions scope/magnitude/the sum. A literal implementation of "build v2" could have deleted the hard-won Hormuz classifier and the magnitude machinery the plan did not know it was discarding → read the ACTUAL code before implementing a plan's spec, and reconcile: I kept the classifier (it feeds catalyst_weight), kept magnitude as diagnostic evidence, and implemented the product form the plan asked for — a faithful build that did not destroy what the plan was blind to. The reconciliation itself is a marked DECISION, because "the plan said X, the code already had Y" is exactly the judgment call the authority order exists to govern → guard: none mechanical, but the lesson is that a plan is written against a snapshot of the code, and a phase that lands days later must diff the plan against what is actually there, not implement it against the plan's own mental model.

2026-07-15 · CC6's significance v2 tested green on every fixture, and the moment I read the REAL production front page it led with a crypto stablecoin PR classified as "macro" and ranked two SeekingAlpha analyst opinions as "M&A" → the formula was correct (the seed, with correctly-classified clusters, orders Fed → FDA → earnings exactly right, and Appendix E passes); the fault is `newsdesk/rank.py`'s pre-existing `classify_event` KEYWORD classifier mislabelling messy real headlines — which CC6 never touched. But v2 is a PRODUCT with catalyst_weight as a full factor, so a misclassification swings the score 3× (macro 1.0 vs product 0.3), where v1's SUM gave the classifier only 15% weight and half-hid the same errors → nothing fixed this in CC6 (the classifier is out of the phase's scope, and fixing real-headline classification robustly is its own piece of work — flagged as Q-CC6-2). The lesson is the pipeline-verification memory's, sharpened: a test suite proves the formula is right GIVEN correct inputs; it says nothing about the quality of the classifier that PRODUCES those inputs from the real world. When a ranking formula multiplies by a classified field, the classifier's accuracy becomes the ceiling on the ranking's quality — and the only instrument that measures it is reading the actual published page. The green suite and the seed both looked perfect; production did not.

2026-07-15 · I planned a VRT shot of the OPEN control-room sheet for first-baseline eyes (the PD3 law on any new surface), then caught myself: the sheet's `full` action state and its Next-run column are CLOCK-dependent (computed in the browser against the reader's clock), and the INTERACTIVE VRT shots don't pin the clock — only `shoot()` does (`page.clock.setFixedTime(SEEDED_EVENING)`) → a clock-dependent modal shot photographed by an unpinned interactive test would rot with the hour CI runs, which is the exact bug the control room's client-state architecture was built to avoid (the old panel once photographed "markets are open" under a nav reading "MARKET CLOSED") → satisfied the "new surface's first baseline gets eyes" law by OPENING the sheet in local e2e (control-room.spec asserts its six sections + the degraded source) AND reading it in production, rather than committing a fragile shot; the sheet's chrome is already pixel-locked via the story/ticker sheet baselines (same DetailOverlay) → guard: the PD3 first-baseline law is satisfied by EYES, not necessarily by a committed baseline, and a clock-dependent surface must not get an interactive VRT shot that does not pin the clock. A baseline that rots is worse than no baseline.

2026-07-15 · The dispatch-loop e2e tests (control-room.spec) that mock GitHub with `page.route` cannot actually pass with a real (or dummy) GH_DISPATCH_TOKEN → `runPipeline` is a "use server" action, so its fetch to api.github.com happens SERVER-SIDE, and `page.route` only intercepts the BROWSER's network — the server fetch sails straight past the mock to real GitHub (a dummy token → 401 → the app reports "GitHub would not start the run") → these tests are `test.skip(!GH_DISPATCH_TOKEN)` and are structurally skip-only in CI (P-2 unprovisioned); the real dispatch path was verified by a live drill (N6 evidence §6), and the mock is best-effort documentation of the intended shape, not an executable proof → guard: don't treat a page.route mock of an external service as covering a server-action fetch; the boundary page.route can reach is the browser's, not the Next server's. (A tell that these were never exercised: the old currentRequestId read a `body.history` field the status route never returned — nobody noticed, because the block always skipped.)
