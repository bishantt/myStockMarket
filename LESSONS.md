# LESSONS.md — mistakes and their guards

Read at session start after CLAUDE.md (plan §9.2). A lesson that repeats means the guard was
inadequate — escalate it to a test or a skill. Write each lesson in plain English so a future
session (or the user) understands it without context (CLAUDE.md, "Readability & documentation").
Format:

```
YYYY-MM-DD · symptom → root cause → fix → guard added
```

---

(no entries yet — no build sessions have run)
2026-07-10 · The font-budget check reported "no latin fonts found" on a build that clearly had fonts → I assumed the `unicode-range: U+0000-00FF` that Google Fonts serves survives into the built CSS, but Next's minifier rewrites it to the wildcard `U+??`; a second bug hid behind it, a `\b` word boundary that can never match after a `?` because both sides are non-word characters → the check now accepts all three spellings of the latin range (`U+??`, `U+0000-00FF`, `U+0-FF`) and the regex was tested against real values from the build before being trusted → guard added: the script exits non-zero when it finds ZERO latin faces, so "found nothing" can never masquerade as "budget met"; both its pass and fail paths were exercised deliberately.

2026-07-10 · `node -v` reported 20.20.0 in every shell even after `nvm install 24 && nvm alias default 24` → Claude Code itself runs on Node 20 and exports its own bin directory into the PATH of every command it spawns, which shadows nvm's default before nvm.sh ever runs → build and test commands prepend the Node 24 bin directory to PATH explicitly → guard added: `.nvmrc` (24) and `"engines": {"node": ">=24"}` in app/package.json, so CI and any human shell fail loudly rather than silently building on the wrong runtime.

2026-07-10 · Twelve of the seventeen colour tokens were missing from the built CSS while five were present → Tailwind v4 tree-shakes `@theme` variables that no utility class references, and only the five used directly in plain CSS survived; the chart hook (plan §3.7) reads `--color-ink`/`--color-up`/`--color-down` off computed styles at runtime, which Tailwind cannot see → `@theme static` forces every variable to be emitted → guard added: the phase-exit token grep now asserts all seventeen §3.3 hexes are present in the built stylesheet, so a silent token deletion fails the gate.

2026-07-10 · My own verification printed "✓ typecheck clean" over a real TypeScript error → I wrote `npm run typecheck 2>&1 | tail -8 && echo "✓ clean"`, and in a pipeline the shell reports the LAST command's status, so `tail` (always 0) masked `tsc` (exit 1) → checks now capture output into a variable and branch on the command's own exit code, never on a pipeline's → guard added: the phase-exit gate runs each step through a `run()` helper that fails loudly; a green tick in the terminal now means the command actually succeeded. A gate that cannot fail is not a gate.

2026-07-10 · `tsc --noEmit` failed on a stale `.next/types/validator.ts` referencing a page I had just deleted → tsconfig includes `.next/types/**/*.ts`, and those generated types outlive the file they describe until the next build → re-running `next build` regenerates them; a fresh CI checkout has no `.next` at all, so the plan's gate order (typecheck before build) is safe there → guard: none needed in CI, but locally, delete `.next` after removing a route rather than trusting an incremental type check.

2026-07-10 · The Playwright smoke test could not find the heading "00 — Pipeline" that was plainly on screen → SectionMasthead wrapped the em-dash separator in `aria-hidden="true"`, and the spaces around it were hidden with it, so the heading's accessible name collapsed to "00Pipeline" — a screen reader announced the index and title as one run-on word → the dash is no longer aria-hidden; every major screen reader is silent on an em dash anyway, while the spaces survive → guard added: the e2e suite locates that heading by its accessible name, so if anyone hides the separator again the test fails. Accessible names are the cheapest a11y check there is, and a test that reads the page the way assistive tech does catches this for free.

2026-07-10 · Two auth e2e tests failed on `getByRole("alert")` resolving to two elements → Next renders its own `role="alert"` route announcer (`#__next-route-announcer__`) into every page, so any bare alert-role locator is ambiguous → the login error carries `data-testid="login-error"` and the tests scope to it → guard: prefer a test id over a role when the framework injects elements with the same role.

2026-07-10 · Every jose signing test failed with "payload must be an instance of Uint8Array" → the tests ran under jsdom (the config default, correct for component tests), and jsdom's TextEncoder returns a Uint8Array from a different JS realm, so jose's `instanceof` check rejects it → lib/auth.test.ts declares `// @vitest-environment node`, which is also simply true: auth runs on the server, in the proxy and a server action, never in a browser → guard: server-only modules get the node environment; the error message points at the payload but the cause is the realm.

2026-07-10 · Auth e2e tests that had passed began failing with "AUTH_USER or AUTH_PASS_HASH is not set", after I added the app/.env symlink → an EMPTY `AUTH_PASS_HASH=` line in .env, combined with a bcrypt hash (full of `$`) passed via the test's process.env, got collapsed to "" — Next loads .env through dotenv-expand, which reads `$2b`/`$12` as variable references and eats them; an empty .env line is enough to trigger it over the process.env value → removed all empty placeholder lines from .env (an absent var is safe; an empty one is not) and, for the real local hash, escape every `$` as `\$` (hash-password.mjs now prints both a raw form for Vercel/GitHub and an escaped form for local .env) → guard added: verified escaped hashes survive loadEnvConfig and that a `$`-value with no .env line is untouched. Vercel/GitHub inject env directly and never run dotenv-expand, so only local .env needs the escaping.

2026-07-10 · The font-budget guard reported "no latin fonts" again, this time after switching the build from Turbopack to `--webpack` for Serwist → the two bundlers spell the latin unicode-range differently: Turbopack emits `U+??`, webpack emits `u+00??` → the regex now accepts every known spelling (`U+??`, `u+00??`, `U+0000-00FF`, `u+0-ff`) and was unit-checked against latin-ext/cyrillic negatives → guard holds: this is the second bundler-specific spelling to break it, so the pattern is now written to be spelling-agnostic rather than patched per-bundler.

2026-07-10 · Serwist's manifest injection is a webpack plugin, but Next 16 defaults to Turbopack, which errors the moment a `webpack` key is present in next.config → applied Serwist only when `process.env.TURBOPACK` is unset, so `next dev` keeps Turbopack (SW is disabled in dev anyway) and `next build --webpack` engages Serwist and emits public/sw.js → guard: the PWA e2e suite runs against the webpack build, so if the SW ever stops being emitted the installability tests fail. Revisit when Serwist ships Turbopack support (serwist/serwist#54).

2026-07-10 · The FMP provider probe returned HTTP 403 while all other 8 providers passed → not a bad key: FMP deprecated its entire /api/v3/ surface on 2025-08-31 and the error body said "Legacy Endpoint ... only available for legacy users prior August 31, 2025"; the key works fine on the new /stable/ API → probe now calls /stable/quote, and a comment flags that the P2 earnings-calendar adapter must use /stable/earnings-calendar (verified returning 200) → guard: when a single provider 403s but others pass, read the response BODY before blaming the key — providers move endpoints, and the body says so.

2026-07-10 · After adding my real AUTH to root .env, 12 e2e login tests started failing locally ("AUTH_PASS_HASH not set") while CI stayed green → app/.env is a symlink to root .env, so the e2e web server loaded my real AUTH_PASS_HASH line, which made dotenv-expand mangle the Playwright fixture hash (bcrypt is full of $) to empty; there is NO single hash form that survives both cases (RAW survives with no .env, ESCAPED survives with a .env — opposite in CI vs local) → tried a Playwright globalSetup that moves .env aside, but the web server starts and reads .env BEFORE globalSetup runs, so it lost the race → real fix: moved the developer login to app/.env.development.local (loaded by `next dev`, NEVER by the `next start` the e2e server runs), leaving root .env AUTH-free so the fixtures survive naturally in both local and CI; deleted the globalSetup hack → guard: the 28-test e2e suite passes locally and in CI with no per-run file juggling. Lesson within the lesson: Playwright's webServer can start before globalSetup — never use globalSetup to prepare state the webServer reads at boot.

2026-07-10 · A REVOKE UPDATE/DELETE on signal_log did not make it insert-only — an UPDATE still succeeded → the app connects as the table OWNER on Supabase, and a table owner bypasses GRANT/REVOKE entirely → enforced insert-only with a BEFORE UPDATE OR DELETE trigger that RAISES an exception, which applies to the owner too (verified UPDATE and DELETE both rejected) → guard: the guardrail is a trigger, not a grant; a future test should assert both are blocked. Also: I verified this against the real Supabase DB and it left an undeletable junk row I had to TRUNCATE away — app-side DB checks should use a throwaway/test database, not production (the plan already calls for a seeded test DB and dockerised Postgres for pipeline tests).

2026-07-11 · Tried to spin up a local dockerized Postgres for publish.py tests; `docker --version` works but `docker ps` fails "Cannot connect to the Docker daemon" → the Docker CLI is installed but Docker Desktop (the daemon) is not running, and I can't start a GUI app autonomously → publish integration tests target TEST_DATABASE_URL and SKIP when it is unset; a Postgres service container in ci.yml runs them on every push, which is the plan's harness anyway (§6.1). To run them locally, start Docker Desktop first → guard: the tests never silently pass — they skip loudly (pytest skip) when there is no test DB, and CI always has one.

## The UI redesign (R0–R6, 2026-07-12)

**A guard that cannot fail is worse than no guard.** The visual-regression suite's "fonts
blocked" shot — the one meant to lock the pre-swap fallback layout — was passing while testing
absolutely nothing. Signing in first had loaded the fonts into the browser's memory cache, so the
later navigation made no network request, the route interception never fired, and the
"fonts-blocked" screenshot was quietly an ordinary one. What gave it away was that the file was
byte-identical to its neighbour. It now asserts that it actually intercepted a font request.
Whenever a test is supposed to capture an UNUSUAL state — offline, fonts blocked, an empty table,
an error boundary — assert that the unusual state actually happened.

**Measure before you take the fallback ladder.** Lighthouse came back at performance 86, and the
plan had a ready-made remedy: strip the card translucency, then remove the orbs. It would have
been easy, plausible, and completely wrong. Total Blocking Time was 20ms — the glass, the wash
and the orbs cost nothing measurable — while the page was pulling ten font files totalling 429KB
against a budget that claimed 273KB. The weight was in bytes, not pixels. Cutting two font
weights took performance from 86 to 93 and left the design intact. The plan's fallback ladder was
a reasonable guess written before anyone had measured; the measurement is what actually knows.

**A budget that measures the wrong thing is worse than no budget, because it reassures you.** The
font-budget script counted basic-latin faces and reported 273KB, and it passed. The browser
fetched 429KB, because Google splits every family into latin AND latin-ext and a real browser
takes both. The check was not wrong — it was NARROW — and it had been quietly flattering us by
about sixty percent for two phases.

**Screenshots catch what tests cannot.** Three real bugs in the redesign were invisible to a
green test suite and obvious in a picture: the Desk rendering module 02 above module 01 (the grid's
dense backfill pulling the Brief into an empty cell); the header reading "Jul 10" while the module
beneath it read "Jul 11" from the same value (a bare calendar date formatted in Eastern time,
which shifts it back a day — a trap that lib/time.ts already documented, and that I walked into
anyway); and every quantile dot in the Range Ladder squashed into an overlapping ellipse by
`preserveAspectRatio="none"`. A dot you cannot count is not a dotplot. Look at the thing.

**Removing an accident can expose a bug that was hiding behind it.** Taking the `cookies()` call
out of the Desk layout (so the theme could live on `<html>`) made the route statically
prerenderable — which is what the plan always wanted, and which the cookie read had been silently
preventing. It also meant the layout's watchlist query started running at BUILD time, on a machine
with no DATABASE_URL, and the build died. The query had never been wrapped in a try/catch, unlike
every other read in the app, and it had got away with that for six phases purely because the route
happened to be dynamic. The bug was always there. The fix just stopped hiding it.

**The plan's own rules will collide, and the collision is information.** The anti-drift grep bans
the word `transition` anywhere inside a P2 file. The motion spec explicitly permits a chevron to
rotate on a `<details>` summary. SetupCards is a P2 file with a chevron. Rather than carve an
exception into the grep — where the next reader would find it and not know why — the chevron moved
into its own component file. A coarse rule that is easy to verify beats a subtle one that is easy
to get wrong.

## The app-feel build (F0–F7, 2026-07-12)

2026-07-12 · My new navigation-timing test reported Scans as the FASTEST room in the app (14ms) when it is in fact the slowest (1342ms) → Playwright matches an accessible name by SUBSTRING, not exactly, so `getByRole("heading", { name: "Scans" })` matched the Desk's own module masthead "07 — Sectors & scans" — which is already on screen before the tab is tapped. The timer started and stopped on the page it was supposed to be navigating AWAY from → the spec now asserts the destination heading is ABSENT before every timed tap, so a locator that starts matching the origin page fails the test instead of flattering it → guard added: the absence check runs on all 8 samples of all 4 destinations, not once. The general rule, which is the one worth keeping: when a test measures the ARRIVAL of something, it must first prove that thing is not already there. `exact: true` would have fixed this one locator and left the trap armed for the next.

2026-07-12 · The new TTFB budget passed /scans/unusual-volume at 46ms — a route that does not exist yet and was answering 404 → I built the probe list from the plan's future route map (correctly — a family left out of the probe set is a gap that becomes permanent), but the check only compared latency against the budget and never looked at the status code. An empty 404 is the fastest response a server can give, so the budget was rewarding the absence of the page it was meant to be measuring → the gate now treats any non-200 as a miss, and the evidence table prints the status beside every number → guard: both the pass path and the fail path were exercised deliberately before the instrument was trusted. This is the same species as the fonts-blocked VRT shot that tested nothing (R-phase): a check that cannot fail is worse than no check, because it also produces a reassuring green tick.

2026-07-12 · There is no way to run Postgres locally on this machine → brew ships no postgresql bottle for macOS 26.3 and its source build dies in configure; the prebuilt embedded binaries link against a libz that exists only inside the macOS dyld shared cache, so there is no file to point them at; Docker Desktop's daemon is not running and cannot be started headlessly (LESSONS 2026-07-11, same wall) → the seed is verified WITHOUT a database instead: 12 unit tests assert its invariants directly (frozen mover ranks, awkward rows below the movers' cut, fills that agree with lib/paper.ts), and CI's Postgres service container runs the real `migrate deploy && db:seed` on every tag → guard: the fixture tests are negative-controlled — nudging SMCI's seeded return by 0.006 turns them red. Do not spend another session trying to install Postgres here; the database oracle is CI, and that was already the project's settled answer.

2026-07-12 · A stale `.next/types` broke typecheck with errors about routes I had just deleted → the same lesson as 2026-07-10, met from the other side: I created two temporary probe routes for the prefetch experiment, deleted them, and `tsc` kept type-checking generated validators that still described them → `rm -rf .next` before the next typecheck → guard: none needed beyond the existing lesson, but note the trigger is ADDING-then-removing a route, not only removing one. Any temporary route must be followed by a clean build.

2026-07-12 · A visual-regression baseline came back as a picture of Next.js's 404 page, and the bug behind it would have deleted an entire room in production → `revalidatePath(path, "layout")` DROPS THE KNOWN-PARAMS SET of any route that declares `dynamicParams = false`. Every URL in that family then 404s — permanently, until the next deploy. `/scans/[preset]` declares exactly that (a closed set of five presets; the flag is what makes /scans/garbage a real 404 rather than a 200 carrying a not-found page). The theme action had called it since P6 and the watchlist action since F1 — so the reader would have found this by changing their theme ONCE, and every scan table in the app would have been gone → the app now has NO layout-scoped revalidations anywhere: the theme applies client-side and needs none, and the watchlist names the five rooms it changes → guard added: drift rule 17 greps for `revalidatePath(..., "layout")` and fails the build. Three lessons inside the one bug: (a) a framework flag with a whole-route-tree blast radius will eventually be aimed at something you did not think about; (b) the failure was invisible in isolation and only appeared in the FULL suite, because it needed another spec to fire the action first — a bug that only exists in combination is exactly what an end-to-end suite is for; (c) it was reproduced locally in two commands (request the page → 200; call a layout revalidation; request it again → 404), which took ten minutes and ended an hour of theorising. Reproduce, then fix.

2026-07-12 · The VRT diff images have never once been uploaded → the ci.yml artifact step pointed at `app/playwright-report/`, which CI never writes: the run uses the `github` reporter, not `html`. Every visual-regression failure for six phases uploaded an empty artifact, so the one thing a pixel failure owes you — the picture — was the one thing you could not get → the step now uploads `app/test-results/`, where Playwright actually writes the expected/actual/diff triptych → guard: the very next failure produced a readable 404 screenshot, which is what identified the bug above in one look. A diagnostic that has never been exercised is not a diagnostic.

2026-07-12 · A `notFound()` inside an ISR-cached dynamic route returns HTTP 200 with the 404 page in the body → Next caches the not-found render as a static entry and serves it with an OK status. `/ticker/GARBAGE` does this today (it became ISR at F1; before that it was force-dynamic and 404'd properly). `/scans/[preset]` avoids it only because `dynamicParams = false` produces a genuine 404 → the scans route keeps the flag (and drift rule 17 protects it); the ticker's wrong status is logged for the user as [FYI] — the reader still SEES "this page could not be found", so it is a correctness wart rather than a lie on screen → guard: e2e asserts the 404 STATUS on /scans/garbage, not merely the words on the page.

2026-07-12 · The e2e that proves the PM journal saves an entry has never once proved it → it waited for a `journal-saved` marker, and that marker renders whenever `state.ok` is true — which is the INITIAL value of `useActionState`. "Saved." was on the page from the moment it loaded, before anything had been written, so the assertion passed whether or not the write happened. It only started failing when the write became genuinely slow, which is the one circumstance in which a guard that cannot fail finally does → the test asserts the TEXTAREA CLEARED instead: the form resets itself on a successful write, so an empty field cannot be true until the server action has come back ok → guard: this is the third guard-that-could-not-fail found in this build (the fonts-blocked VRT shot, the 404-scores-as-fast TTFB probe, and now this). The pattern is always the same — the assertion is true in the state the page STARTS in, so it never observed the transition it was written to observe. When writing a test, ask what the page looks like BEFORE the action: if the assertion already holds there, it is not a test.

2026-07-12 · A server action that saves one journal row took over five seconds in CI, and looked like a hang → a server action's response carries a full re-render of the page it was invoked from. On the Desk that is the entire morning — ten-odd database reads — before the client sees its result. It is a slow round trip by design, not a stuck promise, and on a contended CI Postgres it comfortably outlives Playwright's 5s default → the test's timeout now says so explicitly, with the reason → worth knowing when the Desk grows: every write from a Desk form pays for a full Desk render before the button stops saying "Saving…".

2026-07-12 · Every write on the Desk left its button stuck on "Saving…" forever — while the row landed in the database perfectly — and it had been doing so since the Desk became a cached route on 2026-07-11 → calling `revalidatePath("/")` INSIDE a server action that was invoked FROM "/" deadlocks the action's own reply: the action invalidates the very page whose fresh render it is trying to stream back as its response. The write succeeds, the browser never hears about it, and the reader concludes their journal entry was lost and writes it again → both Desk writes (the journal, and the setup cards' weakener checkboxes) now revalidate inside `after()`, which is the framework's own primitive for work that must happen once the response is finished. The reader gets their reply immediately; the cache entry is invalidated a moment later, for whoever comes next. Nothing about the freshness contract changes — only the ordering → guard: the journal e2e now asserts the FORM CLEARED (which only happens on a successful round trip) instead of a marker that was visible from page load. Isolated in one experiment: delete the revalidate line, and the same action returns in 14 seconds of test wall-clock; put it back, and twenty seconds is not enough. The bug survived six phases because the only test that could have caught it was asserting a state the page starts in.

2026-07-12 · The `feel-final` tag's CI failed on an accessibility rule that printed "nested-interactive (1 nodes)" and nothing else, on a page whose own markup contains no buttons at all → the offending element was injected by a LIBRARY. Lightweight Charts' `attributionLogo` option appends a TradingView link inside the chart container, and that container is `role="img"` — an element whose children are, by that role's own definition, part of one picture. So the licence's link was on the page, present in the DOM, painted on screen, and reachable by nobody with a keyboard. Two lessons stacked: a third-party widget can violate your accessibility rules with markup you never wrote, and `role="img"` on a container is a promise that NOTHING inside it is interactive → the logo option is off and the attribution is a real link in a caption below the chart, which is what the licence asked for in the first place ("a link on the page of your website that is available to your users") → and the gate now prints the failing node's selector and HTML. A guard that names the rule but not the node tells you that something, somewhere on a page of a hundred elements, is wrong, and leaves you to find it. That is half a guard, and I paid the other half in search time.

2026-07-12 · The service-worker test began timing out at F7 — waiting 10 seconds for the worker to take control of the page, on code that had not touched the service worker in eight phases → nothing about the worker broke. Installing a service worker means downloading its entire precache, and this release quietly made that bigger (every room became a cached, prerendered route) while the Desk now prefetches five more of them on idle. On a cold CI runner the install was still in flight when the test reloaded — and a worker that has not finished installing cannot control anything. The test was not waiting too little; it was waiting for the wrong thing, at the wrong time → it waits for `serviceWorker.ready` (an ACTIVE worker — a real state the app reaches) and THEN reloads. The general shape: when a timing test starts failing after an unrelated change, ask what the change made SLOWER before you ask what it broke — and fix it by waiting for a state, never by waiting for longer. A longer timeout is a race you have agreed to lose more slowly.

2026-07-12 · Re-enabling axe's colour-contrast rule reported 58 failures across every colour the app owns — muted, ink, the accent, the up and down chips — which reads as "the palette is broken" and is one of the most alarming things a gate can say → the palette was fine. Every page in this app fades in (`.route-fade`, opacity 0 → 1) and axe composites a foreground colour through any ancestor's opacity, so it was measuring the page WHILE IT WAS STILL ARRIVING and faithfully reporting the colours of a half-transparent screen. `text-muted` at 55% opacity really is 2.23:1; it is also not a colour any human has ever read. Wait for the fade to land and 58 failures collapse to ONE → the gate has a `settle()` helper that waits for every `.route-fade`/`.content-fade` to reach opacity 1 before axe runs → the general rule, which is not about colour: NEVER MEASURE A COLOUR, A SIZE OR A POSITION WHILE THE PAGE IS STILL ANIMATING — you will measure the animation. And note what the false alarm nearly cost: I had already written the 58 into an evidence file and asked the user to approve a palette change on the strength of it.

2026-07-12 · I surveyed contrast across both themes, found dark clean, wrote "a colour that passes is not a colour to change" into the token sheet — and CI failed on the phone Desk in dark → the survey had been run with `--project=desktop`. Midnight's grey clears 5.00:1 on a STANDARD card and 4.44:1 on a RAISED one, and the phone Desk's shelf cards are raised — so the one surface that failed was the one surface my survey could not see. I measured half the app and reported on all of it → both muted values now clear the floor against the LIGHTEST card each theme ever puts them on, and the a11y gate runs both themes × both projects (42 checks) → the lesson is not "run both projects", it is that a clean result from an instrument is only as wide as the instrument's coverage, and the coverage is the first thing to state when reporting a null finding. "No failures found" and "no failures exist" are different sentences.

2026-07-12 · A watchlist row that a test had just added VANISHED when the next button in the same row was clicked — intermittently, on a page that had passed this test for weeks → /settings was an ISR-cached route whose own server actions revalidated "/settings". Every click there runs an action that re-renders that page as its reply, and when the reply can be served from a cache entry written before the click, the reader gets back the page as it was BEFORE they touched it. The extra page-loads from a new test suite warmed the cache more aggressively and turned a rare race into a reproducible one → /settings renders on request; its actions no longer revalidate their own path; the exception is declared in three places that must agree (the page, check-routes' ALLOWLIST, drift rule 13) → the rule, which is the second time this exact shape has cost us (the first was the Desk's hanging writes): A PAGE MAY BE CACHED, OR IT MAY BE WRITTEN TO AND READ BACK IN THE SAME CLICK — NOT BOTH. NEVER REVALIDATE THE PATH YOU WERE INVOKED FROM. Every other room in this app is a reader; the one room that is a writer is the one room that cannot be cached.

2026-07-12 · A tag CI that had never run caught a real bug on its first firing → `nc-0` went red at the "Migrate and seed" step: a fixture helper `t("22")` produced the string `"2026-07-09T22:00.000Z"` — no seconds field — and `new Date()` returned an Invalid Date rather than throwing. Everything local was green: 395 unit tests, typecheck, lint, drift, build, budgets. The fixture tests passed because they asserted the INTERESTING fields (the significance scores, the ordering, the blur placeholders) and never looked at a timestamp → two lessons, and the second is the bigger one. (1) `new Date("nonsense")` does not throw; it returns an object that looks like a Date and fails only when something finally tries to use it — so a bad date travels a long way from its cause. (2) **A fixture's boring fields are still fields.** The test suite was thorough exactly where I had been thinking hard and blank exactly where I had not, which is the normal shape of self-written tests. Guard added: a test that walks every Date in every fixture and asserts it is real — verified by re-introducing the bug and watching it go red.

2026-07-12 · A source-status key that cannot fail is not a status → production recorded `fred: ok` on a night when all three index levels were NULL, because the check was `any(value is not None for value in macro.as_columns().values())` and VIX alone satisfied it. The Desk therefore showed four ETF prices under a footer claiming FRED index levels, and nothing anywhere said the levels were missing → the honest diagnosis was NOT that `fred: ok` was wrong. FRED *was* reachable; it answered for two of its four cells. **One key cannot describe two different failures.** The repair is a second key (`fred-indexes`), not making the first one lie in the other direction. Generalisation: when a status is computed with `any()` over a heterogeneous set, ask what it is really claiming — an `any()` over things that fail independently is a status that reports the healthiest member and hides the rest.

2026-07-12 · An upsert that writes what it fetched will eventually destroy what it had → `sp500 = EXCLUDED.sp500` in the nightly's ON CONFLICT clause meant a flaky FRED night overwrote a perfectly good stored index level with NULL. The run succeeded. The data was gone. The Desk silently fell back to an ETF, and every gate stayed green → **a successful run destroying data is the worst shape a bug can have, because everything looks like it worked.** The fix is not COALESCE(EXCLUDED.x, table.x): the table is keyed by run_date, so each night is a NEW row with nothing to coalesce with — the repair has to reach back to a previous row, which is why it is a function (macro_levels.resolve_index_levels) and not a clause. Rule of thumb: before writing a nullable column from a fetch that can fail, ask what the null MEANS. "The source said nothing" and "the value is nothing" are different facts, and only one of them should ever be persisted.

2026-07-12 · A static provenance line is a lie with a delay fuse → the Macro Pulse's footer was the fixed string "Index levels · FRED · prior close", written for the happy path and rendered regardless of what the rows above it showed. On the one night it mattered it sat beneath four ETF prices and declared them FRED index levels → **a sentence describing data it cannot see will eventually describe data that is not there.** Provenance must be COMPOSED from what actually rendered (ruling C6), and the test that proves it is a property test, not an example: build two different states and assert the line differs. An example test would have passed happily against a hard-coded string.

2026-07-12 · A visual-regression gate that scales its tolerance with page size gets weaker exactly where it needs to be stronger → `maxDiffPixelRatio: 0.01` on the desktop Desk's full-page shot (1366×2763 = 3.77M pixels) is a budget of **37,742 differing pixels**. N1 rewrote the whole macro row — reordered it, relabelled "Russell 2000 · IWM (ETF proxy)" to "Small caps", deleted a chip, replaced the provenance sentence — and the desktop baseline passed unchanged. The phone shot, on a page with half the pixels and therefore half the budget, caught it. Same DOM, same build, same commit: one viewport told the truth and the other did not → **a ratio-based pixel tolerance is a guard whose strength depends on how long the page happens to be, which is the wrong variable entirely.** Replaced with an absolute `maxDiffPixels: 600`. Second, compounding trap: `--update-snapshots` defaults to `changed`, which only rewrites baselines whose comparison FAILED — so a real change that fits inside the tolerance is neither reported nor re-baselined, and the stale image survives as the new "truth". Use `--update-snapshots=all`. How it was caught: not by a test — by opening the regenerated PNG and looking at it, after noticing that only 6 of 46 baselines had changed when the change was to a shared component. **When a re-baseline surprises you by changing less than you expected, that is a finding, not a relief.**

2026-07-12 · On a cached route, "reset the data" is TWO steps, and the second is the one you forget → the Academy VRT shot was photographing a checkmark that `academy.spec.ts` had left behind (opening a lesson marks it read). Deleting the `lesson_progress` rows before the shot did NOT fix it: `/academy` is an ISR route, and the action that marked the lesson read had already revalidated it, so the CACHE held a rendering with the checkmark. Emptying the table changes what the next render would produce; it does nothing to a render that is already cached and being served → clear the state, THEN bust the cache. This generalises to every cached room in this app, and to any test or script that "resets" state behind an ISR page. Related, and the deeper structural point: **the VRT baselines were being generated by a job that runs only `vrt.spec.ts`, but verified in a job that runs the whole suite** — so they were born in a world where no other spec had mutated the database, and checked in a world where several had. A baseline must photograph a state the suite establishes on purpose, not residue.

2026-07-13 · CI can be green for months while production runs a schema the code does not match → N0 authored a migration (five tables, one column), committed it, and every CI run since went green. Production never got it. The reason is structural and worth internalising: **CI migrates a FRESH POSTGRES CONTAINER on every run, so a green pipeline proves only that a migration APPLIES — never that it was applied to the database the deployed app actually reads.** The Vercel build ran `next build` and no migration step, so nothing ever ran it there. And nothing looked broken, because the app's loaders all catch a database error and degrade to their honest empty state (which is correct, and which the DB-free CI build depends on) — so the missing column did not crash anything. It just made the Macro Pulse fall back, silently, every night, which is the exact disease the current plan was commissioned to end. **The honest-degrade path is a good thing that made a real defect invisible; that combination deserves a name.** Fixes: the deploy now runs `prisma migrate deploy` (fails closed — a migration that will not apply is a deploy that does not happen), and `npm run check:migrations` compares disk against the live database before a push. Generalisation: any gate that runs against a DISPOSABLE copy of a system cannot make claims about the PERSISTENT one, and the gap between them is invisible precisely because everything in it is green.

2026-07-13 · Thirty-four passing tests, and the page could not render at all → `pipeline_run.runDate` is a Prisma `@db.Date` — a trading DAY — and the loader serialised it with `.toISOString()`, which is right for an instant and wrong for a date: it produced `"2026-07-10T00:00:00.000Z"` where the freshness machine wanted `"2026-07-10"`. Every date it built from that was an Invalid Date, and the first formatter to touch one threw a RangeError. Nineteen tests on the machine, fifteen on the component, all green — because **every one of them constructed its own fixture, and every fixture was correct.** The defect was in neither unit. It was in the JOINT, and both sides of a joint can be individually perfect while the joint is broken. A test that builds its own inputs is structurally incapable of seeing the seam where the real inputs come from. Fix: a test on the SEAM (the loader's real output, fed to the real machine, with no hand-tidying in between), and the machine now refuses a timestamp at its own front door with a message naming what it wanted — so the next regression fails where the mistake IS, not three components downstream in a formatter that did nothing wrong.

2026-07-13 · Two more guards that could not fail, and they had the same shape as every previous one → (1) the iOS 16px sweep had been PASSING ON /paper WITHOUT LOOKING AT THE FORM: the ticket lives behind a Suspense boundary, `page.goto()` resolves before it hydrates, so the sweep queried the DOM, found zero fields, iterated over nothing, and reported success — on the one page whose form is the entire reason the rule exists. Any unrelated change that shifts the timing flips it, which is why it looked like a flake. It is not a flake; it is a guard that never ran. (2) The touch sweep's running-text list omitted `figcaption`, so the chart's "Chart by TradingView" attribution — a link inside a sentence, exactly what WCAG 2.5.8's inline exception covers — was measured as a bare 82×14 control; it had been passing only because the chart is code-split and the caption often did not exist yet when the sweep ran. **The pattern, now seen six times: a guard passes because the thing it measures is ABSENT, not because it is correct. Every sweep must assert that it swept something.** Both now count what they examined and fail on zero.

2026-07-13 · The iOS zoom rule was ALSO crying wolf, and over-triggering is its own kind of broken → the same sweep measured every `input`, including the segmented control's radios (13.5px, the browser default, unchanged since F2). But iOS zooms on focusing a field that summons a KEYBOARD, and a radio summons none. Failing the build for it would have driven someone to "fix" a control that was never broken, in a way that made the design worse. **A guard that rejects correct code teaches people to work around the guard** — which is the only outcome worse than not having it. Same lesson landed twice this phase: drift rule 18 fired on `has-[:disabled]:text-faint`, which is not merely allowed but exactly what the token exists for. When a rule fires on code you believe is right, the first question is whether the RULE is right.

2026-07-13 · A baseline that depends on the wall clock rots on its own → the pipeline strip grades freshness in the browser (deliberately: a cached render must never photograph a dead pipeline as a healthy one), so the Desk now renders something that depends on WHEN IT IS. The seed publishes one run for a fixed trading day; real time keeps moving. The first baseline run came back with the strip in its AGING state — correct, and unrepeatable: a day later it would have been the DEAD banner, and the baseline would have failed with nobody having touched a line of code. Then someone re-baselines it to go green, and a week later it fails again. **A picture that rots with the calendar is not an oracle; it is a chore with a countdown on it.** Fix: pin the browser's clock (Playwright's `page.clock`) to the seeded session's evening, and ASSERT the expected state before shooting — so if the seed's date moves and the pin does not, it fails loudly instead of quietly photographing the wrong rung of the escalation ladder. Found the same way the last two VRT bugs were found: by opening the PNG and looking at it.

2026-07-13 · Re-baselining is how a budget dies, and every individual step is defensible → the bundle guard fired (/paper +10.5KB over its baseline). The growth was real, understood and shared (a client component imported by two entry points gets hoisted into a chunk every route carries). The tempting move — bump the baseline, note the reason, move on — is correct ONCE and fatal as a habit: six phases of "it only grew a little, and here is why" puts the app at 260KB with a green gate the whole way, because **drift is measured against the last thing that was accepted.** So the rebaseline landed together with the HARD CEILING the plan always specified (200KB) and nothing had ever enforced. Drift catches the unexplained; a ceiling catches the accumulation of the explained. Any budget expressed only as "no worse than last time" has this hole in it.

2026-07-13 · When a gate goes red, the first question is not "what broke" — it is "is the gate asserting the thing it means to assert" → the `nc-2` tag failed with 11 failures across 6 tests. Not one was the product, and not one was a pixel (all 55 VRT baselines passed). Three causes, three ways a test can be confidently wrong: (1) **`getByRole("alert")` was measuring the FRAMEWORK.** Next.js renders `<div role="alert" id="__next-route-announcer__">` on every page — an empty live region for announcing client-side navigations — so a bare `getByRole("alert")` matches everywhere, always, and `toHaveCount(0)` can never pass. Four tests were asserting "no alarm on a healthy night" while the healthy night rendered no alarm at all. (2) **A test that reads time-dependent output did not say WHEN it was reading.** The strip grades against the browser's clock; the seed is a fixed trading day; real time moves. The test asserted the fresh line while CI, days later, correctly showed the aging one. The product was right. (3) **"Zero network requests" was the wrong claim, and enforcing it would have cost real performance** — the router prefetches the tab-bar rooms on idle, which is the mechanism that took a tab tap from 1342ms to 280ms. A test demanding literal silence would have driven someone to delete the app's navigation speed to satisfy it. The claim the control actually rests on is narrower: *no data fetch for this page*. **Generalisation: an assertion inherits the ambient behaviour of the platform it runs on. Before believing a red gate, check what else is on the page answering to the same name.**

2026-07-13 · Three of this repo's FRED fixtures were never recorded — they were invented, and they had been "passing" for three phases → the `new-provider-adapter` skill's first rule is "Record REAL fixtures first", and R0 (commit `4a1c736`) did not: it hand-wrote `sp500.json`, `nasdaqcom.json` and `djia.json` in the shape of a real FRED response and filled in plausible numbers. The tell was `"count": 8000` against the real series' 2610, and a value of 6812.34 where FRED actually said 7575.39. Nothing broke — the parser works on the real data too — so **for three phases every index-level test proved that the parser agreed with my imagination.** What the fiction had already destroyed was a real property of the data: FRED's index series post a day AHEAD of the VIX (the S&P's newest observation is the 10th, the VIX's is the 9th), and the invented fixtures gave them the same date. That is a fact about the world that no test could have learned from them, and one the Mood gauge's as-of reasoning depends on. → The generalisation is the sharpest version yet of this build's recurring disease: **a fabricated fixture is not a weak test, it is an INVERTED one** — it does not fail to check the provider, it actively certifies that the code matches a fiction, and it hands you a green tick for doing so. A guard that measures nothing is merely useless; a guard that measures the wrong thing is worse than none. Rule adopted: **a fixture that was not recorded must say so in its own FILENAME.** Gold's key does not exist, so its success fixture is `xau_usd_UNVERIFIED.json` and the only real gold recording in the repo is `error_403.json` (the genuine unkeyed rejection — which does pin the route and the auth header, and is worth having). Nobody can read that filename and mistake it for evidence.

2026-07-13 · The unit a freshness rule counts in is a product decision, and getting it wrong makes the lamp useless in the direction nobody notices → the macro board's stale-cell rule is "older than three cadences". Implemented in calendar days, gold's cell would go amber **every Monday**: Friday's price is three days old and ZERO SESSIONS old, because the gold market was shut and there is no newer price to have. A lamp that cries wolf every Monday is a lamp nobody looks at by Tuesday, and then it is not there on the morning it is finally telling the truth. But the OPPOSITE unit is equally wrong one cell to the right: Nepal Rastra Bank publishes every calendar day, weekends included, so for the rupee a weekend genuinely IS three missed publications and counting sessions would hide a real outage. **Same ladder, different clocks, because the sources keep different clocks.** This is N2's freshness lesson ("count in sessions, never days") discovered to be half of a bigger rule: the honest unit is not "sessions" and not "days" — it is *whatever unit the source actually publishes in*, and a staleness rule that does not ask that question is guessing. Both consumers now share one session counter (`market-hours.sessionsBetween`); two copies of "what counts as old" is one copy too many.

2026-07-13 · The seed already contained the exact contradiction the ruling was written to prevent, and nothing was checking → the Mood gauge's C8 contract says each component must show which way it pulls the score. N0's seed stored that arrow as a hand-written field beside each percentile — and one of them had already drifted: momentum sat at the **48th percentile**, below its own median, carrying the arrow **"greedy"**. It looked plausible (the index really was above its 125-day mean), it broke nothing, and it would have shipped to screen. → **A derived value that is stored as a field is a value that is free to disagree with what it was derived from, and eventually will.** This is ruling C6 (provenance must be composed from what actually rendered) applied one level down, inside a data structure, and it has now cost this build twice: once as a static provenance sentence describing rows it could not see, once as an arrow describing a percentile it did not read. The arrow is now DERIVED at both ends — the pipeline computes it from the percentile, the app recomputes it from the percentile — so no stored value, however it got into the database, can put a label on screen that contradicts the number printed beside it. When you catch yourself storing a field whose value is a pure function of a field next to it, you are storing a future contradiction.
