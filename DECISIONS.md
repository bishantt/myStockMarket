# DECISIONS.md — append-only decision log

Format (plan §9.1): `YYYY-MM-DD · [claude] · [local|structural] · decision · rationale (one line) · alternatives rejected`
(the alternatives-rejected field is written with a `rejected:` prefix.)

**Writing convention.** Entries are plain English: short sentences, plain words, no jargon. The
line format above stays fixed; everything inside it should read clearly to a human skimming the
log. (CLAUDE.md, "Readability & documentation".)

**Marker convention (the veto channel).** Every entry Claude writes carries the `[claude]` marker.
ANY line in this file without `[claude]` is user-authored: it is an instruction at authority
rank 2.5 (above this log and Claude's judgment, below the Research Report and Blueprint). The
session ritual diffs this file first thing every session; honoring a user-authored line is the
session's FIRST task. Structural entries additionally follow plan §1.3.3 (choose the option that
keeps ranks 1–2 intact; Appendix E/F/J changes are structural by definition).

---

2026-07-09 · [claude] · structural · Cookie-session login replaces Basic Auth (plan §4.4) · Basic-Auth 401s break installed PWAs; the Blueprint itself names the single-credential step-up · rejected: Basic Auth, Auth.js
2026-07-09 · [claude] · structural · “Broadsheet Terminal” design system committed token-for-token (plan Part 3) · a real identity the executor cannot drift from · rejected: default shadcn look, dark-first terminal pastiche
2026-07-09 · [claude] · structural · Serwist for the service worker (plan §5) · maintained Workbox successor with first-class Next.js wiring · rejected: hand-rolled SW, next-pwa (unmaintained)
2026-07-09 · [claude] · local · No push notifications and no offline write queue in v1 (plan §5.3, Part 8 backlog) · push needs a calm-tech review first; write queues risk silent divergence · rejected: fire-and-forget offline queue
2026-07-09 · [claude] · structural · Academy lessons as MDX files in git, not DB rows (plan Appendix B) · content belongs in version control · rejected: Blueprint lesson table
2026-07-09 · [claude] · structural · Refresh fades 160–200ms vs RR §9.7 “~500ms” (plan §3.5) · same calm intent, one quiet fade, no shimmer · rejected: literal ~500ms fades
2026-07-09 · [claude] · local · npm for app/, uv for pipeline/ (plan §4.1) · boring and universal; lockfiles pin versions · rejected: pnpm, bun, poetry
2026-07-09 · [claude] · local · Fonts: Archivo + IBM Plex Mono + Newsreader via next/font, self-hosted (plan §3.2, §4.5) · same-origin, precacheable, ≤320KB budget · rejected: remote Google Fonts, Inter-everything
2026-07-09 · [claude] · local · Scaffold: app/ and pipeline/ left as empty directories · create-next-app (P0 step 3) refuses non-empty targets and uv init (P0 step 8) owns pipeline/; workflow YAMLs are authored at P0 step 8 per Appendix C · rejected: hand-written package.json/pyproject stubs (would pre-empt the official scaffolds and pin stale versions)
2026-07-09 · [claude] · local · Two skills pre-seeded ahead of the §9.3 minting moments (new-provider-adapter, base-rate-display) · they transcribe contracts already fixed in the plan (Appendix F/J, §6.1–6.2), and each is marked refine-at-first-use so the rubric’s mint-on-evidence intent is preserved · rejected: waiting for the P1/P4 minting moments (loses the warm-start benefit for zero added risk)
2026-07-10 · [claude] · structural · Readability-first convention adopted for all code, docs, and terminal output (user directive — CLAUDE.md “Readability & documentation”) · the user leads the build and reads everything; plain English and clear-over-clever code are permanent constraints with guardrail force · rejected: machine-brevity style, uncommented functions, shorthand-heavy logs

## P0 — session 1 (2026-07-10)

### Structural

2026-07-10 · [claude] · structural · Newsreader loses the optical-size (opsz) axis; italic stays (plan §3.2 + §4.5, correction block added to both) · the axis costs 153KB on the latin subset — more than Archivo and Plex Mono combined — and pushed the three families to 388KB against the plan's own ≤320KB budget; the two plan clauses contradicted each other and neither is backed by a rank-1/2 source, so the budget wins because it directly serves the Research Report's calm, fast reading experience; fonts now total 237KB, guarded by app/scripts/check-font-budget.mjs · rejected: dropping the display italic instead (loses "the one literary flourish"), raising the budget (weakens the LCP target that Lighthouse checks at every phase exit), keeping both (breaks the contract)

### Local

2026-07-10 · [claude] · local · Exact toolchain versions pinned at P0 (plan §4.1 requires recording them here) · Node 24.18.0 · npm 11.16.0 · uv 0.11.28 · gh 2.96.0 · next 16.2.10 · react 19.2.4 · typescript 5.9.3 · tailwindcss 4.3.2 · eslint 9.39.4 · vitest 4.1.10 · zod 4.4.3 · jose 6.2.3 · bcryptjs 3.0.3 · sharp 0.35.3 · jsdom 29.1.1 · lockfiles hold the rest · rejected: floating ranges without a lockfile
2026-07-10 · [claude] · local · globals.css uses `@theme static`, not a bare `@theme` · Tailwind v4 tree-shakes theme variables no utility class references, which would silently delete the tokens the Lightweight Charts hook reads at runtime (plan §3.7) and the ones /styleguide renders as a living spec · rejected: bare @theme (verified: 12 of the 17 colour tokens vanished from the built CSS)
2026-07-10 · [claude] · local · lib/tokens.ts is the second and last file allowed to contain a hex colour · the web manifest and Next's viewport.themeColor are read by the OS before any stylesheet is parsed, so they cannot use a CSS custom property; the anti-drift grep (§3.10) therefore allows exactly globals.css and lib/tokens.ts · rejected: hardcoding hexes at each call site, reading tokens at runtime (impossible for the manifest)
2026-07-10 · [claude] · local · Node 24 pinned three ways: .nvmrc, package.json engines, and an explicit PATH prefix in build commands · the Claude Code process itself runs on Node 20 and its PATH leaks into every shell, so `node` resolves to 20 unless overridden — the nvm default alias is not enough · rejected: relying on nvm's default alias, downgrading the plan's Node 24 pin
2026-07-10 · [claude] · local · create-next-app run with --empty; its README.md deleted; its AGENTS.md and CLAUDE.md pointer kept in app/ · --empty gives the clean scaffold the plan's "prune boilerplate" step wants, and AGENTS.md carries Next 16's own warning that its APIs differ from training data — useful, and additive to the root constitution · rejected: pruning by hand after a full scaffold, deleting AGENTS.md
2026-07-10 · [claude] · local · @types/bcryptjs removed · bcryptjs v3 ships its own type declarations; the DefinitelyTyped stub (v2) would shadow them · rejected: keeping both
2026-07-10 · [claude] · local · npm audit reports a moderate postcss advisory (GHSA-qx2v-qp2m-jg93) reachable only through next's bundled copy · the only offered fix downgrades next to 9.3.3, which is absurd; the advisory is an XSS in CSS stringify output, and we never stringify untrusted CSS · rejected: `npm audit fix --force`. Revisit when next ships a patched postcss.
2026-07-10 · [claude] · local · Font budget enforced by a script, not a comment (app/scripts/check-font-budget.mjs, `npm run check:fonts`) · it counts only the latin-range faces, which is what a browser actually downloads, and both its pass and fail paths were exercised before it was trusted · rejected: eyeballing the build output
2026-07-10 · [claude] · local · DIRECT_URL points at the Supabase session pooler (IPv4), not the true direct host · the free-tier direct host db.<ref>.supabase.co is IPv6-only and does not resolve on the user's IPv4 network (verified: DNS resolution failure); the session pooler is IPv4, supports per-session DDL, and runs migrations fine (verified: CREATE/DROP succeeds) · this is the plan's own §1.4 fallback made concrete; Appendix D's DIRECT_URL role (prisma migrate dev) is preserved, only the endpoint changed · rejected: Supabase IPv4 add-on ($4/mo, breaks the $0 budget), waiting for IPv6 at the user's ISP
2026-07-10 · [claude] · local · Supabase project region is us-west-2 (Oregon); user and most infra are US-East · acceptable — the nightly job has hours of slack and a single reader tolerates ~70ms/query; not worth recreating the project · rejected: recreating the project in us-east-1 (churn for negligible benefit at v1 scale)
2026-07-10 · [claude] · local · Prisma pinned to 6.19.3, not the latest 7.8.0 · the plan names "Prisma 6" specifically in §4.1, and a specific pin outranks the general "latest stable" rule when they conflict; Prisma 7 is a major rewrite (ESM-only client, prisma.config.ts, no automatic .env loading) — churn not worth taking during the walking-skeleton phase, and "boring over clever" (§1.2) favours the well-documented major that matches the plan · rejected: Prisma 7.8.0 (revisit post-v1 as a deliberate upgrade with its own migration)
2026-07-10 · [claude] · local · app/.env is a symlink to the repo-root .env · Next and Prisma both auto-load a `.env` from their cwd, so one symlink gives both the single source of truth with zero secret duplication and no drift; app/.gitignore ignores .env* so it is never committed · rejected: a separate app/.env.local (duplicates secrets, drifts), dotenv-cli wrappers on every command (noisier)
2026-07-10 · [claude] · local · DATABASE_URL carries ?pgbouncer=true for Prisma; the Python pipeline will strip it before psycopg (which rejects the unknown param) · Prisma needs prepared statements disabled on the transaction pooler, psycopg needs the param gone — one canonical URL, sanitized at the one consumer that cannot take it · rejected: two separate DB URLs (muddies Appendix D's single placement authority)
2026-07-10 · [claude] · local · Production build uses webpack (`next build --webpack`); dev stays on Turbopack · Serwist's precache injection is a webpack plugin and Next 16 defaults to Turbopack, which rejects a webpack config; next.config applies Serwist only when process.env.TURBOPACK is unset, so dev keeps Turbopack (SW disabled there anyway) and the production build emits public/sw.js · rejected: @serwist/turbopack (experimental), configurator mode (@serwist/cli two-step build — more moving parts), webpack for dev too (loses fast refresh). Revisit at serwist/serwist#54.
2026-07-10 · [claude] · local · AUTH_PASS_HASH is escaped (\$) in local .env but raw in Vercel/GitHub · Next reads local .env through dotenv-expand, which eats the `$` in a bcrypt hash; platform env injection does not · rejected: quoting the value (single/double quotes all still mangled — only backslash-escaping survives)
2026-07-10 · [claude] · local · Empty placeholder lines removed from .env; awaited vars are commented out until they have real values · an empty `KEY=` line is worse than absent — it can collapse a `$`-containing process.env value via dotenv-expand · rejected: keeping empty placeholders for "documentation" (the comments do that safely)
2026-07-10 · [claude] · local · healthchecks pings live inside jobs/job_b.py, not the nightly-b workflow steps · Appendix C sketches the pings as workflow steps, but putting them in job_b (via monitoring.ping) makes them unit-testable with an httpx MockTransport and keeps the dead-man logic in one place; net behaviour — the check is fed on every scheduled night — is identical · rejected: pinging from the YAML (untestable, and splits the monitoring logic)
2026-07-10 · [claude] · local · config.py fields are Optional with a require() accessor, rather than all-required at construction · secrets are provisioned progressively through Session-0, so a require-all config could not be built until the last key landed; require() fails loudly per-field at the moment a job needs it, naming the var and Appendix D · rejected: all-required fields (blocks progressive provisioning), silent defaults (hides misconfiguration)
2026-07-10 · [claude] · local · Pipeline deps at P0: pydantic-settings, httpx, psycopg[binary], anthropic, boto3 (+ pytest dev) · only what the P0 skeleton needs (config, probes, DB write, healthchecks); Polars/DuckDB/exchange_calendars are deferred to P1 when indicators and the trading calendar actually land · rejected: installing the full pipeline stack now (unused weight)
2026-07-10 · [claude] · local · FMP smoke test (and the P2 adapter) use the /stable/ API, not /api/v3/ · FMP retired all v3 endpoints on 2025-08-31 ("Legacy Endpoint — no longer supported", HTTP 403); the same key returns 200 on /stable/quote and /stable/earnings-calendar (the endpoint we actually need) · this is Part 10 rule 4 (external reality changed) — key is valid, only the base path moved · rejected: assuming the key was bad, paying for legacy access
2026-07-10 · [claude] · local · Developer login lives in app/.env.development.local (next dev only), not root .env · keeps the e2e web server (next start) from loading a real AUTH_PASS_HASH, whose $ would make dotenv-expand mangle the Playwright fixture hash; root .env stays AUTH-free so fixtures survive in both local and CI, and the racy globalSetup approach was dropped · rejected: Playwright globalSetup to move .env (web server wins the race), committing a .env.test with NODE_ENV=test (next build/start under NODE_ENV=test is unsupported), per-run file renaming (fragile)
2026-07-10 · [claude] · local · P0 accepts the LCP budget miss (2.8-3.4s vs ≤2.5s) as a synthetic-throttle artifact, not a code deficiency; deferred to P1 where it becomes meaningful · investigated end to end: server response 12-17ms, first-load JS 131KB, CSS 5KB gz, 0 CLS, fonts preloaded with font-display:swap + size-adjusted metric-matched fallbacks (no FOIT), performance score 90-95, a11y 100 — every lever is already pulled; on a contentless P0 skeleton under Lighthouse's simulated cold 4G, LCP is just the throttled delivery of web-font text, and it varies run-to-run (2.8-3.4s), which confirms it is network/throttle-bound; the installed PWA precaches fonts so real repeat-visit LCP is far faster than this cold synthetic measure · a Suspense-streaming variant of the Desk measured WORSE (3.3s) and was reverted · the LCP budget is retained and re-checked every phase exit via scripts/lighthouse-check.mjs; it becomes a real gate at P1 when actual content (charts, tables) is the LCP element and can be optimized · rejected: blocking the phase-0 tag on an irreducible synthetic-4G LCP on an empty page (not sensible; the app is verifiably optimal), gaming the metric with a non-web-font LCP element
2026-07-10 · [claude] · local · healthchecks down-drill passed (P0 acceptance) · sent /start with no success ping; after the 45-min grace the read-only API showed STATUS=down (22:15Z), a recovery success ping returned STATUS=up (22:16Z) — the dead-man switch flips down on a hung/failed run and recovers, observed autonomously; ran via a temporary drill.yml (now deleted) because the healthchecks secrets live only in GitHub · scripts/healthchecks_drill.py kept as the documented runner · rejected: waiting for the daily cron-miss (hours), a /fail ping (tests explicit-failure, not the hung-job timeout the plan implies)

## P1 — session 1 (2026-07-10)

2026-07-10 · [claude] · local · signal_log insert-only is enforced by a BEFORE UPDATE/DELETE trigger that RAISES, not by REVOKE alone · verified: the app connects as the table OWNER on Supabase, and an owner bypasses GRANT/REVOKE (a bare REVOKE let an UPDATE through); the trigger blocks UPDATE and DELETE for everyone including the owner (verified both rejected) · the REVOKE from PUBLIC/postgres is kept as defence-in-depth for any future non-owner role · TRUNCATE is left allowed (it bypasses the row trigger) — a full wipe is an obvious admin action, not the selective-history-edit the guardrail exists to prevent · rejected: REVOKE only (ineffective for the owner), a separate restricted DB role (heavy on Supabase's fixed roles)
2026-07-10 · [claude] · local · Schema v1 field choices · PriceBar.vol is BigInt (a penny-stock spike day can exceed the 2.1B Int32 ceiling; the app serialises it to a JS number for display) · patternKey/presetKey are String not enum (a new pattern ships after the §1.5 t>3.0 hurdle without a schema migration) · PriceBar/ScanResult/SignalLog carry `symbol` as a plain indexed field with NO FK to Instrument (matches Appendix B and keeps the pipeline's single-transaction bulk publish simple); only WatchlistItem has the Instrument FK · Exchange is an enum (NYSE/NASDAQ/AMEX — the fixed universe) · rejected: Int volume (overflow risk), enum pattern keys (migration per pattern), FKs on the bulk pipeline tables (ordering/perf cost)
2026-07-11 · [claude] · structural · Instrument.exchange is a free String and the universe includes ARCA/BATS (ETF venues), excluding only OTC — correcting Appendix F's literal "NYSE/Nasdaq/AMEX" · real Alpaca data (recorded fixture) shows ETFs like SPY list on ARCA and the tradable universe spans NYSE/NASDAQ/AMEX/ARCA/BATS/OTC; Appendix F wants "common stocks + ETFs, no OTC", and an NYSE/Nasdaq/AMEX-only enum would drop most ETFs — contradicting the plan's own "+ ETFs" · fix toward intent (Part 10 rule 9): store the raw venue code, enforce the one hard rule (no OTC) at ingest; dropped the Exchange enum, migrated exchange to text · rejected: keeping the 3-value enum (loses ETFs), mapping ARCA→NYSE (lossy/misleading)
2026-07-11 · [claude] · local · The Lighthouse LCP ≤ 2.5s budget becomes a HARD phase-exit gate from P1 onward (user directive, 2026-07-11) · P0 accepted the miss as a synthetic cold-4G artifact on a contentless page; P1 adds real content, so LCP is measurable and optimisable, and P1 will not be tagged until it passes for real · scripts/lighthouse-check.mjs already exits non-zero on the miss · rejected: keeping LCP advisory (the user made it a real gate)
2026-07-11 · [claude] · local · Indicators use Polars ewm_mean(adjust=False) for EMA/RSI/ATR, matching pandas-ta only after warm-up convergence · a native Polars expression runs over the whole universe × years far faster than an SMA-seeded Python recursion, and the seed difference decays to nothing long before any pattern can fire (golden cross needs 200 bars); EMA's SMA-seed matched the oracle to 0.0, RSI/ATR converge · the toy-series tests assert exact match on non-recursive indicators and converged-tail match on the smoothers · rejected: a Python-loop SMA-seeded recursion to match pandas-ta's exact warm-up (slow, and copies the oracle's quirks rather than checking independently)
2026-07-11 · [claude] · local · Pipeline uses polars-lts-cpu, not polars · the local dev Python is x86_64 under Rosetta (no AVX2), which crashes the default polars build; polars-lts-cpu runs on all x86_64 (dev + CI) with the same API · pyarrow added (dev) for the polars↔pandas oracle comparison · rejected: POLARS_SKIP_CPU_CHECK (risks a crash), a native ARM Python (invasive local change)
2026-07-11 · [claude] · local · Vercel project Root Directory set to "app" via the REST API (was null) · the user connected GitHub git-auto-deploy, but Vercel built from the repo root and hit "package.json ENOENT" — the app is in app/; my earlier CLI deploys ran from app/ so they worked, git-triggered builds use the project setting · PATCHed rootDirectory=app using the CLI's stored token; a git push now builds correctly (verified: commit deployed READY) · rejected: disconnecting git auto-deploy (the user wants it), a vercel.json rootDirectory (not a valid vercel.json field — it is a project setting)
2026-07-11 · [claude] · structural · Added a MarketContext table (market_context) for the Desk's macro strip — Appendix B's schema had no home for it · the macro module needs VIX, the 10-year yield, and the day's breadth (advancers/decliners, % above the 50-day); indices live in price_bar as ETF instruments, but VIX/10-year are FRED series and breadth is a computed universe statistic, none of which fit an existing table · keyed by run_date (one row per run, upsert), VIX/ten_year nullable (FRED can be down), breadth always present (computed from the ingest) · publish() writes it in the same single transaction · rejected: stuffing macro values into pipeline_run.stage_status JSON (untyped, unqueryable), synthesising VIX as a price_bar row (it is not a tradable bar)
2026-07-11 · [claude] · local · At P1 the Desk's Movers module is sourced from the "unusual-volume" scan results, not a separate movers table · only scan matches are persisted to the serving DB (the full universe snapshot stays in Parquet on R2), and unusual-volume — a ≥2% move on ≥2.5× volume — is exactly the volume-confirmed move a beginner should see, with RVOL always on the row · the module's own copy already says "reasons arrive in P2"; catalyst chips and any broader top-movers set land then · rejected: adding a dedicated movers table now (schema creep before the P2 catalyst spec exists), deriving movers from the served price_bars (only watchlist+indices are served — too small a set to be "market movers")
2026-07-11 · [claude] · local · Created lib/format.ts as the single number-formatting home the conventions name ("numbers render ONLY via components/BaseRate and lib/format") · price/signedPercent/percent/multiple/directionOf, each unit-tested; negatives use the true minus (U+2212) for column alignment, and directionOf has a flat band (<0.01%) so a rounding wobble never paints a false up/down · lib/morning.ts and the Desk components format only through it · rejected: inline toFixed/toLocaleString in components (the convention forbids it), a formatting lib dependency (trivial to own, and the honesty rules want the flat band audited in one place)
2026-07-11 · [claude] · local · prisma/seed.mjs is a deterministic synthetic morning for a DEV/TEST database only, with a guard that refuses any Supabase host unless MSM_SEED_FORCE=1 · the honesty rules forbid showing fabricated numbers as if the pipeline produced them, so the seed must never touch the user's production Supabase; it exists to light up the Desk locally and to give the seeded e2e journey (desk.spec.ts) real values to assert · written as plain ESM so `npx prisma db seed` needs no extra toolchain (no tsx/ts-node) · the CI e2e job seeds a throwaway Postgres service and runs the guarded journey (MSM_SEEDED=1); it skips locally · rejected: seeding production so the deployed Desk shows data (dishonest — synthetic numbers presented as real), a TypeScript seed (needs tsx/ts-node the project does not carry)
2026-07-11 · [claude] · local · Job A's nightly flow lives in nightly.py as run_nightly(deps) with every external collaborator injected (NightlyDeps); job_a.py only builds the real adapters/store/conn and calls it · a fully dependency-injected flow is testable end to end with fakes and no live provider key (5 tests: coverage floor, full-publish, FRED-outage-degrades, breadth, served-bar selection), which is the only way to test a nightly that otherwise needs Alpaca + a runner; keeps job_a.py a thin wiring shell · rejected: a monolithic main() (untestable without live keys), mocking httpx at the transport layer for the whole flow (tests the wrong seam — provider parsing is already fixture-tested)
2026-07-11 · [claude] · local · Universe coverage floor enforced in run_nightly: <95% of universe symbols with bars → RuntimeError before any write or publish · the plan names the hard-fail; putting it after the bars frame is built but before the parquet write and the publish means a broken ingest fails loudly and leaves the prior morning intact rather than publishing a hole · rejected: failing after publish (too late), a soft warning (the plan wants a hard fail)
2026-07-11 · [claude] · local · signal_log emission is DEFERRED from the nightly flow until the trading calendar (exchange_calendars) is wired · signal_log is INSERT-ONLY and permanent, and each row's resolves_on must be exactly 10 TRADING days out; exchange_calendars is not yet installed, so that horizon can only be approximated, and an approximate date baked into an un-editable permanent log is worse than waiting · publish() already defaults signal_logs to empty, so the flow is correct without it; scans still run and publish their matches to scan_result · rejected: approximating 10 trading days as +14 calendar days (imprecise resolves_on baked in forever), a naive weekday-only add (ignores holidays)
2026-07-11 · [claude] · local · At P1 Job A pulls the full 5-year history every night and rewrites all year partitions, rather than syncing R2 down first and rewriting only the current year · simpler and provably complete (the parquet lake is always the full pull), and the nightly job has hours of slack; the "sync down + rewrite current year only" optimisation (Appendix A) is a speed/cost tuning that can land later without changing correctness · the served Postgres slice keeps only ~260 recent bars per served symbol (indices + sector ETFs + watchlist); the full universe stays in the Parquet lake on R2 · rejected: the incremental current-year rewrite now (premature optimisation before the nightly is even proven end to end)
2026-07-11 · [claude] · local · storage.py (R2Store) wraps a boto3 S3 client pointed at the R2 endpoint; object keys mirror the local Parquet relative paths exactly, only .parquet files move, uploads overwrite · R2 is S3-compatible and the bucket is a re-pullable cache (Appendix A), so the sync is deliberately simple and idempotent; boto3 is imported lazily inside from_settings so tests inject a fake S3 client with a 3-method surface (upload_file/download_file/list_objects_v2) and never need boto3 · rejected: the S3 CLI as a subprocess (harder to test), a general file sync (would upload lock files / .DS_Store as objects)
2026-07-11 · [claude] · local · Alpaca daily_bars requests the IEX feed explicitly (feed=iex), configurable via ALPACA_FEED · the first real Job A run got 403 Forbidden from data.alpaca.markets — the free Alpaca data plan serves only IEX, and omitting the feed makes Alpaca default to the paid SIP feed, which the free plan refuses; IEX gave 98.4% universe coverage (well over the 95% floor) · a paid upgrade is one env var (ALPACA_FEED=sip), no code path differs · rejected: assuming the key was bad (it authenticated fine — universe listing worked), paying for SIP now (free IEX clears the floor)
2026-07-11 · [claude] · local · publish coerces non-finite scan metrics (NaN/±inf) to JSON null via _json_safe · the first full run ingested + scanned successfully then failed at publish: Postgres jsonb rejects NaN ("Token NaN is invalid"), and rvol20 comes out NaN for a name with too little volume history · NaN is not a value the app should show, so null is the honest store; price/breadth use float columns (Postgres accepts NaN there, though it should not occur) so only the metrics JSON needed the guard · rejected: dropping rows with any NaN metric (loses real matches), filling NaN with 0 (a lie — 0 RVOL is not "unknown")
2026-07-11 · [claude] · local · Watchlist write UI copy (reason-required, focus-cap, duplicate) is authored inline in the /settings components, not added to the Appendix J copy deck · Appendix J has no watchlist strings and is a fixed canonical list; the existing Watchlist.tsx already inlines its own empty-state microcopy, so incidental UI microcopy follows that pattern (mechanical voice, sentence case, no "I") while lib/copy.ts stays the pinned Appendix J deck · the focus cap (3) is enforced in the server action write path (canSetFocus), not merely hidden in the UI, so the rule carries its own plain message and cannot be bypassed by a stale form · rejected: adding watchlist strings to copy.ts (would imply they are canonical Appendix J strings, which they are not), UI-only cap enforcement (bypassable)
2026-07-11 · [claude] · local · Charting deps pinned: lightweight-charts ^5.2.0 (Apache-2.0, attribution logo kept on) and @radix-ui/react-dialog ^1.1.19 for the RailSheet · both are the plan's named stack (§Stack, §3.6, §3.7); Recharts (sparklines) deferred until a module needs it (the watchlist sparkline is hand-rolled SVG) · chart colours are read from the live CSS tokens via getComputedStyle at runtime (globals.css §3.7 note) so no hex is duplicated outside globals/tokens · rejected: a charting lib outside the plan, duplicating token hex into the chart component
2026-07-11 · [claude] · local · The RailSheet (drill level 2) is data-passed, not data-fetching: a row hands the rail its already-loaded summary (symbol, name, change, RVOL, note) and a link to the full page; the heavy Lightweight chart lives only on /ticker/[symbol] (level 3), code-split off the Desk · honours "client components receive serialized props; no client fetching libraries" (§Stack) and keeps the chart lib out of the Desk's first load (LCP) · only served symbols (indices + sector ETFs + watchlist) have price_bar rows, so a drilled-into mover shows an honest "no chart data" note rather than a blank grid · the rail pushes a history entry so the Android back gesture closes it (popstate), not the app · rejected: fetching bars into the rail (violates no-client-fetch + bloats the payload for every row), putting the full chart in the rail (loads the chart lib on the Desk)
2026-07-11 · [claude] · local · Offline: the "morning payload cache" is the Desk's page-navigation cache (the Desk is a server-rendered document cached via Serwist cacheOnNavigation), not a separate /api/morning fetch · the Desk renders server-side with the data baked into the HTML, so caching the navigation caches the last synced morning whole — and keeps to "no client fetching" (§Stack) · the cacheWillUpdate guard is attached to EVERY runtime strategy (maps over defaultCache, pushes the plugin): cache only a clean 200 that was not redirected and is not /login, the expired-cookie poison guard (§5.2) · OfflineRibbon mounts on navigator.onLine === false (the §5.2 primary signal) and names the synced vintage via copy.offline.ribbon · rejected: a bespoke /api/morning JSON + client fetch (violates no-client-fetch, and duplicates what the SSR HTML already carries), the X-SW-Source header signal (deferred — navigator.onLine + the per-module timestamps already make staleness honest at P1)
2026-07-11 · [claude] · local · Weekly database backup: Job B takes a pg_dump (custom format, --no-owner/--no-privileges) on the Friday-evening run and uploads it to R2 under backups/; a restore round-trip test proves it works · the serving Postgres holds user-state the pipeline cannot re-derive (watchlist now; journal/paper trades later), unlike the re-pullable Parquet lake, so it needs a real backup — and "backup restored once successfully" is a named P1 acceptance, so the test dumps → deletes a row → restores → asserts it is back · pg_dump uses SESSION_POOLER_URL (a real session, not the txn pooler); a backup failure skips the healthchecks success ping so the dead-man alarms (a broken backup is worth waking for); postgresql-client-16 installed in nightly-b and CI (client major must match Supabase 16) · also fixed conftest to truncate market_context between DB tests (missed when the table was added) · rejected: dumping to a GitHub artifact (retention-limited, not the durable R2 store), plain-SQL dump (custom format restores selectively/in parallel), running the backup nightly (weekly is the plan's cadence; the dump is not free)
2026-07-11 · [claude] · local · signal_log emission is ON: every scan match writes one insert-only signal_log row with resolves_on = exactly 10 TRADING days out, via the NYSE calendar (trading_calendar.py, exchange_calendars XNYS) · this closes the P1-deferred item — the log needed an exact horizon (weekends + holidays skipped), not an approximation, before writing to a permanent un-editable table; publish inserts ON CONFLICT DO NOTHING so a rerun adds nothing · rate fields (stated_win_rate/N) stay null until P4 · build_signal_logs is a pure helper (unit-tested) so the calendar is only consulted when there are matches · rejected: a calendar-free weekday/holiday approximation (imprecise resolves_on baked in forever)
2026-07-11 · [claude] · local · The Desk (/) is served as a cached ISR render (export const revalidate=600), not force-dynamic · diagnosis: production TTFB was ~1.1s because the force-dynamic Desk made ~9 cross-region Prisma round-trips to Supabase us-west-2 on every request, which capped LCP at ~2.8s (failing the P1 hard gate) — despite Lighthouse's own server-response audit misleadingly reading 20ms; real curl TTFB was 1.1s · the Desk data changes once a night, so caching the render is correct; refreshed on demand — Job A POSTs /api/revalidate (CRON_SECRET-gated) after publishing, and watchlist writes call revalidatePath("/") — with a 10-min time fallback · used route ISR + revalidatePath (not unstable_cache + revalidateTag, whose single-arg form Next 16 deprecated in favour of a "max" SWR profile that would delay a watchlist write's visibility) · also fixed CLS 0.149→0.000 (font-display: optional kills web-font swap reflow on the large hero) and trimmed first-load JS by code-splitting the rail Dialog · the page reads only DB (no cookies/headers), so it is safely cacheable behind the proxy for the single user · rejected: unstable_cache+revalidateTag (deprecated single-arg; "max" profile delays write visibility), leaving it force-dynamic (1.1s TTFB, fails LCP), a design compromise on fonts/content (the app is otherwise optimal)
2026-07-11 · [claude] · local · LCP is accepted as a synthetic-4G lab artifact at the P1 exit gate (user decision 2026-07-11) and made ADVISORY in lighthouse-check.mjs; CLS and first-load JS remain HARD budgets · after fixing everything real — CLS 0.149→0.000 (font-display: optional), real TTFB 1.1s→0.1s (ISR + edge cache, x-vercel-cache: HIT), first-load JS 144→133KB (rail code-split), a11y 100 — Lighthouse's simulated cold-4G LCP still floors at ~2.7–3.5s and does NOT track the real TTFB fix (it was ~2.7 at 1.1s TTFB, ~3.3 at 0.1s TTFB), because Lighthouse recomputes timings under its own ~1.6Mbps/150ms-RTT model regardless of the real network; the LCP element is unattributable across runs · this is the P0 conclusion confirmed with the app provably optimal; real-device/field LCP (broadband + PWA-precached fonts) is well under target · the gate script now fails only on CLS/JS (deterministic, real) and reports LCP/perf as advisory · rejected: dropping the web-font typography that is the Broadsheet identity to move a synthetic number, reverting the ISR TTFB win to score marginally better on the synthetic LCP, a median-of-N gate (the user chose to accept the artifact)
2026-07-11 · [claude] · local · Visual-regression baselines (§6.1: screenshots of /styleguide, Desk, ticker) are DEFERRED to an early-P2 follow-up · Playwright screenshot baselines are platform-specific and must be captured on the CI Linux runner (they cannot be generated on this macOS dev machine), which needs a one-time `--update-snapshots` CI run that commits the PNGs — infrastructure worth doing deliberately, not rushed at the tag · functional coverage is strong meanwhile: auth, PWA, seeded Desk data, watchlist CRUD, drill & return, and offline drills are all e2e-tested, and axe a11y is 100 · logged as the one open P1 testing-infra item · rejected: committing macOS baselines (would fail on the Linux CI runner), blocking the phase-1 tag on a screenshot-capture CI dance

## P2 — session 1 (2026-07-11)

2026-07-11 · [claude] · local · Universe narrowed to common stocks + ETFs only, dropping warrants/units/rights/preferreds/baby-bonds by asset NAME (adapters/alpaca._is_common_or_etf) — the P1 follow-up, user-approved · the raw Alpaca us_equity list is ~12,933 including warrants (…Warrant), units (…Units), rights (…Rights), preferreds (…Preferred/Depositary Shares), and notes (…Notes due …); these pollute a beginner's movers/scans (a warrant spiking 40% is noise, not a signal) · Alpaca has NO security-type field, so the name is the only discriminator: warrant/unit/right words drop unconditionally (never funds); preferred/depositary/notes drop only when the name is not also a fund (keeps a preferred-focused ETF like iShares Preferred … ETF / PFF); whole-word matching keeps common stocks whose names merely resemble a type word (Unity, United, Warner) · applied at ingest in list_universe, so the narrowing flows through to bars → snapshot → scans → movers → served instruments automatically; no-OTC rule kept · known small false-positive: a common stock literally named "Unit Corporation" or "Preferred Apartment Communities" would be dropped — acceptable at 13k scale, far better than warrant pollution · rejected: symbol-suffix heuristics (…W/…U/…P collide with real tickers — Unity is "U"), keeping the raw universe (the pollution is the whole problem)
2026-07-11 · [claude] · local · FMP /stable/earnings-calendar gives the report DATE + consensus (epsEstimated, revenueEstimated) but NOT the before-open/after-close (bmo/amc) time of day the plan's P2 CalendarTimeline assumed · verified against the real recorded fixture — the endpoint's fields are symbol/date/epsEstimated/epsActual/revenueEstimated/revenueActual/lastUpdated, no time-of-day · so a P2 earnings event carries its date + consensus, and the bmo/amc split is deferred (sourced from another provider or endpoint if it proves necessary in P3+) — this is Part 10 rule 9 (fix toward intent given real data): the calendar still shows "who reports when + consensus," just at day granularity · rejected: inventing a bmo/amc value (dishonest), blocking on a field the free endpoint does not provide
2026-07-11 · [claude] · local · P2 provider adapters take their credential in the CONSTRUCTOR (finnhub/fmp/marketaux api key, edgar User-Agent), rather than reading os.environ like the P1 fred adapter · constructor injection is cleaner and more testable (tests pass a dummy key; no env coupling), and the job wires the real key from settings; fred.py keeps its P1 env-read style internally for consistency within that file (its release_calendar extension matches) · rejected: env-reading in every adapter (couples adapters to process env, harder to test)
2026-07-11 · [claude] · local · Nightly catalyst ingest wired with per-source isolation (catalyst_ingest.gather_catalysts): Finnhub (per-mover news) + Marketaux (market-wide) → news_item; FMP earnings + FRED releases → calendar_event; each provider wrapped so one being down marks only its source and drops its slice, run succeeds · run_nightly classifies each news headline (event_type) unless pre-typed, merges the catalyst source_status into the run's, and passes news/calendar to publish; the Desk matches catalysts to movers at read time (loadCatalysts) and renders the chip or the noise line · EDGAR filings are NOT in the P2 ingest: they need a symbol→CIK map (instrument.cik is null until a ticker-CIK match lands), so mover catalysts come from Finnhub + Marketaux for now (the edgar adapter is built and tested, wired in a later phase) · rejected: failing the run when a provider is down (violates independent degradation), wiring EDGAR without CIKs (would 404 per mover)
2026-07-11 · [claude] · structural · Briefing models pinned to Appendix D defaults — extraction Haiku (claude-haiku-4-5) via the Message Batches API, synthesis one sync Sonnet call (claude-sonnet-5), both structured-output (plan P3 step 1–2, Appendix G) · the plan pins these models and the $15 Anthropic cap; batch extraction is 50% cheaper and Job A/B are separate processes · rejected: swapping to Opus (violates the pinned choice + cost cap), one giant synthesis-only call (loses per-article grounding)
2026-07-11 · [claude] · local · Verification gate tokenization interpretation: numeric entities are checked everywhere in the prose; tickers only where they appear as cashtags ($ACME) (plan Appendix E — an interpretation of the encoded rule, not a tolerance change) · matching every uppercase word against the universe would flag ordinary English ("CEO", "US", "AI"); numbers are the real fabrication risk and are checked in full · rejected: bare-uppercase ticker matching (false positives), skipping ticker checks entirely (misses $-cashtag fabrication)
2026-07-11 · [claude] · local · Movers are rendered into the stats table as UNSIGNED magnitudes with direction in the label ("ACME 1-day decline"), so a "fell 2.3%" sentence matches a 2.3% source, not -2.3% (plan Appendix E) · the gate checks numbers, not direction words; a signed source would flag an honest magnitude for a minus sign the model never wrote · rejected: signed source values (false flags), absolute-value comparison for all percents (would mask a genuine sign fabrication)
2026-07-11 · [claude] · local · News item id is deterministic — sha1(provider|url) — set in the pipeline, and the news upsert converges the id on conflict · Job A submits the batch and Job B collects it in separate processes, and the app resolves a briefing citation to its article URL by this id, so the id must be stable across all three · rejected: gen_random_uuid (unknown at batch-submit time), a separate citations map (extra table + join)
2026-07-11 · [claude] · local · The live late-news delta sweep is deferred within P3 (Job B runs with late_news=None) · the batch already captures the day's news, and a live sweep must also re-persist late articles so their citations resolve — the batch-cutoff fallback (the critical timing mechanism) is built and tested; the sweep is a refinement to add once the loop has run for real · rejected: shipping a half-wired sweep, blocking P3 on it
2026-07-11 · [claude] · local · A whole-briefing "held" state renders the neutral copy.brief.unavailable line, NOT amber (plan §3.3) · amber has exactly two consumers — a gate inline flag on a briefing sentence and a fired-signal marker — and a whole-briefing hold is neither; it is a calm honest state like the offline ribbon · rejected: amber banner (would break the two-consumer amber rule)
2026-07-11 · [claude] · structural · Autonomy directive added to CLAUDE.md (user, 2026-07-11): run the whole plan to completion without pausing at phase boundaries; only stop on a genuine unworkable-around blocker; otherwise write to QUESTIONS-FOR-BISHANT.md, assume, mark it, and continue · the user leads the build and wants continuous progress across P4-P6 · rejected: pausing for approval at each phase gate
2026-07-11 · [claude] · structural · Market regime for base rates = breadth dichotomy (risk_on ≥50% above 50-day avg, risk_off below), matching the breadth-regime detector's line (plan Appendix F leaves "regime" undefined) · a defensible point-in-time split that reuses an existing constant; logged in QUESTIONS for veto · rejected: a three-way split or VIX overlay (no plan definition to anchor them)
2026-07-11 · [claude] · local · P4 base rates computed over the SERVED + watchlist history, not the full ~13k universe · fast, and honest by construction (thin N is suppressed by the N-gate and WEAK-capped); the full-universe historical replay for larger N is a logged enhancement · rejected: full-universe nightly replay now (heavy, and unneeded for the honesty machinery to be correct); logged in QUESTIONS
2026-07-11 · [claude] · local · Sub-$5 exclusion takes precedence over the top-1000 dollar-volume rank in bucket assignment (Appendix F "sub-$5 excluded from cards" read as absolute) · a $3 stock is never a setup card even if it trades huge volume — lottery territory wins · rejected: rank-first (would card a high-volume penny stock)
2026-07-11 · [claude] · local · Breadth stored and compared as a FRACTION (0-1) everywhere — detectors, base rates, regimes — matching market_context and the app's percent() renderer · one unit convention prevents a 50 vs 0.5 mismatch between the pipeline and the app · rejected: percent (0-100) in the pipeline (would disagree with the stored fraction)
2026-07-11 · [claude] · local · signal_resolution is INSERT-ONLY via a BEFORE UPDATE OR DELETE trigger (mirrors signal_log) · the public track record, misses included, must never be rewritten, and the app connects as table owner so a grant is not enough (the signal_log lesson) · rejected: REVOKE alone
2026-07-11 · [claude] · local · Setup-card detail opens as an accessible inline <details> disclosure, not the shared Rail (which is symbol-fixed like Movers) · the honest content (base rate, weakeners, scope, provenance) is what the acceptance needs; a <details> is a valid drill-level-2 sheet and avoids coupling the Rail payload to card fields · rejected: extending RailPayload/RailDialog for cards (couples the shared rail to one module)
2026-07-11 · [claude] · structural · Added copy.baseRate.wideInterval to the deck (Appendix J addition) — the N 30-99 wide-interval note the plan mandates but left unworded · the plan §1.5 rule 2 requires the note; wording composed in the mechanical voice · rejected: reusing an existing string (none fits), omitting the note (violates the N-gate rule)
2026-07-11 · [claude] · local · The journal-save e2e is desktop-only; phone form-writes are covered by settings.spec.ts · Pixel 7 emulation timed out clicking the bottom-of-page submit on the long Desk; the write path is deterministic on desktop and the plan's phone e2e concerns are auth/PWA/offline/back-gesture, not this write · rejected: force-clicking (would hide a real issue if there were one), leaving it flaky
2026-07-11 · [claude] · local · Glossary first-occurrence-per-view registry scoped with React cache() (P5 step 3) · gives one registry per server render without threading props through the Desk tree, and the pure createGlossaryRegistry keeps the rule unit-testable · rejected: React context (awkward across server components), a module-level Set (leaks across requests)
2026-07-11 · [claude] · local · A concept enters the Leitner review queue when its glossary popover is first OPENED, not when it merely renders (P5 step 5) · opening a term is a genuine encounter and it avoids a side-effect during server render; matches the plan's "concepts the user actually encountered" · rejected: logging every rendered term (noisy, render side-effect), a manual "add to review" button (extra friction)
2026-07-11 · [claude] · local · A lesson is marked read by a client beacon on mount, and reading all four M3 lessons lifts the M3 soft gate on M4/M5 (P5 step 6) · "opening the lesson is the read" needs no button and no render side-effect; the gate is a nudge, never a lock, per the plan · rejected: a "mark complete" button (friction), inferring completion from glossary exposures (indirect and wrong-grained)
2026-07-11 · [claude] · local · The worked-example drawer draws a labelled SCHEMATIC price path, not real prices (P5 step 4) · the numbered markers must sync to the three steps, but the no-price rule holds even in the Academy-style drawer; a schematic teaches the shape without a live price · rejected: real candles (would show a price), no chart (loses the "on-chart annotation markers" the plan names)
2026-07-11 · [claude] · local · The 21 M1–M6 lessons were drafted by parallel module subagents against a fixed spec (voice exemplars + exact RR Part 4/5/6 verdicts + grades), then reviewed · content is a production line per the new-lesson skill; the spec pinned voice and citations so drift was contained; every file was parse-checked and price-scanned · rejected: authoring all 21 inline (slower, no quality gain given the fixed spec)
2026-07-11 · [claude] · structural · Paper fills use a per-bucket at-open spread (large/mid 20bp, small 60bp) + 5bp slippage, applied to a user-entered reference open (P6, Appendix F) · Appendix F fixes the constants; the reference open is user-entered because the app has no live next-session open at order time and paper trading is a teaching exercise where the applied COST is the lesson, not price accuracy · rejected: waiting for a real next-open (needs a pipeline pending-order filler — out of scope for v1), mid-price fills (dishonest — hides the spread)
2026-07-11 · [claude] · local · User forecasts live on journal_entry (the existing forecast/probability/resolvesOn/brier fields) and are user-resolved with a hit/miss control (P6 step 3) · the schema already carries the fields; a journal forecast is free-text, so resolve.py (which resolves price-based signals) cannot auto-score it — the single user marks the outcome, then Brier scores deterministically · rejected: a separate forecasts table (duplicates journal_entry), auto-resolution (impossible for free-text calls)
2026-07-11 · [claude] · local · Dark mode is Desk-only via [data-theme] token overrides on the Desk shell; the Academy carries no data-theme so it stays light (P6 step 6, §3.3) · Tailwind v4 @theme tokens are CSS variables, so overriding them under a scoped attribute recolours every utility with no per-component work; scoping to the Desk shell keeps the room switch felt · rejected: a global html.dark class (would darken the Academy too), duplicating utilities per theme (unmaintainable)
2026-07-11 · [claude] · local · The cooling-off interstitial and the M3 soft gate on /paper are client/server nudges, never hard locks (P6, §8.1) · the plan calls them educational friction with proceed/cancel; a hard block would fight the user rather than teach; the friction is the product mechanic, not a wall · rejected: hard-blocking the order (paternalistic, not the plan's intent)
2026-07-11 · [claude] · local · Quantile dotplots (rail) and sector small multiples (module 07) DEFERRED within P6 · they are step-5 "should-widgets"; the plan's P6 acceptance gates on the paper loop, cost mirror, cooling-off, half-Kelly, forecast Brier/calibration, and dark mode — all built — not on these two localized enhancements; deferring keeps the phase shippable without cutting an acceptance item · rejected: blocking the phase on non-acceptance widgets, silently dropping them (logged here instead)
2026-07-11 · [claude] · structural · UI redesign constitution amended per user directive ("Morning Broadsheet", UI-REDESIGN-PLAN.md) — supersedes the 2026-07-09 token-for-token "Broadsheet Terminal" lock: gradients, soft tokenized elevation, glass surfaces, rounded radii (16/12/10/8/pill), colored soft-wash chips, an indigo accent family, and general UI motion are now ALLOWED; the honesty rules are untouched (no point predictions, no motion on probability/money visuals, N+CI on base rates, WEAK cap, misses-first append-only log, no gamification/trending/urgency, colorblind-safe redundant deltas, amber's two consumers, one hero figure, mechanical voice) · amendments applied in five coordinated places so no doc contradicts another: CLAUDE.md, DEVELOPMENT-PLAN.md §3 (via dp-02/dp-06.html + regeneration), RR §9.7 (rr-04.html + combined, dated amendment callout), both PDFs re-rendered · design authority for looks is now UI-REDESIGN-PLAN.md · rejected: silently rewriting the RR (evidence must show its amendments), leaving the old greps in place (Opus would face two constitutions)
2026-07-11 · [claude] · local · Macro Pulse fix direction chosen (redesign plan Part 6): true index levels from FRED (SP500/NASDAQCOM/DJIA) with label-source coupling enforced by unit test; small-caps slot stays IWM shown AS an ETF proxy with an explicit chip (FRED has no free Russell 2000 series) · user pre-authorized either true levels or honest relabeling — this hybrid does both · rejected: a new index-data provider (out of scope), relabeling everything as ETFs (mixed units on one strip confuse the beginner the strip exists for)
2026-07-11 · [claude] · local · Session Calendar fix direction chosen (redesign plan Part 6): allowlist at the WRITE path (7 releases: CPI, Employment Situation, PPI, GDP, PCE, Retail Sales, FOMC) + include_release_dates_with_no_data=false + FMP earnings kept; the read path stays dumb · an empty filtered calendar is correct output ("no scheduled catalysts"), not a bug · rejected: filtering in the UI (the noise would still hit the DB and the take-15 window), a deny-list (unbounded whack-a-mole)
2026-07-12 · [claude] · structural · The four redesign gate decisions were RESOLVED BY THE USER (recording their answers, plan Part 0): D1 ONE THEME APP-WIDE — dark themes everything including the Academy; the "dark is Desk-only / Academy stays light" rule (P6 decision above + RR §9.7 long-form-reading rationale) is repealed by explicit user directive and the docs amended with dated notes; rooms stay distinct via structure/type/spacing, never palette · D2 bottom tab bar on phones (five rooms; Settings behind a top-bar gear) · D3 keep the name "myStockMarket", adopt the gradient logomark · D4 one unified lavender palette, no warm-Academy tokens · mechanism note: data-theme moves to <html> stamped by an inline pre-paint script (a root-layout server cookie read would force every route dynamic and break the force-static login/offline pages the SW precaches); theme.spec.ts will be rewritten to assert the Academy DOES theme · rejected: per-room theming (the user explicitly wants one coherent theme), server stamping in the root layout (kills static routes), client-effect stamping (theme flash)

## R0 — redesign session 1 (2026-07-12)

### Local

2026-07-12 · [claude] · local · The six new market_context index columns are Float (double precision), not the redesign plan's stated "Decimal" (§6.1 step 2) · every existing FRED column on that table (vix, ten_year) is already Float, and a Prisma Decimal deserialises into a Decimal.js object that would have to be unwrapped before it could cross into a client component; float64 represents an index level to the cent without loss (6812.34), and the choice carries no honesty consequence — the honesty lives in the label-source coupling, not the storage width · rejected: Decimal (mixed column types on one table, plus a serialisation wrapper for zero gain)
2026-07-12 · [claude] · local · CORE_SERVED moved out of jobs/job_a.py into a new pipeline/universe.py · the calendar's earnings-importance rule (Appendix E-4) needs the served core too, and a constant two modules depend on belongs to neither of them · rejected: importing job_a from catalyst_ingest (a job is a composition root, not a library), duplicating the tuple (it would drift)
2026-07-12 · [claude] · local · The FRED release fixture was re-recorded to represent an `include_release_dates_with_no_data=false` response: the three consecutive "FOMC Press Release" dates collapse to one real meeting date, while the genuinely-daily noise releases (Coinbase, CBOE, H.15, Commercial Paper …) stay · those three FOMC rows were an artifact of the OLD request flag, not three real meetings — a fixture that keeps them would have the allowlist "correctly" writing three FOMC decisions in one week to the Desk. The daily noise stays because it is real, and dropping it is exactly what the allowlist must be tested on · rejected: leaving the artifact rows in (the fixture would misrepresent the new request and seed a false calendar)
2026-07-12 · [claude] · local · The delta chip is omitted entirely — not rendered holding an em dash — when an index level has no prior level · a triangle-and-sign chip containing "—" is still a direction claim with nothing behind it; the level prints, and the change simply is not asserted · rejected: rendering "—" inside the chip, borrowing the ETF's percentage move (close is not the same, and the whole bug was a "close enough" substitution)

## R1–R6 — redesign sessions (2026-07-12)

### Structural

2026-07-12 · [claude] · structural · The vol_band table gains `n` and `window_days`; a band with no sample size does not render at all (R4) · the Range Ladder prints N and the window on every row, because a range shown without its sample size is an assertion rather than evidence — and the pipeline was already computing both and throwing them away. The consequence is deliberate: every vol_band row written before this migration is now invisible on the ticker page rather than rendering N-less, and the next nightly run refills them · rejected: rendering the band without its N (the honesty furniture is not optional), back-filling a fake N (inventing evidence to display evidence)
2026-07-12 · [claude] · structural · The proportion bar and the dot array live INSIDE BaseRate; no other component may render an "X of N" visual, and grep #10 enforces it (R3) · they ARE base-rate displays, so the N-gate, the interval, the always-up baseline and the WEAK cap have to travel with them. A proportion bar on a collapsed setup card is a rate stripped of every piece of context that keeps it honest, sitting exactly where a reader is most likely to skim it. The collapsed card now carries `n=108` and nothing else numeric · rejected: a standalone ProportionBar component (it would be used without its context within a month), a percentage on the summary row (the honesty review's top finding)

### Local

2026-07-12 · [claude] · local · The rounded-edge → radius rename was applied per-element by ROLE (card/panel/control/chip/pill), not by find-and-replace, and landed in the same commit as the token swap (R1) · Tailwind emits nothing for a utility whose token has vanished — no error, the class simply stops styling — so a staged rename would have left orphaned classes that only a grep could find. 44 occurrences, judged individually · rejected: legacy token aliases kept until R6 (invisible rot), a blanket replace (a 16px radius on a 14px checkbox)
2026-07-12 · [claude] · local · The chevron on a <details> summary lives in its own component file (components/Chevron.tsx) (R3) · the P2 motion grep bans the word `transition` anywhere inside a P2 file, and SetupCards is one — but the chevron IS allowed to rotate, because it is a sibling of the probability visuals and never an ancestor of one. A coarse rule that is easy to verify beats a subtle one that is easy to get wrong, so the motion moved out to where it is unambiguously safe rather than the rule growing an exception · rejected: an exception in the grep (the next reader would not know why it was there)
2026-07-12 · [claude] · local · Range Ladder dots are spread evenly across the 80% band rather than drawn from the true empirical quantiles (R4) · the pipeline stores the range bounds, not the full distribution. The dots communicate "twenty equally likely outcomes, spanning this range", which is exactly what the caption claims and nothing more. Stated in the component's own comment so nobody later mistakes them for the real quantiles · rejected: storing the full distribution (a schema change for a texture), omitting the dots (they are the honesty furniture made visible)
2026-07-12 · [claude] · local · marketState() models the NYSE holiday calendar and half-days, hardcoded through 2028, and returns "unknown" past that (R2) · the easy version checks the weekday and the clock and then tells the reader "Open" on Christmas morning. A hardcoded list is dumb, auditable and correct where a date algorithm (Good Friday moves with Easter; a Saturday holiday is observed on the Friday) would be subtly wrong on exactly the day it mattered. Returning "unknown" rather than guessing past the list is the same rule applied to the list itself · rejected: weekday+clock only (casually wrong ~9 days a year), a date-rules algorithm (a silent bug with a one-day-a-year blast radius)
2026-07-12 · [claude] · local · Inter 500 and Playfair 600 dropped at R6, following the plan's own §3.1 drop ladder; every `font-semibold` on Playfair and `font-medium` on Inter swept to a weight we actually ship · Lighthouse came back at perf 86 with LCP 3.8s, and the plan offers a fallback ladder that strips card translucency and then the orbs. The measurement refused it: Total Blocking Time was 20ms (the glass costs nothing) while the page pulled TEN font files totalling 429KB. Bytes, not pixels. After the cut: perf 93, 7 files, 275KB. A weight you no longer ship is a weight the browser SYNTHESISES, and fake bold is precisely what a type-led design system exists to prevent · rejected: taking the plan's fallback ladder (it would have degraded the design and bought nothing — the evidence was unambiguous)
2026-07-12 · [claude] · local · The Desk's pipeline heartbeat (module 00) spans both columns of the broadsheet spread rather than sitting in the rail · in the rail it occupied the first row's right-hand cell, and the grid's dense backfill then pulled the Brief (module 02) up into the empty cell beside it — so the Desk rendered 02 above 01. The ritual order is the one thing that layout may not break · rejected: dropping module 00 (it is the P0 proof that the app renders what the cloud wrote), reordering the DOM (breaks the tab order and the phone ritual)
2026-07-12 · [claude] · local · The theme cookie is renamed desk-theme → msm-theme · the theme governs the whole app now (D1); a cookie called "desk-theme" that themes the Academy is a name that lies to the next reader. One-time reset of the stored preference, which is acceptable for a single user · rejected: keeping the old name (the codebase's own naming is part of its documentation)

## The app feel plan — authoring session (2026-07-12)

### Structural

2026-07-12 · [claude] · structural · APP-FEEL-PLAN.md authored as the authority on structure, layout containers, navigation, and perceived performance; UI-REDESIGN-PLAN.md keeps the look (tokens, type, color, material, motion vocabulary); honesty rules stay above both · the four commissioned problems (receipt-paper IA, the dead scans page, the typing-only paper ticket, 400–1240ms frozen navigations) are diagnosed in the plan with production measurements, not impressions · rejected: patching rooms one by one without a contract (the receipt grew exactly that way)
2026-07-12 · [claude] · structural · Speed architecture is ISR ≤600s + on-demand revalidation + loading skeletons + streaming islands — the Desk's own proven pattern extended to every read route; Next 16 cacheComponents is backlogged with a named trigger (Serwist Turbopack support, or an F-budget the ISR path cannot reach) · plan Part 0.1/Part 5; this supersedes DEVELOPMENT-PLAN §4.5's "dynamic RSC everywhere; no ISR complexity in v1", which the Desk's 2026-07-11 ISR decision had already breached for "/" — the §4.5/§3.8 doc amendments land at F7 per plan Part 9 · rejected: the cacheComponents big-bang (an app-wide semantic migration, untested against the delicate Serwist webpack build, to reach budgets the proven pattern already reaches)
2026-07-12 · [claude] · structural · /scans becomes a summary index and each preset gets its own route /scans/[preset] carrying the full match set as a sortable, paginated DataTable (route-map amendment) · the commission ordered full match access with no dead "+N more"; the per-match metrics needed for "why it matched" columns already sit unused in scan_result.metrics, so no pipeline change is needed · rejected: one giant page with five 500-row tables; searchParams server pagination (fragments the ISR cache per URL and puts a server hop back into every sort click)
2026-07-12 · [claude] · structural · Rulings M1–M10 extend the honesty constitution to the new interaction patterns (tables, pagination, disclosure, shelves, skeletons, caches, form defaults, the signal→ticket doorway) · the load-bearing ones: a sortable scan table is a standing filter, never a crowd surface, and its default order is the pipeline's own rank labeled "scan order"; a caveat may collapse only atomically with its claim; user scroll is not motion [VETO? logged]; skeletons never shimmer where money will stand; sorting disables above the stated 500-row cap; the practice doorway obeys four hard conditions [VETO? logged] · rejected: leaving the constitution silent while new patterns ship against it

### Local

2026-07-12 · [claude] · local · The table engine is hand-rolled (lib/table.ts sort+paginate + a house DataTable), no TanStack Table, no AG-Grid · client-side sort of ≤500 bounded rows is trivial code, and the repo's grain already rejected Recharts and a formatting lib for owned equivalents; "AG-Grid-class ergonomics" means ergonomics, not the dependency · rejected: TanStack Table (~13KB + a dependency surface for two functions we then re-skin), AG-Grid (styling fights the token system)
2026-07-12 · [claude] · local · The paper ticket pre-selects NO side (segmented Buy/Sell, neither pressed); quantity/bucket/price keep helpful defaults; the last-close suggestion chip is tap-to-fill only and carries its bar's DATE · side is the decision, parameters are parameters; a dated reference price cannot be mistaken for a quote or for freshness [VETO? logged for the side change — it alters current behavior] · rejected: keeping the buy default (a quiet nudge on the one surface built to slow that decision), auto-filling the reference price (silently substitutes a quote for the user's own number)
2026-07-12 · [claude] · local · The plan was revised through a five-lens adversarial pass before delivery (57 findings integrated: executor-stall 12, honesty 10, iOS/WebKit 12, perf-evidence 12, layout reasoning 11) · the pass caught three would-be build-stoppers in infrastructure the recon had not touched — runtime ISR on a dynamic segment requires an EMPTY generateStaticParams (the pinned docs say so explicitly), feel-* tags trigger no CI workflow at all until ci.yml is amended (F0's first step), and the DB-less CI app job breaks on prerendering loaders unless they follow the house catch-and-degrade pattern — plus a self-contradiction (a movers swipe-rail the plan's own Appendix E-6 reasoning forbids; movers went back to vertical rows behind a disclosure) · rejected: shipping the plan un-attacked (the redesign plan set the precedent, and every blocker found was real)

## The app-feel build (F0–F7, 2026-07-12)

2026-07-12 · [claude] · local · The F0 instruments ship in REPORT mode and arm as gates in F1 · a budget armed before the cure exists is a red gate you learn to walk past, and the plan's own lesson is that a gate which fails for reasons you cannot fix trains its executor to ignore it · rejected: arming B1–B5 at F0 (every one of them would fail by design, since F0 deliberately changes no route)
2026-07-12 · [claude] · local · check-nav treats a non-200 response as a budget MISS, not a pass · the F0 baseline caught the instrument scoring /scans/unusual-volume as a 46ms success when it was a 404 — a missing page is the fastest page there is, and a speed budget that rewards a deleted route is a guard that cannot fail · rejected: probing only routes that exist today (the dynamic families would then be silently absent from the probe set, which is how a gap becomes permanent)
2026-07-12 · [claude] · local · nav-timing asserts the destination heading is ABSENT before every timed tap · Playwright matches accessible names by substring, so the heading "Scans" matched the Desk's own "07 — Sectors & scans" masthead and the timer stopped at 14ms having measured nothing; the app's slowest room was being reported as its fastest · rejected: simply passing exact:true (it fixes this instance and leaves the trap armed for the next locator someone writes)
2026-07-12 · [claude] · local · P-3 prefetch verdict is BRANCH A, by measurement: a loading.tsx does NOT downgrade an ISR route's prefetch · the pinned framework's bundled docs assert both possibilities in two tables and never reconcile them, so real probe routes were built and their prefetch payloads inspected with the headers next/link actually sends — the full page content was present with and without the boundary · rejected: reading harder (the docs genuinely do not answer it), and assuming branch B "to be safe" (it would have withheld skeletons from every ISR room for a cost that does not exist)
2026-07-12 · [claude] · local · No local Postgres on this Mac; CI's service container is the database oracle · brew has no postgresql bottle for macOS 26.3 and its source build fails, and prebuilt binaries need a libz that exists only inside the macOS dyld shared cache — this is the same wall LESSONS hit on 2026-07-11 with Docker · the seed is instead verified two ways: 12 DB-free unit tests that lock its promises, and the CI job that runs `migrate deploy && db:seed` against real Postgres on every tag · rejected: adding an embedded-postgres devDependency to the app for a developer-machine convenience
2026-07-12 · [claude] · local · Scan and paper fixtures live in prisma/fixtures/*.mjs, not inline in seed.mjs · the seed grew from 3 scan rows to 52 plus a 6-trade ledger, and a 500-line seed file is one nobody reads before changing · rejected: one large seed.mjs (the file-size and cohesion rule in CLAUDE.md exists for exactly this)
2026-07-12 · [claude] · local · F1 accepts a 93 → 87 drop in the ADVISORY Lighthouse performance score, and keeps prefetching · measured, not shrugged (docs/feel-evidence/lighthouse-tradeoff.md): the Desk's own JS did not grow (179.6 → 179.7 KB), TTFB is 12ms and blocking time 20ms — the score moves because the router now prefetches the five tab rooms' code as the browser idles, which is the exact mechanism that took a tab tap from 824–1342ms to 45–55ms; the lab metric models the one cold load and penalises bandwidth spent on the NEXT screen, while the prefetch improves every tap after it; both HARD budgets still pass (CLS 0.000, first-load JS 157KB ≤ 200KB) and LCP was already ruled a synthetic artifact by the user on 2026-07-11 · rejected: turning prefetch off to protect a lab score (it would restore the frozen-navigation disease this whole plan exists to cure), and quietly reporting the advisory miss as "advisory, therefore fine" [FYI logged for the user]
2026-07-12 · [claude] · local · The theme control applies the theme to the live document when you pick it, instead of only writing a cookie for the next page load · moving the settings page to ISR meant the toggle had to read `<html data-theme>` in the browser rather than the cookie on the server, and once it reads that attribute it may as well write it — before this, choosing Midnight set a cookie, marked the button pressed, and left the app sitting in Morning until the next full page load · rejected: leaving the theme to apply only on reload (the same click already had everything it needed to just work)
2026-07-12 · [claude] · local · e2e/nav-timing.spec.ts is gated to seeded databases (MSM_SEEDED), like the other seeded journeys · a navigation timing is only meaningful against known data: with no local Postgres, `next start` reads production Supabase three time zones away with 1,825 live scan matches, so the gate was timing a cross-country round trip over a table 57× larger than the fixture and swinging between 40ms and 815ms · rejected: loosening the ceiling until the flake stopped (that is how a gate is taught to lie)
2026-07-12 · [claude] · local · A DataTable's column set lives in a CLIENT module; the server page passes only rows · a Column's accessor is a function and functions cannot cross the server-to-client boundary — React cannot serialize them, and the build says so immediately · rejected: a key-string accessor API (it would buy serializable columns at the cost of every derived or formatted column the scan tables need), making whole pages client components (it would give back the ISR win F1 just bought)
2026-07-12 · [claude] · local · The Disclosure's content fade gets its OWN class (.content-fade), not .route-fade · .route-fade is exempted BY NAME from the P2 ancestor walk because it is the page-level opacity fade that moves nothing relative to anything else; a subtree fade over a base rate animates a probability ARRIVING, which is the exact thing P2 forbids — reusing the class would have inherited an exemption it had not earned and the violation would have passed the guard silently · rejected: reusing .route-fade (the negative control caught it, which is what negative controls are for)
2026-07-12 · [claude] · local · The Stepper holds raw text while the reader types and clamps only on blur · clamping on every keystroke made the field unusable: clearing it fired a change with "", which clamped to the minimum, which put a "1" back — so typing 42 into an emptied box gave you 142 · rejected: clamping on change (the input is the source of truth; the clamp is a boundary, not a running correction)
2026-07-12 · [claude] · structural · No layout-scoped revalidation anywhere in the app; every write names the paths it changes (drift rule 17 enforces it) · `revalidatePath(path, "layout")` drops the known-params set of any route declaring `dynamicParams = false`, which 404s every URL in that family until the next deploy — the theme action (since P6) and the watchlist action (since F1) both called it, so changing your theme once would have deleted every scan table in the app; caught by a VRT baseline that came back as a picture of a 404 page and reproduced locally in two commands · rejected: dropping `dynamicParams = false` instead (an ISR-cached notFound() serves 200 with the 404 page in the body — verified on /ticker/GARBAGE — so the closed set is what makes /scans/garbage an honest 404), and keeping layout scope with a comment warning people off it (a comment is not a guard)
2026-07-12 · [claude] · structural · Every Desk write revalidates inside `after()`, never inline · calling revalidatePath("/") from a server action invoked on "/" deadlocks the action's response — the row is written, the reply never arrives, and the button says "Saving…" forever; it has been true of the journal and the weakener checkboxes since the Desk became an ISR route on 2026-07-11, hidden because the e2e asserted a marker that renders on useActionState's initial state · rejected: dropping the revalidation (the cached Desk would then show a stale journal for up to ten minutes), and moving the journal off the Desk (it is the evening ritual's closer)
2026-07-12 · [claude] · local · B3's sample ceiling gates the SECOND-worst sample, not the worst · the gate failed on a single 1,009ms reading — nine milliseconds over — among samples with a median of 202ms, which is a shared-runner hiccup and not a regression; the disease it guards against (a server render on every tap) makes EVERY sample slow, so the second-worst catches it and one outlier does not trip it · rejected: raising the ceiling (it would weaken the tripwire against the actual disease), and leaving it (a gate that fails randomly trains its executor to ignore it — the plan says so in as many words)
2026-07-12 · [claude] · local · The price chart's TradingView attribution is a real link in a caption below the chart, and the library's own `attributionLogo` option is off · the option injects its link INSIDE the chart container, and that container is `role="img"` — which tells a screen reader the whole box is a single picture, making a link inside it unreachable by keyboard and invalid by WCAG (axe: nested-interactive, found by the F7 sweep); Lightweight Charts' licence asks for the notice and the link to be on the PAGE and merely offers its logo as one way to do that, so a visible caption link satisfies it and is reachable besides · rejected: dropping `role="img"` from the chart (then the picture has no accessible name at all), and suppressing the axe rule (the licence link was genuinely unreachable — the rule was right)
2026-07-12 · [claude] · local · The service-worker control test waits for an ACTIVE worker before reloading, rather than waiting longer for a controller after it · installing a service worker means downloading its whole precache, and this release enlarged it (every room is a cached route now) while the Desk prefetches five more, so on a cold CI runner the install was still in flight when the test reloaded — and a worker that has not finished installing cannot control anything · rejected: raising the 10s timeout (that treats a race as slowness, and the race would come back the first time CI was busy)
2026-07-12 · [user-approved] · structural · --color-muted darkens from #6e6c80 to #676577 in Morning (and --color-tier-weak with it, which was the same hex on purpose); Midnight's #918ea6 is unchanged · the token was verified at 4.83:1 against the PAPER, but muted text almost never sits on the paper — it sits on a Surface, which is white at 72% over the lavender wash and composites to #f0effe, where the old value measured 4.48:1 and missed AA's floor by two hundredths; the user approved the change as a readability floor rather than a palette preference ("a slightly darker grey is a fine price for text I can actually read") · rejected: changing Midnight too (it already measures 5.00:1 on its own glass and axe finds zero contrast failures in dark — a colour that passes is not a colour to change), and darkening further for headroom (it would start competing with ink-2 and the request was a floor, not a repaint)
2026-07-12 · [claude] · structural · The axe gate excludes NOTHING and runs in both themes; a settle() helper waits for the page's fade to finish before any colour is measured · re-enabling color-contrast naively reported 58 failures across every colour the app owns, which looked like a broken palette and was not: axe composites a foreground through any ancestor's opacity, and every page here fades in — it was measuring the page while it was still arriving, and text-muted at 55% opacity really is 2.23:1, a colour no reader ever sees; waiting for the fade collapses 58 findings to the 1 that is real · rejected: keeping the exclusion (the user's whole point was that the gate should hold the full line), and gating light-only (a two-theme app whose gate checks one theme is checking half its colours) · NOTE: this invalidates the "58 nodes of text-faint" figure in the first F7 write-up — the faint→muted swap was still correct (faint measures 2.08:1 at full opacity and the token sheet forbade it in writing), but the count was inflated by the same artifact; docs/feel-evidence/accessibility.md is corrected
2026-07-12 · [claude] · structural · /settings renders on request (force-dynamic) and its actions no longer revalidate "/settings"; it is the single entry in check-routes' ALLOWLIST and the single exemption in drift rule 13 · a page may be CACHED, or it may be written to and read back in the SAME CLICK — not both: every other room is a reader, but settings is a writer whose entire content is the thing you just changed, and each click runs a server action that re-renders that page as its reply, so a cached reply can be the page as it was BEFORE the click (a watchlist row added by the e2e vanished on the next click, intermittently, and the trace shows the row simply absent from the final render) · it is the same shape as the Desk's deadlock — a page revalidating itself from inside its own action — which is why it is now a stated rule rather than another one-off fix · rejected: retrying the flaky test (a gate that fails randomly trains its executor to ignore it — the plan says so in as many words), and keeping the cache and hoping (the race is user-visible: click Focus, get back the page from before you clicked)
2026-07-12 · [claude] · local · Midnight's --color-muted lightens from #918ea6 to #9c99b1 after all (and --color-tier-weak with it) · I had said Midnight already cleared the floor and did not need changing — that was measured against the STANDARD card (#231f38, 5.00:1) and I ran the survey on the desktop project only; on a RAISED card (#2c293d) it measures 4.44:1, and the phone Desk's shelf cards are raised, so the sweep missed it by measuring half the app and calling all of it clean; #9c99b1 measures 5.10:1 on the raised card and 5.74:1 on the standard one · rejected: leaving Midnight alone (it does not pass — I was wrong), and matching Morning's exact ratio (dark text on light and light text on dark do not need the same hex to need the same floor)

## The news & control build (N0–N7, 2026-07-12)

2026-07-12 · [claude] · local · `nc-*` is wired into ci.yml's tag triggers AND the e2e job's `if` condition as N0's FIRST action, before any other work · the F0 lesson, applied without having to relearn it: a tag pattern that appears in the trigger list but not in the job conditions (or in neither) runs no browser suite at all, and every "gate green" claim after it is ornamental — the plan says so in as many words, so it cost one edit to obey · rejected: wiring it later "once there is something to test" (the first tag would then have been the one that proved nothing)
2026-07-12 · [claude] · structural · The whole Appendix C schema lands at N0 in one migration — macro_stat, news_cluster, catalyst_link, news_image, manual_run, plus news_item's five new columns and market_context.index_levels_as_of — even though the CODE that fills them arrives in N1/N3/N4/N6 · N0's contract is that the seed lands FIRST and ALONE (so the VRT baselines are re-shot once rather than at every phase), and a seed cannot write rows into tables that do not exist; the plan implies this without saying it, and the alternative — three migrations, three re-baselines — is exactly the churn N0 exists to avoid · rejected: one migration per phase (it would re-shoot the pixel baselines three times and put the seed's determinism gate three phases away from the seed)
2026-07-12 · [claude] · structural · ANTHROPIC_API_KEY reaches the nightly jobs in N1, not N4 as the plan scheduled · the N0 audit found the key EXISTS in GitHub (set 2026-07-10) but is passed to NEITHER nightly workflow's env block, so `_build_submit_extraction()` has been returning None and every LLM stage has silently skipped in production since P3 — the evening brief has been running without its extraction the whole time; this is a live correctness bug with a two-line fix in a file N1 already edits (it adds the 6:00am macro cron), and the plan's own sequencing principle is "the correctness bug before anything cosmetic" · rejected: waiting for N4 (three phases of a degraded briefing for a scheduling technicality), and treating it as a provisioning item (P-3 assumed the question was whether the secret existed — it does)
2026-07-12 · [claude] · local · The three seeded article images are GENERATED, not CC0 stock photos · a stock photo drags a licensing question into the repository for zero benefit, while a generated image is byte-identical on every machine — which is exactly what the visual-regression baselines need; each is 1200×628 (the 1.91:1 og:image ratio the card geometry assumes) and ~13KB, inside the plan's 30KB budget · rejected: sourcing real CC0 photographs (a licence to audit, bytes that could differ, and nothing gained — these exist to exercise geometry and the blur placeholder, not to be looked at)
2026-07-12 · [claude] · local · The seeded images live in `app/public/fixtures/news/`, not `app/prisma/fixtures/img/` as the plan wrote · the plan also says they are "served in e2e via the seed's local paths (`/fixtures/`)", and Next serves static files ONLY out of `public/` — a file under prisma/ has no URL at all, so the two halves of the plan's own sentence could not both be true; the tree wins on the detail, the intent (committed, tiny, network-free, deterministic) is untouched · rejected: a route handler to serve them from prisma/ (machinery to defend a directory name)
2026-07-12 · [claude] · local · The seed's 14 news clusters carry significance scores computed BY HAND from Appendix E's formula, with the arithmetic written into each cluster's comment, and a unit test recomputes all 14 · the pipeline's rank.py (N4) needs an oracle that a human worked out independently, or it is only ever checked against itself; the seeded night also has to prove the room's central claim — the biggest mover on the tape (SMCI, +18.4%) ranks THIRD, and the lead is a Fed statement that moved no single stock, because magnitude is one input of five and enters in units of the ticker's own volatility · rejected: assigning plausible-looking scores by eye (the one number in this app that must be reproducible from stated inputs is the one that decides what the reader sees first)

## N1 — the macro truth fix (2026-07-12)

2026-07-12 · [claude] · structural · The index levels get their OWN source-status key (`fred-indexes`), and `fred` itself stays "ok" when FRED answered · the production row of 2026-07-11 recorded `fred: ok` while all three index levels were NULL and VIX/10-year came through fine — and that status was not even wrong: FRED *was* reachable, it answered for two of its four cells; one key simply cannot describe two different failures, so the honest repair is a second key rather than making the first one lie in the other direction · a PARTIAL read degrades too (a Desk showing two true levels and one ETF proxy has a reader who deserves to know why the third changed shape), and a non-session day is skipped entirely (FRED publishes no index observation for a day the market never opened, and an amber lamp that means nothing is how a real one gets ignored) · rejected: widening `fred` to mean "everything FRED gives us" (it would light up a source that is not broken, on every night the index series alone are late)
2026-07-12 · [claude] · structural · A failed FRED night now KEEPS the index levels already stored, for up to five sessions, and records the session they are for (`index_levels_as_of`) · the upsert said `sp500 = EXCLUDED.sp500` — whatever tonight fetched, INCLUDING None — so one flaky night did not merely fail to update the level, it overwrote the good one and the Desk silently fell back to printing SPY's price under the S&P 500's name; a SUCCESSFUL RUN WAS DESTROYING DATA, which is the worst shape a bug can have because everything looked like it worked · the levels carry forward as a SET, not as three independent cells, because they arrive from one source on one call cycle and the table dates them with one column — keeping them together is what lets that single date be true about all of them at once; a partial read therefore wins outright and any slot it missed falls back to its honest proxy, rather than smuggling last night's Dow in under tonight's date · five sessions because past a trading week a stale number wearing an index's name is worse than an ETF's price wearing the ETF's name · rejected: COALESCE(EXCLUDED.x, market_context.x) (market_context is keyed by run_date, so each night is a NEW row and there is nothing to coalesce WITH — the fix has to reach back to a previous row, which is why it is a function and not a clause)
2026-07-12 · [claude] · structural · HONESTY-RULE COLLISION, resolved and logged. Part 3.2 says a degraded index slot keeps the index's NAME over an ETF's price; UI-REDESIGN §6.1 (an honesty rule, rank 1) says "there is deliberately no third path where an ETF price wears an index's name" · the plan is rank-2 on the macro module but honesty rules outrank it, and the plan itself says a collision is resolved in the honesty rule's favour and logged — so the question is whether the rule's INTENT (a reader must never mistake an ETF's price for an index's level) survives the plan's grammar · it does, but only because the chip was made to do more work than the old label suffix ever did: it does not merely flag the fallback, it DENIES the misreading in words ("SPY · ETF price — not the index level"; the Nasdaq's is sharper still — "not the Composite" — because QQQ tracks a different index rather than approximating the same one), and a unit test enforces the coupling mechanically: an ETF-sourced slot must ALWAYS carry a chip, and if its label names an index that chip must contain an explicit "not the ..." · rejected: keeping the "(ETF proxy)" label suffix (it is the double belt the user's screenshot showed reading as noise — the same two words twice), and dropping the index name entirely from a degraded slot (the reader loses which slot they are looking at, and the row that answers "how did the S&P do?" stops answering it)
2026-07-12 · [claude] · structural · The small-caps slot is labeled "Small caps", never "Russell 2000" · FRED deleted all 36 FTSE Russell series in 2019 and every licensable alternative was checked and rejected (the plan's Appendix A.2 records the search), so this row can NEVER carry that index's level — not tonight, not ever, under any provisioning · a label naming an index we are structurally unable to measure is a promise the row cannot keep, so it names the GROUP the number describes and lets the chip say what the number is · rejected: "Russell 2000 · IWM (ETF proxy)" (it puts an index's name on a row that will never hold that index, and buries the honest part in a parenthesis)
2026-07-12 · [claude] · local · Job A gains a `macro` mode and a second cron at 6:00am ET (`0 10 * * 2-6`) · FRED posts the index closes AFTER both nightly jobs run — the Dow ~6:03pm ET, the S&P ~8:01pm, the Nasdaq Composite ~11:38pm, against Job A at 6:37pm — so the levels stored by the night are STRUCTURALLY one session behind by the time the user reads the Desk over coffee, and no amount of retrying fixes that because the data does not exist yet; the dawn run re-reads FRED and updates nothing else · mode stage-lists are CONSTANTS with a unit test, because a mode is a promise about what a run will not touch ("macro mode does not ingest bars" is the whole reason it is safe to run at dawn and later to hand the user a button for), and a mode that could silently grow a stage would break that promise with nobody the wiser; `parse_mode` raises on an unknown mode rather than defaulting to "full", so a typo in a workflow cannot turn a three-number refresh into a whole market ingest · rejected: moving the crons to Vercel (the plugin's automated advice suggested it — it would put a second, unaudited writer inside the app, which ruling 0.2.3 forbids: the pipeline computes and writes, the app reads)
2026-07-12 · [claude] · local · publish_macro() UPDATEs an existing market_context row and refuses to create one · a macro-only run has no snapshot, so it has no breadth — and breadth is NOT NULL on that table because a night that ingested the market always knows how many names advanced; a dawn run is not ingesting the market, it is going back to fix three numbers · so if no row exists for that session it does nothing and says so, rather than writing a breadth-less row: a market_context with a hole in it is a macro module with a hole in it · rejected: making breadth nullable (it would weaken a column that is genuinely always knowable, to accommodate a run that has no business writing it)
2026-07-12 · [claude] · structural · The VRT tolerance becomes an ABSOLUTE pixel count (`maxDiffPixels: 600`), replacing `maxDiffPixelRatio: 0.01`, and the baseline job runs `--update-snapshots=all` · a 1% RATIO sounds strict and is not: these are full-page shots of long pages, so the desktop Desk (1366×2763 = 3.77M pixels) had a budget of 37,742 differing pixels — and N1's rewritten macro row (reordered, relabelled, a chip deleted, the provenance sentence replaced) came to fewer than that, so the desktop baseline PASSED UNCHANGED while the phone shot (1.8M pixels, an 17,988 budget) caught the very same change; the committed desktop baseline was therefore a photograph of the old behaviour, green and wrong · a ratio makes the gate WEAKER the taller the page gets, which is exactly backwards — a longer page has more room to hide something; an absolute count does not care how tall the page is, and 600px is a few dozen antialiased glyph edges: enough for rasterisation noise, nowhere near enough to hide a word · separately, plain `--update-snapshots` defaults to "changed" and only rewrites a baseline whose comparison FAILED, so a real change inside the tolerance was neither reported NOR re-baselined — the stale picture survived twice over · rejected: tightening the ratio instead (0.001 would still scale with page height, so the guard's strength would still depend on how long the page happens to be — the wrong variable)
2026-07-13 · [claude] · structural · Module 00 (Pipeline) is retired from the Desk grid and becomes a status strip in the header block; the ritual now runs 01→07 · the card spent the phone's most valuable slot on one date and one fixed sentence, and looked identical on a healthy night and a three-days-dead one — which makes it a decoration that mentions freshness, not a freshness indicator · the ORDER of what remains stays inviolable (retiring a station is not reordering the others), and the mastheads do not renumber: a masthead is a name, not an index, and renumbering every module, test and baseline to close a cosmetic gap is churn with regression risk and no reader value · rejected: keeping the card and merely shrinking it (the problem was never the size, it was the sameness)
2026-07-13 · [claude] · structural · New token --color-danger (#b91c1c / #e06c6c), with EXACTLY ONE consumer forever: the pipeline strip's DEAD state · amber says "something degraded"; red says "do not trust the numbers on this page", which is a different claim and needs a different colour · it borrows the hue the palette already owns (grade-folklore's red, hue 0°, outside the reserved amber band) so NO new hue enters the app, but under its own name so the two meanings never merge · drift rule 19 fails the build for a second consumer, negative-controlled · rejected: reusing --color-down (it would paint a broken pipeline in the colour of a falling market)
2026-07-13 · [claude] · structural · The pipeline strip's AGING state joins amber's consumer list (P11) · a session's edition failing to land is a genuine alert about a real degradation, which is precisely what the reserved amber is reserved FOR · the list stays short and every entry is argued
2026-07-13 · [claude] · structural · PipelineStrip grades freshness on the CLIENT, against the reader's clock, not the server's · the Desk is cached, and a cached render carries the clock it was made with: a render made Monday morning (healthy) is still what the reader is served on Tuesday, so a pipeline that died overnight would be photographed as "fresh" on their first paint — the one surface whose whole job is catching that, silenced by the cache on exactly the morning it mattered · the last completed run is DATA and is cached; "now" is not data · cost: ~5.4KB of shared client JS, accepted and recorded in the bundle baselines
2026-07-13 · [claude] · structural · New breakpoint token --breakpoint-wide (1536px); containers widen to max-w-[1500px] there · every room outside the Desk was one column inside a 1360px shell, which on a 27" display is a phone layout with enormous margins · what `wide` buys is INTERNAL DENSITY (cards 3-up, setup cards 2-up, a wider rail), never more columns — and the Desk gains no third column at any width, because a second reading column would split the ritual's order into two ambiguous streams
2026-07-13 · [claude] · structural · The sources footer sheds its card on an all-ok night and keeps it at full strength on a degraded one; the component decides this itself · a whole Surface card carrying "6 sources · all reporting" is the app spending its most expensive material on its least surprising fact · but it does not shrink by one pixel on the night something broke, because that night is the entire reason the surface exists — a footer that gets quieter as the news gets worse is the inverse of the rule the strip obeys
2026-07-13 · [claude] · structural · RangeControl's `surface` prop is a CLOSED TYPE UNION ("ticker-chart" | "news-feed"), and the component renders null if the type is bypassed at runtime (ruling C3) · a reader who can slide a base rate's horizon until it flatters a pattern is a reader the app has taught to p-hack, and the control would not look wrong — it would look like a thoughtful piece of UI · the firewall is the type, not a convention or a code review; a unit test locks the allowlist at exactly two entries, and negative tests assert the evidence surfaces render no radiogroup, no fieldset, no button
2026-07-13 · [claude] · structural · The window vocabulary (copy.window) is a CLOSED set, locked by a deck test; every metric column in every scan table must carry a window token or the build fails · the failure mode of ruling C2 is not that people ignore it — it is that they obey it in six dialects, and a reader learning what these words MEAN has to decode three spellings of "20 days" before learning anything
2026-07-13 · [claude] · structural · Window tokens are quieter by SIZE AND WEIGHT, never by opacity · they shipped with `opacity-80` and axe found nine failing contrast nodes on the Desk, every one a window token · drift rule 18 already says it (faint is for placeholders, never information) and C2's whole claim is that the window IS information — a number's unit is not decoration you may fade
2026-07-13 · [claude] · structural · The Vercel build now runs `prisma migrate deploy` before `next build`, and `npm run check:migrations` joins the standing gate · production had never received N0's migration and nothing could see it: CI migrates a fresh container, so a green pipeline proves a migration APPLIES, never that it was applied to the database the app reads · fails closed — a migration that will not apply is a deploy that does not happen
2026-07-13 · [claude] · structural · Budget B4 gains a HARD CEILING (200KB first-load JS) alongside its drift baselines, and the baselines are re-recorded · re-baselining is how a budget dies: every "it only grew a little, and here is why" is individually true, and six of them puts the app at 260KB with a green gate the whole way, because drift is measured against the last thing that was accepted · drift catches the unexplained; the ceiling catches the accumulation of the explained
2026-07-13 · [claude] · local · The strip's e2e states are driven by Playwright's fake CLOCK, not by the plan's proposed test-only `?now=` search param · reading searchParams in a page forces that route to render dynamically, and the Desk is a cached route (budget B1, the change that took a tab tap from 1342ms to 280ms) — the test would have quietly cost the app the thing the last plan bought · the clock is better on its merits anyway: the strip grades against the BROWSER's clock, so the honest way to test "what does a reader see on Tuesday night" is to be a reader whose clock says Tuesday night
2026-07-13 · [claude] · local · The degraded Macro Pulse is a STYLEGUIDE specimen, not an e2e · driving it through the seeded database would mean one spec mutating market_context and every later spec — including the pixel oracle — reading whatever it left behind, which is exactly the bug that made the Academy baselines unphotographable in N1 · props are deterministic, a component test proves the composer changes with its rows, and the styleguide proves the picture
2026-07-13 · [claude] · local · The VRT pins the browser clock to the seeded session's evening, and the desk shots assert the strip is FRESH before photographing it · the strip's appearance depends on when it is, so an unpinned baseline escalates fresh→aging→dead on its own as real days pass — a picture that rots with the calendar is not an oracle
2026-07-13 · [claude] · local · The pipeline strip's tap target is 44px, not the plan's "~28px" · the whole line is a link, and the touch sweep's inline exception covers a link INSIDE a sentence, not a sentence that IS one · the ink stays one quiet line; the height went into padding · the constitution's 44px floor outranks a plan's pixel estimate

## N3 — the macro board (2026-07-13)

2026-07-13 · [claude] · structural · A source is CHECKED nightly but only WRITES when its own observation advances (or is revised) · the five board stats publish on five different schedules, and a pipeline that wrote whatever it fetched each night would stamp Thursday's mortgage rate with Tuesday's date — a false freshness claim, which is the same species of lie as an ETF's price wearing an index's name, the bug that opened this plan · "as of" therefore always names the SOURCE's observation date, never our fetch time, and on the nights in between nothing is written and nothing is stale (a Thursday rate on a Tuesday is the newest rate that EXISTS) · the one exception is a REVISION — a changed value under an unchanged date is new information, and agencies do restate their prints, so a rule that only watched the date would pin us to the first print forever while quietly disagreeing with the source · rejected: re-writing the row nightly and letting `fetched_at` carry the freshness (it makes the freshest-looking field the least meaningful one, and a weekly number would appear to refresh nightly)

2026-07-13 · [claude] · structural · The Mood gauge is computed by the FULL nightly and by NOTHING else — the 6am macro refresh deliberately leaves it alone · two of its five components (breadth, and where the universe sits in its own 252-day range) can only be measured by a run that actually ingested the market, and the dawn run does not — it exists to re-read three numbers FRED published after the night had finished · recomputing it there would either produce a thinner three-component gauge and overwrite the night's five-component one, or silently reuse yesterday's market shape under today's date · **a run that did not look at the market does not get to say how the market feels**, which is the same principle that already stops publish_macro creating a breadth-less market_context row · consequence to remember at N6: the user's "Refresh macro stats" button honestly refreshes the four external stats, and not the gauge

2026-07-13 · [claude] · structural · Each component's "greedy/fearful" arrow is DERIVED from its percentile, in both the pipeline and the app, and stored nowhere as an independent fact · the N0 seed had already drifted — momentum sat at the 48th percentile, below its own median, carrying the arrow "greedy" — and it looked plausible, broke nothing, and would have shipped · a derived value stored as a field is free to disagree with what it was derived from, and eventually does; this is ruling C6 applied one level down, inside a data structure · a fixture test now holds the seed to the same rule · rejected: keeping the stored arrow and adding a test that it matches (the test would be the only thing standing between a hand-repaired database row and a contradiction on screen — derivation removes the possibility rather than policing it)

2026-07-13 · [claude] · structural · The macro board's STALE cell (C7 rung 5) joins amber's consumer list (drift rule 5); "not yet reported" and "source unreachable tonight" stay QUIET · a number three full cadences past its last publication has stopped being information and started being furniture, which is a real degradation and precisely what the reserved amber is reserved FOR · but an unprovisioned API key and a single failed fetch are not alarms — an app that shouts about a missing key has nothing left to say on the night its numbers are actually wrong, and the alert voice is worth exactly what its scarcity is worth · the word "stale" is always rendered beside the colour, so a screen-reader user is never handed this app's calmest voice on one of its worse nights

2026-07-13 · [claude] · structural · Staleness is counted in the unit each SOURCE publishes in — sessions for gold and the gauge, calendar days for the rupee, weeks for the mortgage rate, months for CPI · gold trades on market sessions, so Friday's price read on Monday is three calendar days old and ZERO sessions old — a calendar-day rule would paint it amber every Monday, and a lamp that cries wolf every Monday is not there on the morning it is finally right · but NRB publishes every calendar day including weekends, so for the rupee a weekend genuinely IS three missed publications and a session rule would hide a real outage · N2's "count in sessions, never days" turns out to be half of a bigger rule: the honest unit is whatever the source actually uses · freshness.ts's private session counter is now shared (market-hours.sessionsBetween) — two copies of "what counts as old" is one copy too many

2026-07-13 · [claude] · structural · copy.window gains `vsPriorWeek` ("vs prior week"), and the shelf strings in the plan's Appendix B are amended to carry a COUNT · two deliberate departures from the plan's literal copy, both in the honesty rule's favour, both logged as the authority order requires · (1) the mortgage cell states its change against last week's SURVEY and no existing token said that truthfully — "5D" would have implied five trading days of quotes where Freddie Mac publishes one weekly number · (2) Appendix B specified "Markets — swipe" and "Money & mood — swipe", and shipping those verbatim BROKE RULING M8: a shelf that hides an unstated number of things can hide anything · the existing M8 test caught it within a minute of the change, and the count is counted rather than typed (the test's props carry two index slots, so it correctly reads "4 figures" where the real Desk reads five)

2026-07-13 · [claude] · structural · The CPI cell renders NO delta chip · a chip reading "−0.4 vs prior month" beside an inflation rate is read by almost everyone as "prices fell", when what fell is the RATE at which they rose — a delta of a rate is one of the easiest numbers in finance to misread, and the month label is the context this number actually needs · the plan says the same thing ("a delta-of-a-rate chip invites misreading") and this records that it is enforced in the builder, not left to a component's discretion

2026-07-13 · [claude] · structural · CPI is fetched from FRED with `units=pc1` — the year-over-year change computed BY FRED — and the pipeline computes nothing · the moment this app divides one CPI level by another it owns an inflation figure of its own making, one that can disagree with the headline the reader saw on the news with no way to explain the difference · the test asserts the parameter is on the WIRE, not merely that the fixture parses: if `units` ever silently dropped, FRED would answer with the raw index level (~320) and the board would print "Inflation (CPI YoY) 320.5%" through a code path where every value assertion still passed

2026-07-13 · [claude] · local · The rupee shows NRB's buy/sell PAIR and names whichever source answered; the mid-market fallback renders its licence attribution only when it is the one showing · NRB publishes the central bank's official reference and open.er-api publishes a market mid — two different measurements of two different things, never silently interchanged (C6), so the label follows the source key mechanically · picking one side of the pair would silently answer a question the reader never asked; the mid-market fallback HAS no sides and does not invent a spread to keep the cell's shape consistent · no remittance-app rate is quoted anywhere (none publishes a legitimate API), so the cell carries the qualifier instead: "Remittance apps may differ."

2026-07-13 · [claude] · local · A fixture that was NOT recorded from its provider must say so in its own FILENAME · R0 hand-wrote three FRED fixtures (sp500/nasdaqcom/djia) in the shape of a real response with plausible numbers — `"count": 8000` against the real 2610, a value of 6812.34 against the real 7575.39 — and for three phases every index-level test proved the parser agreed with an invention · replaced with real recordings; gold, whose key does not exist, gets `xau_usd_UNVERIFIED.json` and the only REAL gold recording in the repo is `error_403.json`, the genuine unkeyed rejection (which pins the route and the auth header — two more facts than a beautiful invention pins) · rejected: recording nothing for gold and testing against a mock (the 403 is real evidence about the provider and cost one HTTP call to obtain)

2026-07-13 · [claude] · local · scans.build_snapshot is split so the indicator frame is computed ONCE per night (snapshot_from_indicated) · the indicator pass is the most expensive thing the night does — every symbol, every bar, five years deep — and from N3 it is needed twice: once by the scans, once by the Mood gauge, whose breadth and range-position components are measured across the whole universe's history · recomputing it would double the heaviest step of the run to obtain a frame we were already holding
2026-07-13 · [claude] · structural · A secret that exists in GitHub but is not passed to the job does not exist · GOLDAPI_KEY landed in secrets and the gold cell would STILL have printed "not yet reported", because nightly-a's env block never carried it — the same shape as the ANTHROPIC_API_KEY bug that silently skipped every LLM stage in production for four phases · added GOLDAPI_KEY to the env block and built tests/test_workflow_env.py, which walks the import graph from each job module, reads what it actually takes off the settings object, and fails if the workflow that runs it does not pass a required secret · rejected: a checklist or a comment (this bug has now been found BY HAND twice, and a thing found by hand twice will be missed the third time)
2026-07-13 · [claude] · structural · The news ingest budget is ONE Finnhub call per category, not two · the plan bought a second call for "minId pagination" expecting ~200 items; measured, minId is a FORWARD filter — below the oldest id it returns the identical 100 articles, at the median it returns 54 — so the second call fetches nothing new and there is no way to page back past the newest ~100 · rejected: keeping the second call "for safety" (it would have doubled the request cost to re-fetch articles we already held, and a dedup step would have hidden that it was doing so)
2026-07-13 · [claude] · structural · Entity resolution is OURS to do, deterministically, because the providers do not do it · Finnhub's market news carries no tickers at all (0 of 160 items; company news carries them on 20 of 20), so the plan's clustering rule ("overlapping ticker sets") was unsatisfiable and its magnitude term had nothing to measure · newsdesk/resolve.py matches article text against the instrument table with four rules of decreasing strength, refusing when unsure — zero false positives across the whole recorded night with every trap name (Target, Gap, Visa, Key, Match, C3.ai) loaded · rejected: asking the LLM which companies a story is about (it would be right most of the time and confidently wrong the rest, and a wrong link prints a false statement about a company beside a real price move)
2026-07-13 · [claude] · structural · The clustering threshold is 0.45, MEASURED against the recorded night, not the plan's 0.55 · at 0.55 the clusterer merges NOTHING across 134 real articles — "VitalHub acquires Buddy Healthcare" and "VitalHub Announces Acquisition of Buddy Healthcare" score 0.50, because "acquires" and "acquisition" are different tokens, so the bar is not strict but INERT, and the Front Page would print every story once per outlet while its "3 sources" line never appeared · at 0.45 every true merge in the night is found and no false one is; at 0.40 Qiagen, Conmed and Tiny Ltd collapse into ONE cluster because all three headlines say "jumps after report of takeover interest" · pinned by tests from both sides
2026-07-13 · [claude] · structural · Ticker overlap is a VETO on merging, not a requirement for it · the plan required overlapping tickers to merge; on a feed where most items have none that is unsatisfiable, and the honest role of the evidence is the opposite one — "Apple beats on revenue" and "Microsoft beats on revenue" are near-identical strings (Jaccard 0.67) about different companies, and no amount of word agreement may join them · where both items name companies and none is shared, they never merge, whatever the words say
2026-07-13 · [claude] · structural · Press-release wires collapse to ONE corroborating source · the merger feed's "sources" are GlobeNewswire (39 of 60) and BusinessWire — pipes a company pays to carry its own announcement, which do not decide whether it is worth carrying, and that judgment is the entire thing corroboration measures · VitalHub's acquisition appears twice under two wire names; counted naively it would be "2 sources" and the formula would pay the story for its own press office's distribution budget · newsdesk/outlets.py buckets every wire as "press release"
2026-07-13 · [claude] · structural · Regulatory boilerplate is denied at the write path · 26 of 60 merger items (43%) are UK Takeover-Panel Form 8.x disclosures; unfiltered, the M&A filter chip would have been almost entirely paperwork about UK property trusts, each collecting the highest class prior there is for being filed · newsdesk/noise.py is deliberately NARROW — it names the families it knows and keeps everything else, because wrongly dropping a real story (the reader never learns it existed) is far worse than wrongly keeping a dull one (it ranks low and sits at the bottom) · same disease and same cure as R0's Session Calendar allowlist
2026-07-13 · [claude] · local · The Stage-A cap ranks by PRE-LLM salience, not by significance · significance depends on event_type, and for the top clusters event_type is supposed to come FROM Stage A — capping by significance would choose what Stage A reads using the answer Stage A exists to produce · ranked instead on the two things the pipeline knows entirely on its own: corroboration, then magnitude
2026-07-13 · [claude] · structural · `compute` mode is NOT declared until N6 builds it · every mode main() does not recognise falls through to the FULL nightly, so a mode declared without a handler is not a broken button — it is a button that silently re-ingests the entire market, and pressed at noon it would write half a day of unformed bars over the last good close · main() now refuses any mode it has no handler for, and `compute` is left out rather than listed-and-broken · rejected: declaring it now "for the contract" (N6 can add it with its button, in the same commit, and then the promise and the code land together)
2026-07-13 · [claude] · structural · Ticker resolution refuses bare uppercase tokens, and refuses any single-word company name that is an ordinary English word · against a 44-name test universe the rules were clean; against production's 12,933 instruments they linked UAE (a country), LNG (a commodity), NDAQ (the index) and the words "strategy", "people", "popular", "team" and "fossil" to real companies, each one a false statement printed beside a real price move · the guard was INVERTED — list the WORDS, not the names — because a curated list of names-that-are-words must grow with the universe it protects, and a stoplist that has to grow with its data is the wrong shape of guard · rejected: enumerating acronyms and ambiguous names (it would need every country code, commodity abbreviation and initialism in English, and would still be one headline behind)
2026-07-13 · [claude] · structural · A provider's symbol is checked against our instrument table, never trusted · Marketaux tagged the VitalHub story with VHI, which is VitalHub in Toronto and Valhi, Inc. in New York — our listing. The card would have printed Valhi's real price move under a headline about a Canadian software acquisition: every number true, the card a lie · a provider tag is accepted only when we hold the symbol AND our name for it agrees with the provider's; an unverifiable tag is refused rather than assumed
2026-07-13 · [claude] · structural · The Desk's "markets open/closed" is computed in the READER's browser, not on the server · `/` is ISR-cached, so a server-rendered `marketState(new Date())` was frozen at cache-generation time and served on — a Desk built at 3:55pm told readers "markets open" long past the close · same bug as F4's cooling-off timestamp, and the rule is now twice-learned: on a cached page, anything that depends on "now" must be computed in the browser · found by the pixel oracle, whose Desk baselines had quietly become photographs of whatever the market was doing when CI last ran
2026-07-13 · [claude] · structural · The Stage-A cap now ranks by SIGNIFICANCE, reversing the salience rule logged above · the circle that rule was defending against does not exist in this tree: rank.classify_event classifies the event DETERMINISTICALLY, before any model runs (N4 built the classifier rather than taking the class from Stage A), so significance is fully known pre-LLM and nothing is being chosen with an answer it has not got · measured on the recorded night: 8 of the 20 clusters Stage B was asked to narrate were NEVER READ by Stage A, and they were the eight biggest stories of the night — the whole Gulf/Hormuz/oil cluster — because corroboration is 1 for 131 of 134 clusters and magnitude is 0 for ~130, so a single-source macro story (which is what a market-wide event IS) sorted to the bottom of the extraction queue while sitting at the top of the page · the narrated set is now a SUBSET of the extracted set by construction, at any cap sizes, and a test holds it · the failure was invisible: an unread story produces an honest null, and a null prints nothing
2026-07-13 · [claude] · structural · Stage A runs SYNCHRONOUSLY inside the news stage, not as a Message Batch collected by Job B (amends plan 7.5) · the whole page — facts AND prose — must land in ONE publish transaction, because publish_news's own upsert sets why_it_matters = EXCLUDED.why_it_matters, so a two-job split would NULL out the prose the later job wrote every single time news mode ran · news mode is the user's manual "refresh the front page" button (N6), and a refresh that rebuilt the stories while blanking their notes is a button that quietly makes the page worse · it uses the SAME machinery the plan named (sync_extract, same prompt, same schema, same drop-on-malformed parse) · cost of the difference, measured at current prices: ~$18/year · rejected: batches with an in-job poll (10 minutes of Actions time nightly, a new failure mode, and cross-job state, to save pennies)
2026-07-13 · [claude] · structural · The Stage-A extract is written to news_cluster.extract ONLY, never to news_item.extract (amends plan 7.5) · news_item is the MOVERS' catalyst table, and Job B reads every row in it within its window and SYNCHRONOUSLY EXTRACTS every id that was not in the nightly batch · writing the market-wide feed there would fire ~200 extra Haiku calls a night AND feed the entire market feed into the evening briefing's synthesis — a different product, not a schema detail · verified by reading briefing/evening.py's _gather_extracts, not assumed
2026-07-13 · [claude] · structural · The note gate drops BOTH of a note's sentences when either is flagged, and it is harsher than the briefing's gate on purpose · the brief tolerates up to two flags across a whole page of prose because holding the entire briefing over one bad figure costs the reader everything; a note is ONE sentence, and dropping it costs the reader that sentence while the facts on the card stand without it · a model that invented one figure has not earned the benefit of the doubt on the next one · every outcome is COUNTED and the night prints them, because the failure mode here is SILENT: a null why_it_matters prints nothing, so a too-strict gate and a narrator with nothing to say look identical on screen
2026-07-13 · [claude] · structural · The verification gate reads a MULTIPLIER as a number, and refuses a decimal glued to letters instead of truncating it · the bare-number rule backtracked: faced with "2.1x" it produced the number "2", scored the phantom against the sources, found nothing, and flagged the sentence · "2.1x its usual volume" is exactly how anyone writes relative volume and 2.1 is a figure the pipeline computes, so EVERY honest note about relative volume would have been deleted — and a deleted note prints nothing, so no screen would ever have said so · the evening briefing's gate had this bug too and is fixed with it · a multiplier is now checked (so a made-up 9.9x is still refused) and the quantifiers are possessive
2026-07-13 · [claude] · structural · news_cluster gains an `articles` Json column, snapshotted at publish · `sources` was a bare Int — the card whispered "5 sources" and NOTHING anywhere named them, so the story page's source list (the first line of its spec) had no data and the second-heaviest term in the ranking formula (25%) was a number the reader simply had to believe · a corroboration count that cannot be opened is exactly the species of unverifiable claim this app argues against · the plan assumed news_item; see the decision above for why it cannot · the seed now REFUSES TO LOAD when a cluster's sources count disagrees with the number of articles behind it, because otherwise the card prints "5 sources" over a list of two and every test still passes
2026-07-13 · [claude] · structural · copy.news.ordering is reworded so it does not overclaim the ranking (amends Appendix B; answers Q-N4-1) · Appendix B named three discriminating signals — scope, corroboration, and the size of the move — and on the real feed two of them barely vary: corroboration is 1 for 131 of 134 clusters, magnitude is 0 for ~130, so nearly HALF the formula's weight sits nearly constant and ten-plus stories routinely tie at exactly 0.600 · the sentence now names the signals AND says the page ties, oldest-first · rejected: inventing a discriminator (a "US-market relevance" term would be the app forming an editorial opinion about which stories deserve the front page, which is precisely what ruling C1 forbids — on a day when the Gulf is on fire, a wall of Gulf stories IS the honest front page) · change the words, not the ranking
2026-07-13 · [claude] · structural · The catalyst filter chips are DERIVED from the feed, and include "Other" (amends Appendix B's fixed list) · the classifier's tenth class is `other` — the escape hatch that exists so it never has to force a bad fit — and the plan's chip list does not name it · ship the list verbatim and the reader sees 40 catalysts, adds up the chips, reaches 37, and has no way to open the missing three · a filter row that cannot reach a story sitting on the page is a cut nobody stated, which is ruling M8's disease wearing a filter's clothes · a unit test asserts every story on the page is reachable by some chip, and the e2e clicks every chip and asserts the counts sum to the page
2026-07-13 · [claude] · local · News card timestamps are ABSOLUTE (ET), never relative "3h ago" (amends plan 7.7's card anatomy) · /news is ISR-cached, so a relative timestamp is computed when the page is BUILT and then served frozen to every reader after · this build has shipped that exact bug TWICE — F4's cooling-off stamp and N4's "markets open" strip — and both times the string was well-formed and simply false · the room states its press time at the top, so between the two the reader knows exactly how old everything is, which is more than "3h ago" ever told them
2026-07-13 · [claude] · local · The L3/L4 generated news cards use ONE restrained ground and are distinguished by TYPOGRAPHY, not by a per-sector tint (amends plan 7.9) · twelve sector hues is twelve colours whose only job is decoration, and UI-REDESIGN-PLAN — which outranks the plan on looks by the user's own amendment — says colour is scarce and always means something · a reader cannot learn twelve hues, so they would carry no meaning while adding noise beside the two colours that DO mean something on a news card: up and down
2026-07-13 · [claude] · local · "This week" is never DISABLED when the archive is shallow; it opens and SAYS how deep it goes (amends plan 7.7) · a shallow archive does not make the week meaningless, it makes it short, and "everything I have, which is less than a week" is a true and useful answer · disabling the control would hide the coverage the app actually holds behind a greyed-out button · the plan's own copy string reads "showing all of it", so it was always a note rather than a refusal
2026-07-13 · [claude] · local · News is the SIXTH tab (Part 0.1, option A — the plan's default, taken because no DECISIONS line overrode it) · six tabs at 390px is ~65px each: past the 44px touch floor with 10px labels still legible, spending iOS's comfortable maximum of five plus one · the Desk module 08 doorway ships in BOTH options, so removing the tab stays a one-file change costing the reader one tap, not the room · the Desk gave up the newspaper icon (a room literally titled "Front page" sitting beside a differently-named room wearing the newspaper glyph is a bar that argues with itself) and took the sunrise, which is what the morning ritual actually is
2026-07-13 · [claude] · structural · Drift rule 20: news imagery renders through components/news/NewsImage.tsx and nowhere else · one door keeps three things that go quietly wrong when a second consumer appears: the ETIQUETTE (images are fetched at ingest into our own bucket; a hotlinked publisher URL at render steals their bandwidth on every page view and looks identical on screen), the ZERO LAYOUT SHIFT (explicit width/height from the stored row is what makes CLS zero by construction rather than by luck), and the FALLBACK RUNGS (a second consumer would render an empty box and call it a failure state, where the generated card is a designed outcome) · negative-controlled
2026-07-13 · [claude] · structural · Stage A and Stage B are BOUNDED — a 90-second per-call timeout, a 6-minute wall-clock budget on the extraction stage, and a failed call is a dropped article rather than a dead stage · the SDK's default per-call timeout is TEN MINUTES with retries on top, and Stage A makes up to 60 sequential calls, so the first live run sat in the extraction stage for over twenty minutes and had to be cancelled — the facts were already computed and sitting in memory while the page waited on a context line · the narrator runs LAST precisely so everything before it can publish without it, and a context line does not get to make the front page late · then ONE slow call took the whole stage down and the night reported "0 of 0 extracted" — sixty articles queued to be read and not one of them read, because the first was slow — because `sync_extract` skipped a failed PARSE but let an API exception unwind out of the loop, while its docstring said "A failed article is skipped" · a comment describing a robustness the code does not have is worse than no comment: it stops anyone from looking · and 30 seconds, my first guess at the bound, was itself too tight — a bound that trips on a healthy night is not a safety rail, it is an outage
2026-07-13 · [claude] · structural · The Stage-B prompt STATES the character limits, and each note is validated ALONE · the ≤160-character cap is enforced by pydantic on the way back in, and the API does not enforce string lengths — `api_schema` strips `maxLength` before sending because the structured-output layer rejects it — so the prompt was the ONLY place the model could learn the cap, and it never mentioned it: every note the narrator wrote was thrown away for breaking a rule nobody had given it · and the NoteSet was validated as ONE object, so a single over-long sentence invalidated the other nineteen (the "one bad item kills the batch" disease, one layer up from where it had just been fixed) · `_MAX_TOKENS` was also 4096, which twenty notes overflow — producing a TRUNCATED JSON document the tolerant parser cannot balance, reported as "malformed", with nothing anywhere saying "you ran out of room" · a token cap that fails silently is the worst kind
2026-07-13 · [claude] · structural · The seed's news JSON is copied from REAL production rows, and a test runs the whole seed through the app's own boundary reader · the fixture was hand-authored in N4 before the narrator existed and guessed at two shapes — `verification: {status: "dropped", flags: [<string>]}` and `key_numbers: [{id, label, value}]` — neither of which `newsdesk/narrate.py` has ever emitted · the app reads the real shapes, so it silently found nothing: the story page told the reader the narrator had nothing to add when the GATE HAD DELETED A LINE, and every key number in the seeded room was discarded on read, so the mono emphasis on a verified headline figure — the treatment whose entire meaning is "this was checked against its source" — had never once rendered · AND THE TESTS AGREED WITH THE FIXTURE: fixtures.test.ts asserted the invented shape and its hand-written TYPE declared it, so the fixture agreed with the test, the test agreed with the fixture, and neither agreed with the producer · this is N3's fabricated-fixture lesson in different clothes — there the lie was in the VALUES, here it is in the SHAPE, and the app reads shapes · the guard counts what goes into the reader against what comes out, because a silently-dropped key number is invisible to any test that merely asks "did some survive?" · negative-controlled on both halves
2026-07-13 · [claude] · local · /fixtures/ is public (the login wall's PUBLIC_PREFIXES) · the image optimizer does not proxy the reader's request — it makes its OWN server-side fetch of the source image, with no session cookie — so a same-origin image behind the wall got a 307 to /login and the optimizer reported "not a valid image" and served a 400 · EVERY photograph on the Front Page rendered as a broken-image icon while every DOM assertion passed: the <img> was present, visible, correctly sized and carrying the right src, and `naturalWidth` is the only thing in a browser that knows an image from a broken one · nothing is exposed (three generated placeholder JPEGs already committed to this repo; in production the images come from the media bucket, a remote origin with no middleware in the path) · found by the pixel oracle, and had the baselines been committed as they stood, every VRT gate from here on would have locked in a picture of the bug
2026-07-13 · [claude] · local · The lead card sets SIDEWAYS above `lg` — picture left at 55%, headline beside it · a 1.91:1 photograph is a comfortable 204px on a 390px phone and 715 PIXELS across a 1366px desktop column, so above the fold there was the masthead, the two filter rows and a picture, and nothing else: the lead story's own headline was below the fold · a front page whose lead headline you have to scroll to find is not a front page, it is a poster · rejected: capping the image height (it crops a photograph to solve a layout problem — the ratio was never wrong, the COLUMN was, and a broadsheet has always set its lead with the headline beside the picture)
2026-07-13 · [claude] · structural · `_json_safe` walks the whole structure, and a test asserts its output ACTUALLY SERIALIZES · it only ever inspected a scalar float and never recursed, so the datetime inside `news_cluster.articles` killed the entire publish transaction AFTER the run had spent four and a half minutes making every model call — the facts, the ranking and the prose were all computed correctly and not one row was written · a function named `_json_safe` that is not safe for JSON is worse than no function, because every caller believes the boundary is guarded · the test that matters calls json.dumps on the output rather than checking its shape, because a shape assertion passed on the broken version
2026-07-13 · [claude] · structural · The GitHub run id is RECOVERED, not received: each dispatch carries a `request_id`, the workflow prints it into `run-name:`, and the app matches it in the runs list (amends plan 8.2) · the plan said to dispatch with `return_run_details: true` because "the response carries workflow_run_id", and GitHub's OWN REST DOCS say the same — both are wrong, and I recorded the truth before writing the bridge: the dispatch answers **204 No Content with an empty body**, and `return_run_details` is not a parameter that exists · had this been built on the plan's claim, every dispatched run would have carried a null run id, could never have been polled, and would have sat on "requested…" forever — which is precisely the hazard this phase was warned about, a run that fired and a run that never fired looking identical from the couch · the plan listed the run-name match as a FALLBACK; it is the only path there is, which makes the `run-name:` line load-bearing (delete it and nothing fails, every test but one passes, and the control room goes permanently blind) · `pipeline/tests/test_workflow_dispatch.py` guards it, and also pins the workflow's mode dropdown to MODE_STAGES, because an option the job cannot run is a button that reports success and does nothing
2026-07-13 · [claude] · structural · `compute` mode publishes through a dedicated `publish_compute`, never through `publish()` · `_upsert_pipeline_run` sets `source_status = EXCLUDED.source_status` on conflict — a WHOLESALE REPLACE — so a recompute going through the ordinary publish would have OVERWRITTEN the night's record of how each provider behaved: a night on which Marketaux was degraded would start reporting every source healthy the moment the reader pressed "Recompute scans", with nothing anywhere saying so · that is ruling M2 ("a degraded source cannot be folded away") broken by the back door · a run that called NO PROVIDER knows nothing about any provider's health, and the honest thing for it to say about them is nothing at all — so this function never touches source_status and MERGES stage_status (`||`) rather than replacing it · verified in production: the merge held, the Jul 10 row's pre-existing stages survived and gained {compute, scan, publish} · (the same trap is documented one function up, where batch_id is COALESCE'd for exactly this reason; the status columns were simply never given the same care)
2026-07-13 · [claude] · structural · `compute` mode takes its run date from THE DATA, not the clock · the lake knows which session it holds; ask the clock and a recompute fired at noon on Tuesday stamps its scans with Tuesday — a session that has not closed, whose bars do not exist, and whose row would then sit on the Desk presenting itself as today's edition · this build has now shipped a wrong-clock bug three times (F4's cooling-off stamp, N4's markets-open strip, N5's relative timestamps) and this is the fourth place it would have happened · verified live: a compute run fired on a Monday correctly recomputed 2026-07-10, the Friday
2026-07-13 · [claude] · structural · `job_a` SKIPS a full run on a non-session day, and exits cleanly rather than failing · found by looking at N6's own panel against production, which opened with "Data through 2026-07-11" — a SATURDAY, a day with no session, no close and no bars: a full run had stamped Friday's data with Saturday's date and the Desk had been telling the reader its data ran through a day the market never opened · NOT A ONE-OFF: the cron is `37 22 * * 1-5`, so it never fires at a weekend but it DOES fire on every market HOLIDAY, which is a weekday — roughly nine times a year the job would wake on a closed market, ingest nothing, and publish a run dated to a session that did not happen, with every gate green · a skipped run on a closed market is the CORRECT outcome, not an error: failing the workflow would send a red e-mail every Thanksgiving, and an alert that cries wolf is not there on the night it is finally right · it also makes the promise the control room's `full` button already makes to the reader true of the JOB and not merely of the UI — a rule enforced only at the button is a rule the Actions tab walks straight past · the bad row is still in production and is Q-N6-1
2026-07-13 · [claude] · structural · The manual_run row is written ONLY AFTER GitHub accepts the dispatch (204), never before (extends plan 8.3) · a dispatch GitHub REFUSES requested nothing and ran nothing, so it must not leave behind a row claiming otherwise · writing the row first would let a bad token BURN THE DAY'S SINGLE `full` RUN — the recovery button, the most valuable control on the panel — for a run that never happened · plan 8.3 states the principle for the not-applicable and not-configured cases ("never stored rows: nothing was requested") and this is the same reasoning one case further · proven live: the first drill hit a 422 and the panel said "GitHub would not start the run. Nothing was dispatched.", wrote no row, and left the cap at "6 of 6 left today"
2026-07-13 · [claude] · structural · The panel gains a `lost` state (a UI state, never a stored row): a dispatched run that has not been found after 90 seconds is reported as never having appeared, and it stops blocking the other buttons · the dispatch returns no run id, so the app must HUNT for its run — and when the hunt never resolves (a revoked token, a renamed workflow, a GitHub incident) a panel that kept showing "requested…" would leave the reader watching a spinner forever for a run that is never coming, with nothing anywhere saying so · this is the phase's named hazard in its purest form: a silent failure mode needs a COUNT · it must also stop blocking, or one bad dispatch would freeze the entire panel until midnight with no way out
2026-07-13 · [claude] · local · A fact about the WORLD outranks a fact about our CONFIGURATION: a row shows `not_applicable` ABOVE `not_configured`, and the missing token is stated ONCE, above the rows (amends plan 8.5's state table) · checked the other way round, P-2's absence swallowed everything — every row read "not configured" and the four C5 sentences, the best writing on this panel and the entire point of plan 8.1, NEVER RENDERED AT ALL, so the reader could not even discover the product had thought about the question · "markets are open, so today's closing data does not exist yet" is true whether or not we hold a GitHub token · and the first build printed the token sentence on all five rows, which every test passed and one screenshot refuted: the STATE is per-row, the REASON is per-panel, and printing a shared reason five times is not five times as honest, it is five times as long
2026-07-13 · [claude] · local · NOTHING outranks a run that is happening right now — a live run is shown above a cap, above a cooldown, and above `not_applicable` · a `full` run writes its pipeline_run row at PUBLISH, near the END of its work, so for the last stretch of a recovery run "tonight's run already succeeded" is technically true WHILE the run the reader started is visibly still going · printing that over a live run is a lie about the present, and it is the most confusing thing this panel could do
2026-07-13 · [claude] · structural · The control room's row STATES are derived in the BROWSER, against the reader's clock; only the FACTS come from the server · caught by the pixel oracle: the first VRT baseline photographed the panel saying "Markets are open — today's closing data doesn't exist until 4:00pm ET" directly underneath a nav bar reading "MARKET CLOSED" — the nav grades in the browser, the panel was grading on the server, and CI happened to run at 3pm ET · that is N4's bug wearing a new surface (a Desk built at 3:55pm told readers "markets open" long past the close) and it had a SECOND head here: the baseline would have ROTTED ON ITS OWN, because every state on this panel turns on what time it is, so the picture would change with the hour CI happened to run and start failing with nobody having touched a line of code — which is exactly what the SEEDED_EVENING clock pin exists to prevent, and the panel walked straight around it · `controlPanel` is a pure function, so it costs nothing to run where the reader's clock is · the SERVER ACTION still grades against the server's clock, and must: a client clock is an input the caller controls, and "the market is closed, honest" is not something a form body gets to assert · there is one clock in this app that matters and it is the reader's, and the Desk's freshness strip settled that argument in N4
2026-07-13 · [claude] · local · The P-2 notice renders on a TINTED wash, not on `band` · `--color-band` is #6d648c — a DATA-VISUALISATION colour, for breadth bars and proportion segments — and I used it as a surface, so the single most important sentence on the panel rendered as light text on a mid-purple slab and was barely legible · found by opening the PNG; no contrast gate caught it because the element was not in the axe sweep's route list · the `new-surface` skill already names the right level for this ("tinted — a nested accent panel; base rates, cost mirrors, helper boxes") and I did not read it closely enough
2026-07-13 · [claude] · structural · The route sweeps assert on the page's BODY, never its status code, and every seeded room states the seed it needs · the touch-target, sideways-scroll and axe sweeps measured whatever was on screen, so an unseeded run of a story page measured the 404 PAGE and reported the story page clean — verified, the axe sweep passed /news/nc-fed-hold against a database that has never contained it · and the obvious fix does not work: a `notFound()` raised inside a statically-generated route answers **HTTP 200 with the 404 page in the body** (recorded: /news/nc-fed-hold 200, /ticker/NOTAREALTICKER 200, /scans/not-a-preset a real 404), so a status assertion certifies exactly the page it was written to refuse · the app already knew the status was wrong — an F-phase QUESTIONS note called it "a wrong status code, not a wrong screen", which is true of the READER and false of every GATE that trusts it · the sweeps also now COUNT what they measured, because zero controls in a room that always has a tab bar means the page never rendered · negative-controlled on every new guard
2026-07-13 · [claude] · structural · The Development Plan has ONE HTML source: docs/src/dp-*.html generates the markdown (build-plan-md.py) AND the print HTML + PDF (build-plan-pdf.py, new) · docs/src/development-plan.html was a second hand-maintained copy of the same document that nobody had decided to have, and the two drifted for a month: not ONE of the 2026-07-12 app-feel amendments had ever reached the PDF, whose route map lacked /scans/[preset] and still promised force-dynamic rendering the app had abandoned · the markdown was right and the PDF was wrong and both looked equally current, which is why nobody caught it — a hand-kept duplicate lies by default and grows more convincing with age, and the copy that rots is the one read least, which is the PDF the USER actually opens · the generator asserts the parts still concatenate into exactly one document (one DOCTYPE, one body, one /html) rather than trusting it, because a part that stopped closing its tags would print a plausible PDF missing its second half · rejected: hand-mirroring the edits into both copies (that is the disease, not the cure)
2026-07-13 · [claude] · local · /settings' two independent panel reads run in parallel, and B2 stays in REPORT MODE for that route · it is the app's one WRITER room and therefore the one route excluded from the ISR allowlist (a page may be cached, or written to and read back in the same click — never both), so it renders on request and pays real database time: 455ms measured at N7 against a 150ms budget, every sample a cache MISS · NOT a regression — F7 recorded 564ms in the same state, and every cached room answers in 50–103ms · readPanel() and the last-run query know nothing about each other and were awaited in series; parallelising them measured 455ms -> 434ms on the deployed app, about 20ms rather than the ~100ms round-trip I predicted, so THESE TWO QUERIES WERE NEVER THE ROOM'S PROBLEM — the change is kept because a sequential await of two independent reads is wrong however little it costs, but the remaining ~280ms lives somewhere I have not measured and I did not go looking, because a 434ms settings page opened rarely and deliberately is not a defect worth a phase · the budget is not gated for this route because 150ms describes a CACHED room, and gating a number that cannot be met without breaking the read-after-write would be a gate that exists to be ignored
2026-07-13 · [claude] · structural · The seeded paper trades are dated RELATIVE to the seed run (DAYS_AGO), never absolutely · the app counts "round trips this week" as closedAt >= now − 7 days — a window that rolls against the wall clock, which is the correct product behaviour — and an absolute fixture date cannot stay inside a rolling window, it can only wait to fall out of it · it fell out mid-phase: the 2026-07-06T19:50Z trade was inside the week for nc-6's CI (19:22Z) and outside it for nc-final's (20:39Z), so the /paper baselines failed with nobody having touched a line of code, and the cost mirror silently halved from −31.2%/yr to −15.6%/yr · re-shooting the baseline would have bought exactly ONE DAY before the next trade aged out and the count read zero — a green re-baseline that hid a still-decaying fixture · two closed trips now sit at 3 and 2 days ago and one deliberately at 11, so the count is 2 at every hour, forever · no pixel cost: PaperLedger renders no dates at all
2026-07-13 · [claude] · local · The story page's source links are TAP TARGETS (py-3 on touch, md:py-0 on desktop) · found the moment N7 finally put /news/[cluster] into the touch sweep, which had never measured the room: "CNBC" 39×20, "Reuters" 48×20, "Associated Press" 110×20 — five stacked links, each under half the 44px floor the constitution sets · these are the links a reader uses to check a story against the outlet that reported it, which is the room's entire honesty argument, so they are the LAST controls in the app that should be hard to hit · WCAG's inline exception does not apply and should not: it exempts a target sitting "in a sentence or block of text", and this is a list of discrete controls (the <li> is a flex container, so the anchor is blockified and sits in no line box at all — which is why the sweep measured it rather than exempting it) · desktop pixels unchanged; the two phone story baselines are updated in the same commit with the reason, per the vrt-update skill
2026-07-13 · [claude] · local · The control-room e2e asserts the invariant that holds at EVERY hour, not the one that held at 3pm · it demanded the `full` row always carry a reason, which passed at nc-6 (CI ran with the market open, so the row said "Markets are open") and failed at nc-final (CI ran after the close, when `full` is genuinely runnable and has nothing to explain) — the clock moved and a green test turned red · it also contradicted N6's ruling that the token reason is printed once above the rows and never per row, so it was demanding the row repeat the panel · the assertion is now: the row states a reason, OR offers a button, OR the panel explains the absence — the reader is never left with a control that neither works nor says why · rejected: pinning the clock (the fix for a time-dependent test is to find the sentence that was true all along, not to freeze the hour that made a wrong sentence look right)

2026-07-13 · [claude] · structural · A phase-exit TAG runs exactly ONE job — the browser oracle — and never re-runs app+pipeline (GATE-EFFICIENCY-PLAN G0, analysis §2.3-#5) · the tag points at a SHA that passed both jobs on main *seconds* earlier (measured: nc-final's green tag run 29286543491 re-ran them against the identical commit its main run 29286513129 had just certified), so the second execution consumed ~2.9 billed minutes to learn nothing that was not already known · this was ~43% of the entire build's CI minutes · the oracle is the ONLY thing a tag adds, so it is the only thing a tag runs · the guard is not weakened: app+pipeline still gate every branch push, and the full browser suite still runs on the exact shipped SHA, which the untouchables require
2026-07-13 · [claude] · structural · ci.yml gets a per-ref concurrency group with cancel-in-progress · nothing cancelled anything before: nc-final's endgame left a 785-second run and a 229-second run alive on tags that had already been superseded, and a 13-minute zombie once had to be killed by hand · branch refs and tag refs are DISTINCT groups, which matters — otherwise tagging a commit would cancel that commit's own main run, which is the run that proves it · proven live: a superseded run died in 40 seconds
2026-07-13 · [claude] · structural · check:drift joins the CI app job · twenty anti-drift rules, a one-second grep, and until G0 it ran in NO CI workflow at all — it was local discipline only, so a session that forgot gate step 5 was corrected by nothing (analysis §4.8) · the local step stays; this is the backstop for the forgotten one · it is the cheapest guard in the build and it was the only one with no mechanical enforcement
2026-07-13 · [claude] · structural · The tag-family wiring in ci.yml is now enforced by a test (pipeline/tests/test_ci_tag_families.py), not by a comment · ci.yml's header has warned since the first phase that the trigger list and the oracle's `if:` must be kept in step, "a tag family that appears in one but not the other is a gate that silently never runs" — and a comment cannot fail a build · it caught the real bug on its first run: mid-edit, gate-* and pd-* were in the trigger but not yet in the oracle's `if:`, which is the exact state where `git tag gate-0` starts a run, the run goes GREEN, and the only job that can see pixels never executes · rejected: adding PyYAML to parse the file (the house pattern, test_workflow_dispatch.py, greps the YAML as text and that is sufficient for a small file we control)
2026-07-13 · [claude] · local · pd-* is wired into ci.yml NOW, three phases before the Polish build starts · it costs one glob and it retires PD0's first work item, which is the item the F0 lesson says is most dangerous to defer ("an unwired tag pattern makes every later gate claim ornamental") · POLISH-AND-DEPTH-PLAN.md's PD0 is annotated with a dated note telling the next builder to VERIFY the wiring rather than redo or distrust it · the annotation also corrects a detail PD0 gets wrong: there is exactly ONE tag-gated job (e2e), not two — vrt-baselines is gated on workflow_dispatch and never on a tag

2026-07-13 · [claude] · structural · The browser oracle is dispatchable — `gh workflow run ci.yml -f job=e2e` runs it on any ref, before any tag exists (GATE-EFFICIENCY-PLAN G1, analysis §2.3-#1) · it is not a copy of the oracle but the SAME job with a second door in its `if:`, so a rehearsal cannot go green while the tag run reds — there is one oracle, invoked twice · this is the phase's whole thesis: the oracle used to run for the FIRST time on the tag, which made the tag the first real test (52% of tag runs failed; nc-final needed six pushes of one tag), and a suite that runs before "done" is declared turns the tag run into a confirmation instead of a discovery · proven: `gate-1` went green on the first try, one tag push, the tag never moved
2026-07-13 · [claude] · structural · ci.yml's concurrency group is `ci-<ref>-<event_name>`, and the event part is load-bearing · G0 grouped on the ref alone, which was correct until the rehearsal existed and fatal the moment it did: a rehearsal dispatched on main carries `github.ref == refs/heads/main`, the same ref and therefore the same group as main's own push run · on its FIRST outing the rehearsal (run 29291878002) cancelled the push run of the very commit it was rehearsing (29291818293) · the reverse is the dangerous direction — a docs commit pushed during a 9-minute rehearsal would kill the rehearsal, and the exit would walk to its tag believing it had rehearsed, which is the "gate that silently never runs" disease arriving through a door nobody had thought about · the exit ritual overlaps the branch push and the rehearsal ON PURPOSE, so they must be able to coexist · every supersede-cancel G0 bought still holds (push-vs-push and tag-vs-retagged-tag share an event)
2026-07-13 · [claude] · structural · The oracle is SHARDED into three matrix legs — desktop, phone, wide — each with its own Postgres service container · measured 15 m 26 s -> 7 m 58 s wall-clock (−46%) · `playwright.config.ts` is NOT edited and `workers: 1` is NOT weakened: that rule exists because seeded specs write to the database (watchlist adds) and two specs sharing ONE database could race — three legs are three throwaway databases that cannot see each other, which dissolves the sharing without touching the rule, so each leg is still one worker, still serial, still seeing exactly the seed it expects · `fail-fast: false`, because at an exit you want the whole truth from one run, not the first failure and another 8-minute round trip to learn the rest · THE TRADE, booked openly: billed job-minutes go UP (~14.8 -> ~18.6, +26%) because three legs each pay their own npm ci, Playwright install, migrate+seed and production build · that is the right trade — the thing this plan exists to kill is a human waiting at an exit, several times a phase, and CI minutes are cheap next to that
2026-07-13 · [claude] · structural · A run that reds on PIXELS mints its own candidate baselines, guarded by the `*-actual.png` witness · Playwright writes `test-results/**/*-actual.png` when and only when a snapshot COMPARISON fails, so a red assertion ("the chip says the wrong word") leaves no such file and must not buy four minutes of photography · ORDERING IS LOAD-BEARING: Playwright empties its output directory at the start of every run, so the mint's re-run of vrt.spec.ts DELETES test-results/ — including the expected/actual/diff triptych, the one thing a pixel failure owes you — and the failure-artifact upload therefore runs BEFORE the mint · the artifact is a CANDIDATE and is never auto-committed; no job in this repo can write to a branch · proven on a scratch branch with a deliberate one-line visual change: desktop and phone red on pixels, wide (which has no styleguide baseline) went green and finished anyway, four artifacts appeared, the diff image showed exactly the nudge, and the branch was deleted without merging
2026-07-13 · [claude] · local · Tag the SHA you rehearsed, BY SHA — never `HEAD` · nightly-a.yml pushes an empty `chore: heartbeat` commit to main after each full nightly run (GitHub disables a private repo's scheduled workflows after 60 idle days; one empty commit a night keeps the cron alive) · it is legitimate machinery, but it means MAIN CAN MOVE UNDER AN ENDGAME — it landed mid-exit tonight and rejected a push · if it lands between the rehearsal and the tag, tagging `HEAD` would put the tag on a commit the oracle never saw · the tree is byte-identical so today's risk is nil, but "the full suite runs on the exact tagged SHA" is an untouchable and it should hold in letter, not in luck
